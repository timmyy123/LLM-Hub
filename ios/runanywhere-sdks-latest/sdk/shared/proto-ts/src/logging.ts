// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: logging.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

/**
 * ---------------------------------------------------------------------------
 * Severity, mirroring the C ABI `rac_log_level_t`. Larger value = more severe.
 * 0 is TRACE (not UNSPECIFIED) to keep numeric parity with the C enum — the
 * same C-ABI-aligned convention used by HttpDownloadStatus (0=OK) and
 * SdkInitEnvironment (0=DEVELOPMENT).
 * ---------------------------------------------------------------------------
 */
export enum LogLevel {
  LOG_LEVEL_TRACE = 0,
  LOG_LEVEL_DEBUG = 1,
  LOG_LEVEL_INFO = 2,
  LOG_LEVEL_WARNING = 3,
  LOG_LEVEL_ERROR = 4,
  LOG_LEVEL_FATAL = 5,
  UNRECOGNIZED = -1,
}

export function logLevelFromJSON(object: any): LogLevel {
  switch (object) {
    case 0:
    case "LOG_LEVEL_TRACE":
      return LogLevel.LOG_LEVEL_TRACE;
    case 1:
    case "LOG_LEVEL_DEBUG":
      return LogLevel.LOG_LEVEL_DEBUG;
    case 2:
    case "LOG_LEVEL_INFO":
      return LogLevel.LOG_LEVEL_INFO;
    case 3:
    case "LOG_LEVEL_WARNING":
      return LogLevel.LOG_LEVEL_WARNING;
    case 4:
    case "LOG_LEVEL_ERROR":
      return LogLevel.LOG_LEVEL_ERROR;
    case 5:
    case "LOG_LEVEL_FATAL":
      return LogLevel.LOG_LEVEL_FATAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return LogLevel.UNRECOGNIZED;
  }
}

export function logLevelToJSON(object: LogLevel): string {
  switch (object) {
    case LogLevel.LOG_LEVEL_TRACE:
      return "LOG_LEVEL_TRACE";
    case LogLevel.LOG_LEVEL_DEBUG:
      return "LOG_LEVEL_DEBUG";
    case LogLevel.LOG_LEVEL_INFO:
      return "LOG_LEVEL_INFO";
    case LogLevel.LOG_LEVEL_WARNING:
      return "LOG_LEVEL_WARNING";
    case LogLevel.LOG_LEVEL_ERROR:
      return "LOG_LEVEL_ERROR";
    case LogLevel.LOG_LEVEL_FATAL:
      return "LOG_LEVEL_FATAL";
    case LogLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * SDK logging configuration. Per-environment presets
 * (development/staging/production) stay in each SDK as factory helpers.
 * ---------------------------------------------------------------------------
 */
export interface LoggingConfiguration {
  /** Write logs to the platform-local sink (os_log / Logcat / console). */
  enableLocalLogging: boolean;
  /** Minimum severity emitted. Messages below this level are dropped. */
  minLogLevel: LogLevel;
  /** Attach file:line:function source location to each record. */
  includeSourceLocation: boolean;
  /** Attach device/build metadata (model, os version, app build) to records. */
  includeDeviceMetadata: boolean;
  /** Forward records to the remote logging pipeline. */
  enableRemoteLogging: boolean;
}

/**
 * ---------------------------------------------------------------------------
 * A single structured log record. Mirrors the per-SDK LogEntry shape.
 * ---------------------------------------------------------------------------
 */
export interface LogEntry {
  /** Wall-clock epoch milliseconds. */
  timestampUnixMs: number;
  level: LogLevel;
  /** Subsystem/tag (e.g. "STT", "Download"). */
  category: string;
  message: string;
  /** Optional structured context. */
  metadata: { [key: string]: string };
  /**
   * Optional source location + context (Kotlin LogEntry carries these as
   * first-class fields; other SDKs leave them empty). `line`/`error_code`
   * use 0 as "unset".
   */
  file: string;
  line: number;
  function: string;
  /** SDKError code, when the record describes an error. */
  errorCode: number;
  modelId: string;
  framework: string;
}

export interface LogEntry_MetadataEntry {
  key: string;
  value: string;
}

function createBaseLoggingConfiguration(): LoggingConfiguration {
  return {
    enableLocalLogging: false,
    minLogLevel: 0,
    includeSourceLocation: false,
    includeDeviceMetadata: false,
    enableRemoteLogging: false,
  };
}

export const LoggingConfiguration: MessageFns<LoggingConfiguration> = {
  encode(message: LoggingConfiguration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enableLocalLogging !== false) {
      writer.uint32(8).bool(message.enableLocalLogging);
    }
    if (message.minLogLevel !== 0) {
      writer.uint32(16).int32(message.minLogLevel);
    }
    if (message.includeSourceLocation !== false) {
      writer.uint32(24).bool(message.includeSourceLocation);
    }
    if (message.includeDeviceMetadata !== false) {
      writer.uint32(32).bool(message.includeDeviceMetadata);
    }
    if (message.enableRemoteLogging !== false) {
      writer.uint32(40).bool(message.enableRemoteLogging);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoggingConfiguration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoggingConfiguration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enableLocalLogging = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minLogLevel = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.includeSourceLocation = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.includeDeviceMetadata = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.enableRemoteLogging = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoggingConfiguration {
    return {
      enableLocalLogging: isSet(object.enableLocalLogging)
        ? globalThis.Boolean(object.enableLocalLogging)
        : isSet(object.enable_local_logging)
        ? globalThis.Boolean(object.enable_local_logging)
        : false,
      minLogLevel: isSet(object.minLogLevel)
        ? logLevelFromJSON(object.minLogLevel)
        : isSet(object.min_log_level)
        ? logLevelFromJSON(object.min_log_level)
        : 0,
      includeSourceLocation: isSet(object.includeSourceLocation)
        ? globalThis.Boolean(object.includeSourceLocation)
        : isSet(object.include_source_location)
        ? globalThis.Boolean(object.include_source_location)
        : false,
      includeDeviceMetadata: isSet(object.includeDeviceMetadata)
        ? globalThis.Boolean(object.includeDeviceMetadata)
        : isSet(object.include_device_metadata)
        ? globalThis.Boolean(object.include_device_metadata)
        : false,
      enableRemoteLogging: isSet(object.enableRemoteLogging)
        ? globalThis.Boolean(object.enableRemoteLogging)
        : isSet(object.enable_remote_logging)
        ? globalThis.Boolean(object.enable_remote_logging)
        : false,
    };
  },

  toJSON(message: LoggingConfiguration): unknown {
    const obj: any = {};
    if (message.enableLocalLogging !== false) {
      obj.enableLocalLogging = message.enableLocalLogging;
    }
    if (message.minLogLevel !== 0) {
      obj.minLogLevel = logLevelToJSON(message.minLogLevel);
    }
    if (message.includeSourceLocation !== false) {
      obj.includeSourceLocation = message.includeSourceLocation;
    }
    if (message.includeDeviceMetadata !== false) {
      obj.includeDeviceMetadata = message.includeDeviceMetadata;
    }
    if (message.enableRemoteLogging !== false) {
      obj.enableRemoteLogging = message.enableRemoteLogging;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoggingConfiguration>, I>>(base?: I): LoggingConfiguration {
    return LoggingConfiguration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoggingConfiguration>, I>>(object: I): LoggingConfiguration {
    const message = createBaseLoggingConfiguration();
    message.enableLocalLogging = object.enableLocalLogging ?? false;
    message.minLogLevel = object.minLogLevel ?? 0;
    message.includeSourceLocation = object.includeSourceLocation ?? false;
    message.includeDeviceMetadata = object.includeDeviceMetadata ?? false;
    message.enableRemoteLogging = object.enableRemoteLogging ?? false;
    return message;
  },
};

function createBaseLogEntry(): LogEntry {
  return {
    timestampUnixMs: 0,
    level: 0,
    category: "",
    message: "",
    metadata: {},
    file: "",
    line: 0,
    function: "",
    errorCode: 0,
    modelId: "",
    framework: "",
  };
}

export const LogEntry: MessageFns<LogEntry> = {
  encode(message: LogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestampUnixMs !== 0) {
      writer.uint32(8).int64(message.timestampUnixMs);
    }
    if (message.level !== 0) {
      writer.uint32(16).int32(message.level);
    }
    if (message.category !== "") {
      writer.uint32(26).string(message.category);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      LogEntry_MetadataEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    if (message.file !== "") {
      writer.uint32(50).string(message.file);
    }
    if (message.line !== 0) {
      writer.uint32(56).int32(message.line);
    }
    if (message.function !== "") {
      writer.uint32(66).string(message.function);
    }
    if (message.errorCode !== 0) {
      writer.uint32(72).int32(message.errorCode);
    }
    if (message.modelId !== "") {
      writer.uint32(82).string(message.modelId);
    }
    if (message.framework !== "") {
      writer.uint32(90).string(message.framework);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestampUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.level = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = LogEntry_MetadataEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.metadata[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.line = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.function = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.framework = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogEntry {
    return {
      timestampUnixMs: isSet(object.timestampUnixMs)
        ? globalThis.Number(object.timestampUnixMs)
        : isSet(object.timestamp_unix_ms)
        ? globalThis.Number(object.timestamp_unix_ms)
        : 0,
      level: isSet(object.level) ? logLevelFromJSON(object.level) : 0,
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      file: isSet(object.file) ? globalThis.String(object.file) : "",
      line: isSet(object.line) ? globalThis.Number(object.line) : 0,
      function: isSet(object.function) ? globalThis.String(object.function) : "",
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      framework: isSet(object.framework) ? globalThis.String(object.framework) : "",
    };
  },

  toJSON(message: LogEntry): unknown {
    const obj: any = {};
    if (message.timestampUnixMs !== 0) {
      obj.timestampUnixMs = Math.round(message.timestampUnixMs);
    }
    if (message.level !== 0) {
      obj.level = logLevelToJSON(message.level);
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.file !== "") {
      obj.file = message.file;
    }
    if (message.line !== 0) {
      obj.line = Math.round(message.line);
    }
    if (message.function !== "") {
      obj.function = message.function;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.framework !== "") {
      obj.framework = message.framework;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogEntry>, I>>(base?: I): LogEntry {
    return LogEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogEntry>, I>>(object: I): LogEntry {
    const message = createBaseLogEntry();
    message.timestampUnixMs = object.timestampUnixMs ?? 0;
    message.level = object.level ?? 0;
    message.category = object.category ?? "";
    message.message = object.message ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.file = object.file ?? "";
    message.line = object.line ?? 0;
    message.function = object.function ?? "";
    message.errorCode = object.errorCode ?? 0;
    message.modelId = object.modelId ?? "";
    message.framework = object.framework ?? "";
    return message;
  },
};

function createBaseLogEntry_MetadataEntry(): LogEntry_MetadataEntry {
  return { key: "", value: "" };
}

export const LogEntry_MetadataEntry: MessageFns<LogEntry_MetadataEntry> = {
  encode(message: LogEntry_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogEntry_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogEntry_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogEntry_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: LogEntry_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LogEntry_MetadataEntry>, I>>(base?: I): LogEntry_MetadataEntry {
    return LogEntry_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LogEntry_MetadataEntry>, I>>(object: I): LogEntry_MetadataEntry {
    const message = createBaseLogEntry_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
