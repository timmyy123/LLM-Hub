// Prefer subpath imports such as `@runanywhere/proto-ts/llm_service`.
// The generated ts-proto files each define helper types with identical names
// (`DeepPartial`, `Exact`, `protobufPackage`), so the package root intentionally
// avoids star re-exports that would create ambiguous bindings.
export {};
