// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: pipeline.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

export enum DeviceAffinity {
  DEVICE_AFFINITY_UNSPECIFIED = 0,
  DEVICE_AFFINITY_ANY = 1,
  DEVICE_AFFINITY_CPU = 2,
  DEVICE_AFFINITY_GPU = 3,
  /** DEVICE_AFFINITY_ANE - Apple Neural Engine */
  DEVICE_AFFINITY_ANE = 4,
  UNRECOGNIZED = -1,
}

export function deviceAffinityFromJSON(object: any): DeviceAffinity {
  switch (object) {
    case 0:
    case "DEVICE_AFFINITY_UNSPECIFIED":
      return DeviceAffinity.DEVICE_AFFINITY_UNSPECIFIED;
    case 1:
    case "DEVICE_AFFINITY_ANY":
      return DeviceAffinity.DEVICE_AFFINITY_ANY;
    case 2:
    case "DEVICE_AFFINITY_CPU":
      return DeviceAffinity.DEVICE_AFFINITY_CPU;
    case 3:
    case "DEVICE_AFFINITY_GPU":
      return DeviceAffinity.DEVICE_AFFINITY_GPU;
    case 4:
    case "DEVICE_AFFINITY_ANE":
      return DeviceAffinity.DEVICE_AFFINITY_ANE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceAffinity.UNRECOGNIZED;
  }
}

export function deviceAffinityToJSON(object: DeviceAffinity): string {
  switch (object) {
    case DeviceAffinity.DEVICE_AFFINITY_UNSPECIFIED:
      return "DEVICE_AFFINITY_UNSPECIFIED";
    case DeviceAffinity.DEVICE_AFFINITY_ANY:
      return "DEVICE_AFFINITY_ANY";
    case DeviceAffinity.DEVICE_AFFINITY_CPU:
      return "DEVICE_AFFINITY_CPU";
    case DeviceAffinity.DEVICE_AFFINITY_GPU:
      return "DEVICE_AFFINITY_GPU";
    case DeviceAffinity.DEVICE_AFFINITY_ANE:
      return "DEVICE_AFFINITY_ANE";
    case DeviceAffinity.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EdgePolicy {
  EDGE_POLICY_UNSPECIFIED = 0,
  /** EDGE_POLICY_BLOCK - Producer blocks when channel is full (default, safest). */
  EDGE_POLICY_BLOCK = 1,
  /** EDGE_POLICY_DROP_OLDEST - Oldest item is dropped when channel is full (audio routing only). */
  EDGE_POLICY_DROP_OLDEST = 2,
  /** EDGE_POLICY_DROP_NEWEST - Newest item is dropped when channel is full (pager coalescing). */
  EDGE_POLICY_DROP_NEWEST = 3,
  UNRECOGNIZED = -1,
}

export function edgePolicyFromJSON(object: any): EdgePolicy {
  switch (object) {
    case 0:
    case "EDGE_POLICY_UNSPECIFIED":
      return EdgePolicy.EDGE_POLICY_UNSPECIFIED;
    case 1:
    case "EDGE_POLICY_BLOCK":
      return EdgePolicy.EDGE_POLICY_BLOCK;
    case 2:
    case "EDGE_POLICY_DROP_OLDEST":
      return EdgePolicy.EDGE_POLICY_DROP_OLDEST;
    case 3:
    case "EDGE_POLICY_DROP_NEWEST":
      return EdgePolicy.EDGE_POLICY_DROP_NEWEST;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EdgePolicy.UNRECOGNIZED;
  }
}

export function edgePolicyToJSON(object: EdgePolicy): string {
  switch (object) {
    case EdgePolicy.EDGE_POLICY_UNSPECIFIED:
      return "EDGE_POLICY_UNSPECIFIED";
    case EdgePolicy.EDGE_POLICY_BLOCK:
      return "EDGE_POLICY_BLOCK";
    case EdgePolicy.EDGE_POLICY_DROP_OLDEST:
      return "EDGE_POLICY_DROP_OLDEST";
    case EdgePolicy.EDGE_POLICY_DROP_NEWEST:
      return "EDGE_POLICY_DROP_NEWEST";
    case EdgePolicy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Pipeline lifecycle status — shared by compile/start/stop results.
 * ---------------------------------------------------------------------------
 */
export enum PipelineStatus {
  PIPELINE_STATUS_UNSPECIFIED = 0,
  PIPELINE_STATUS_OK = 1,
  PIPELINE_STATUS_FAILED = 2,
  UNRECOGNIZED = -1,
}

export function pipelineStatusFromJSON(object: any): PipelineStatus {
  switch (object) {
    case 0:
    case "PIPELINE_STATUS_UNSPECIFIED":
      return PipelineStatus.PIPELINE_STATUS_UNSPECIFIED;
    case 1:
    case "PIPELINE_STATUS_OK":
      return PipelineStatus.PIPELINE_STATUS_OK;
    case 2:
    case "PIPELINE_STATUS_FAILED":
      return PipelineStatus.PIPELINE_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PipelineStatus.UNRECOGNIZED;
  }
}

export function pipelineStatusToJSON(object: PipelineStatus): string {
  switch (object) {
    case PipelineStatus.PIPELINE_STATUS_UNSPECIFIED:
      return "PIPELINE_STATUS_UNSPECIFIED";
    case PipelineStatus.PIPELINE_STATUS_OK:
      return "PIPELINE_STATUS_OK";
    case PipelineStatus.PIPELINE_STATUS_FAILED:
      return "PIPELINE_STATUS_FAILED";
    case PipelineStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A pipeline is a labelled DAG of operators connected by typed edges. There
 * are no cycles. Every input edge has a resolvable producer; every output
 * edge has at least one consumer.
 */
export interface PipelineSpec {
  /** Human-readable, e.g. "voice_agent_basic" */
  name: string;
  operators: OperatorSpec[];
  edges: EdgeSpec[];
  options?: PipelineOptions | undefined;
}

export interface OperatorSpec {
  /**
   * Unique within the spec, used as the prefix in edge endpoints like
   * "stt.final" or "llm.token".
   */
  name: string;
  /**
   * The primitive the operator implements: "generate_text", "transcribe",
   * "synthesize", "detect_voice", "embed", "rerank", "tokenize", "window",
   * or a solution-declared custom operator ("AudioSource", "AudioSink",
   * "SentenceDetector", "VectorSearch", "ContextBuild").
   */
  type: string;
  /**
   * Free-form parameters interpreted by the operator. The C++ loader
   * validates required keys per type before instantiating.
   */
  params: { [key: string]: string };
  /**
   * Optional override of the engine that will serve this operator. When
   * empty, the L3 router picks based on capability + model format.
   */
  pinnedEngine: string;
  /** Optional model identifier (resolved against the model registry). */
  modelId: string;
  /**
   * Affinity hint: run this operator on CPU, GPU, or Neural Engine. The
   * scheduler may override if the requested device is unavailable.
   */
  device: DeviceAffinity;
}

export interface OperatorSpec_ParamsEntry {
  key: string;
  value: string;
}

export interface EdgeSpec {
  /**
   * Endpoints are formatted "<operator_name>.<port_name>".
   * Source port names are operator-specific output channels; sink port
   * names are operator-specific input channels. Typing is enforced by the
   * pipeline validator.
   */
  from: string;
  to: string;
  /**
   * Channel depth override. Proto3 scalars have no presence bit, so the
   * sentinel value 0 means "use the per-edge default (16 for PCM, 256 for
   * tokens, 32 for sentences)". uint32 keeps the wire representation
   * identical to int32 on the happy path while making negative inputs
   * statically unrepresentable.
   */
  capacity: number;
  policy: EdgePolicy;
}

export interface PipelineOptions {
  /**
   * Maximum end-to-end latency budget in milliseconds. The pipeline emits
   * a MetricsEvent with is_over_budget=true if exceeded.
   */
  latencyBudgetMs: number;
  /**
   * When true, the pipeline emits MetricsEvent on every VAD barge-in and
   * on pipeline stop.
   */
  emitMetrics: boolean;
  /**
   * When true, the pipeline validates the DAG for deadlocks and
   * disconnected edges before running.
   */
  strictValidation: boolean;
}

/** Result of compiling a PipelineSpec into a runnable graph. */
export interface PipelineCompileResult {
  /** Opaque compiled-graph identifier. Empty on failure. */
  handleId: string;
  status: PipelineStatus;
  errorMessage?: string | undefined;
  errorCode: number;
}

/** Request to start a previously compiled pipeline. */
export interface PipelineStartRequest {
  /** Identifier returned by Compile. Required. */
  handleId: string;
}

/** Live pipeline instance handle. */
export interface PipelineHandle {
  /** Stable identifier for the started pipeline instance. */
  handleId: string;
  status: PipelineStatus;
  /** Optional engine-specific state string (e.g. "running", "stopped"). */
  state?: string | undefined;
}

/** Result of stopping a pipeline instance. */
export interface PipelineStopResult {
  handleId: string;
  status: PipelineStatus;
  errorMessage?: string | undefined;
  errorCode: number;
}

function createBasePipelineSpec(): PipelineSpec {
  return { name: "", operators: [], edges: [], options: undefined };
}

export const PipelineSpec: MessageFns<PipelineSpec> = {
  encode(message: PipelineSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.operators) {
      OperatorSpec.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.edges) {
      EdgeSpec.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.options !== undefined) {
      PipelineOptions.encode(message.options, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operators.push(OperatorSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.edges.push(EdgeSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.options = PipelineOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineSpec {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      operators: globalThis.Array.isArray(object?.operators)
        ? object.operators.map((e: any) => OperatorSpec.fromJSON(e))
        : [],
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => EdgeSpec.fromJSON(e)) : [],
      options: isSet(object.options) ? PipelineOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: PipelineSpec): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.operators?.length) {
      obj.operators = message.operators.map((e) => OperatorSpec.toJSON(e));
    }
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => EdgeSpec.toJSON(e));
    }
    if (message.options !== undefined) {
      obj.options = PipelineOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineSpec>, I>>(base?: I): PipelineSpec {
    return PipelineSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineSpec>, I>>(object: I): PipelineSpec {
    const message = createBasePipelineSpec();
    message.name = object.name ?? "";
    message.operators = object.operators?.map((e) => OperatorSpec.fromPartial(e)) || [];
    message.edges = object.edges?.map((e) => EdgeSpec.fromPartial(e)) || [];
    message.options = (object.options !== undefined && object.options !== null)
      ? PipelineOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseOperatorSpec(): OperatorSpec {
  return { name: "", type: "", params: {}, pinnedEngine: "", modelId: "", device: 0 };
}

export const OperatorSpec: MessageFns<OperatorSpec> = {
  encode(message: OperatorSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    globalThis.Object.entries(message.params).forEach(([key, value]: [string, string]) => {
      OperatorSpec_ParamsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.pinnedEngine !== "") {
      writer.uint32(34).string(message.pinnedEngine);
    }
    if (message.modelId !== "") {
      writer.uint32(42).string(message.modelId);
    }
    if (message.device !== 0) {
      writer.uint32(48).int32(message.device);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OperatorSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = OperatorSpec_ParamsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.params[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pinnedEngine = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.device = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorSpec {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      params: isObject(object.params)
        ? (globalThis.Object.entries(object.params) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      pinnedEngine: isSet(object.pinnedEngine)
        ? globalThis.String(object.pinnedEngine)
        : isSet(object.pinned_engine)
        ? globalThis.String(object.pinned_engine)
        : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      device: isSet(object.device) ? deviceAffinityFromJSON(object.device) : 0,
    };
  },

  toJSON(message: OperatorSpec): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.params) {
      const entries = globalThis.Object.entries(message.params) as [string, string][];
      if (entries.length > 0) {
        obj.params = {};
        entries.forEach(([k, v]) => {
          obj.params[k] = v;
        });
      }
    }
    if (message.pinnedEngine !== "") {
      obj.pinnedEngine = message.pinnedEngine;
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.device !== 0) {
      obj.device = deviceAffinityToJSON(message.device);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperatorSpec>, I>>(base?: I): OperatorSpec {
    return OperatorSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperatorSpec>, I>>(object: I): OperatorSpec {
    const message = createBaseOperatorSpec();
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    message.params = (globalThis.Object.entries(object.params ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.pinnedEngine = object.pinnedEngine ?? "";
    message.modelId = object.modelId ?? "";
    message.device = object.device ?? 0;
    return message;
  },
};

function createBaseOperatorSpec_ParamsEntry(): OperatorSpec_ParamsEntry {
  return { key: "", value: "" };
}

export const OperatorSpec_ParamsEntry: MessageFns<OperatorSpec_ParamsEntry> = {
  encode(message: OperatorSpec_ParamsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OperatorSpec_ParamsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperatorSpec_ParamsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperatorSpec_ParamsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: OperatorSpec_ParamsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperatorSpec_ParamsEntry>, I>>(base?: I): OperatorSpec_ParamsEntry {
    return OperatorSpec_ParamsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperatorSpec_ParamsEntry>, I>>(object: I): OperatorSpec_ParamsEntry {
    const message = createBaseOperatorSpec_ParamsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseEdgeSpec(): EdgeSpec {
  return { from: "", to: "", capacity: 0, policy: 0 };
}

export const EdgeSpec: MessageFns<EdgeSpec> = {
  encode(message: EdgeSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== "") {
      writer.uint32(10).string(message.from);
    }
    if (message.to !== "") {
      writer.uint32(18).string(message.to);
    }
    if (message.capacity !== 0) {
      writer.uint32(24).uint32(message.capacity);
    }
    if (message.policy !== 0) {
      writer.uint32(32).int32(message.policy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.capacity = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.policy = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeSpec {
    return {
      from: isSet(object.from) ? globalThis.String(object.from) : "",
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      capacity: isSet(object.capacity) ? globalThis.Number(object.capacity) : 0,
      policy: isSet(object.policy) ? edgePolicyFromJSON(object.policy) : 0,
    };
  },

  toJSON(message: EdgeSpec): unknown {
    const obj: any = {};
    if (message.from !== "") {
      obj.from = message.from;
    }
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.capacity !== 0) {
      obj.capacity = Math.round(message.capacity);
    }
    if (message.policy !== 0) {
      obj.policy = edgePolicyToJSON(message.policy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EdgeSpec>, I>>(base?: I): EdgeSpec {
    return EdgeSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EdgeSpec>, I>>(object: I): EdgeSpec {
    const message = createBaseEdgeSpec();
    message.from = object.from ?? "";
    message.to = object.to ?? "";
    message.capacity = object.capacity ?? 0;
    message.policy = object.policy ?? 0;
    return message;
  },
};

function createBasePipelineOptions(): PipelineOptions {
  return { latencyBudgetMs: 0, emitMetrics: false, strictValidation: false };
}

export const PipelineOptions: MessageFns<PipelineOptions> = {
  encode(message: PipelineOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.latencyBudgetMs !== 0) {
      writer.uint32(8).int32(message.latencyBudgetMs);
    }
    if (message.emitMetrics !== false) {
      writer.uint32(16).bool(message.emitMetrics);
    }
    if (message.strictValidation !== false) {
      writer.uint32(24).bool(message.strictValidation);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.latencyBudgetMs = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.emitMetrics = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.strictValidation = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineOptions {
    return {
      latencyBudgetMs: isSet(object.latencyBudgetMs)
        ? globalThis.Number(object.latencyBudgetMs)
        : isSet(object.latency_budget_ms)
        ? globalThis.Number(object.latency_budget_ms)
        : 0,
      emitMetrics: isSet(object.emitMetrics)
        ? globalThis.Boolean(object.emitMetrics)
        : isSet(object.emit_metrics)
        ? globalThis.Boolean(object.emit_metrics)
        : false,
      strictValidation: isSet(object.strictValidation)
        ? globalThis.Boolean(object.strictValidation)
        : isSet(object.strict_validation)
        ? globalThis.Boolean(object.strict_validation)
        : false,
    };
  },

  toJSON(message: PipelineOptions): unknown {
    const obj: any = {};
    if (message.latencyBudgetMs !== 0) {
      obj.latencyBudgetMs = Math.round(message.latencyBudgetMs);
    }
    if (message.emitMetrics !== false) {
      obj.emitMetrics = message.emitMetrics;
    }
    if (message.strictValidation !== false) {
      obj.strictValidation = message.strictValidation;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineOptions>, I>>(base?: I): PipelineOptions {
    return PipelineOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineOptions>, I>>(object: I): PipelineOptions {
    const message = createBasePipelineOptions();
    message.latencyBudgetMs = object.latencyBudgetMs ?? 0;
    message.emitMetrics = object.emitMetrics ?? false;
    message.strictValidation = object.strictValidation ?? false;
    return message;
  },
};

function createBasePipelineCompileResult(): PipelineCompileResult {
  return { handleId: "", status: 0, errorMessage: undefined, errorCode: 0 };
}

export const PipelineCompileResult: MessageFns<PipelineCompileResult> = {
  encode(message: PipelineCompileResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handleId !== "") {
      writer.uint32(10).string(message.handleId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(26).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(32).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineCompileResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineCompileResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineCompileResult {
    return {
      handleId: isSet(object.handleId)
        ? globalThis.String(object.handleId)
        : isSet(object.handle_id)
        ? globalThis.String(object.handle_id)
        : "",
      status: isSet(object.status) ? pipelineStatusFromJSON(object.status) : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
    };
  },

  toJSON(message: PipelineCompileResult): unknown {
    const obj: any = {};
    if (message.handleId !== "") {
      obj.handleId = message.handleId;
    }
    if (message.status !== 0) {
      obj.status = pipelineStatusToJSON(message.status);
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineCompileResult>, I>>(base?: I): PipelineCompileResult {
    return PipelineCompileResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineCompileResult>, I>>(object: I): PipelineCompileResult {
    const message = createBasePipelineCompileResult();
    message.handleId = object.handleId ?? "";
    message.status = object.status ?? 0;
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBasePipelineStartRequest(): PipelineStartRequest {
  return { handleId: "" };
}

export const PipelineStartRequest: MessageFns<PipelineStartRequest> = {
  encode(message: PipelineStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handleId !== "") {
      writer.uint32(10).string(message.handleId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handleId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineStartRequest {
    return {
      handleId: isSet(object.handleId)
        ? globalThis.String(object.handleId)
        : isSet(object.handle_id)
        ? globalThis.String(object.handle_id)
        : "",
    };
  },

  toJSON(message: PipelineStartRequest): unknown {
    const obj: any = {};
    if (message.handleId !== "") {
      obj.handleId = message.handleId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineStartRequest>, I>>(base?: I): PipelineStartRequest {
    return PipelineStartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineStartRequest>, I>>(object: I): PipelineStartRequest {
    const message = createBasePipelineStartRequest();
    message.handleId = object.handleId ?? "";
    return message;
  },
};

function createBasePipelineHandle(): PipelineHandle {
  return { handleId: "", status: 0, state: undefined };
}

export const PipelineHandle: MessageFns<PipelineHandle> = {
  encode(message: PipelineHandle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handleId !== "") {
      writer.uint32(10).string(message.handleId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.state !== undefined) {
      writer.uint32(26).string(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineHandle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineHandle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.state = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineHandle {
    return {
      handleId: isSet(object.handleId)
        ? globalThis.String(object.handleId)
        : isSet(object.handle_id)
        ? globalThis.String(object.handle_id)
        : "",
      status: isSet(object.status) ? pipelineStatusFromJSON(object.status) : 0,
      state: isSet(object.state) ? globalThis.String(object.state) : undefined,
    };
  },

  toJSON(message: PipelineHandle): unknown {
    const obj: any = {};
    if (message.handleId !== "") {
      obj.handleId = message.handleId;
    }
    if (message.status !== 0) {
      obj.status = pipelineStatusToJSON(message.status);
    }
    if (message.state !== undefined) {
      obj.state = message.state;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineHandle>, I>>(base?: I): PipelineHandle {
    return PipelineHandle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineHandle>, I>>(object: I): PipelineHandle {
    const message = createBasePipelineHandle();
    message.handleId = object.handleId ?? "";
    message.status = object.status ?? 0;
    message.state = object.state ?? undefined;
    return message;
  },
};

function createBasePipelineStopResult(): PipelineStopResult {
  return { handleId: "", status: 0, errorMessage: undefined, errorCode: 0 };
}

export const PipelineStopResult: MessageFns<PipelineStopResult> = {
  encode(message: PipelineStopResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handleId !== "") {
      writer.uint32(10).string(message.handleId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(26).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(32).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PipelineStopResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePipelineStopResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PipelineStopResult {
    return {
      handleId: isSet(object.handleId)
        ? globalThis.String(object.handleId)
        : isSet(object.handle_id)
        ? globalThis.String(object.handle_id)
        : "",
      status: isSet(object.status) ? pipelineStatusFromJSON(object.status) : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
    };
  },

  toJSON(message: PipelineStopResult): unknown {
    const obj: any = {};
    if (message.handleId !== "") {
      obj.handleId = message.handleId;
    }
    if (message.status !== 0) {
      obj.status = pipelineStatusToJSON(message.status);
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PipelineStopResult>, I>>(base?: I): PipelineStopResult {
    return PipelineStopResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PipelineStopResult>, I>>(object: I): PipelineStopResult {
    const message = createBasePipelineStopResult();
    message.handleId = object.handleId ?? "";
    message.status = object.status ?? 0;
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
