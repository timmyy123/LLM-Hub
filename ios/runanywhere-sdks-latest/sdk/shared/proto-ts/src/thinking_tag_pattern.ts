// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: thinking_tag_pattern.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

/**
 * ---------------------------------------------------------------------------
 * Pattern used to extract a model's "thinking" / reasoning block from its
 * raw output. Used by Qwen3 and LFM2 family models that emit
 * <think>...</think> wrappers. Shared by LLM generation options (per-call
 * override) and ModelInfo catalog metadata (default pattern for a model).
 * ---------------------------------------------------------------------------
 */
export interface ThinkingTagPattern {
  /** Opening tag string. Default if empty: "<think>". */
  openTag: string;
  /** Closing tag string. Default if empty: "</think>". */
  closeTag: string;
}

function createBaseThinkingTagPattern(): ThinkingTagPattern {
  return { openTag: "", closeTag: "" };
}

export const ThinkingTagPattern: MessageFns<ThinkingTagPattern> = {
  encode(message: ThinkingTagPattern, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.openTag !== "") {
      writer.uint32(10).string(message.openTag);
    }
    if (message.closeTag !== "") {
      writer.uint32(18).string(message.closeTag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThinkingTagPattern {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThinkingTagPattern();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.openTag = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.closeTag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThinkingTagPattern {
    return {
      openTag: isSet(object.openTag)
        ? globalThis.String(object.openTag)
        : isSet(object.open_tag)
        ? globalThis.String(object.open_tag)
        : "",
      closeTag: isSet(object.closeTag)
        ? globalThis.String(object.closeTag)
        : isSet(object.close_tag)
        ? globalThis.String(object.close_tag)
        : "",
    };
  },

  toJSON(message: ThinkingTagPattern): unknown {
    const obj: any = {};
    if (message.openTag !== "") {
      obj.openTag = message.openTag;
    }
    if (message.closeTag !== "") {
      obj.closeTag = message.closeTag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThinkingTagPattern>, I>>(base?: I): ThinkingTagPattern {
    return ThinkingTagPattern.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThinkingTagPattern>, I>>(object: I): ThinkingTagPattern {
    const message = createBaseThinkingTagPattern();
    message.openTag = object.openTag ?? "";
    message.closeTag = object.closeTag ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
