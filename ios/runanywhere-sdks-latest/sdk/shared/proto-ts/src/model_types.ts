// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: model_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  AccelerationPreference,
  accelerationPreferenceFromJSON,
  accelerationPreferenceToJSON,
  HardwareProfile,
} from "./hardware_profile";
import { ThinkingTagPattern } from "./thinking_tag_pattern";

export const protobufPackage = "runanywhere.v1";

/**
 * ---------------------------------------------------------------------------
 * Audio format — union of all cases currently defined across SDKs.
 * Sources pre-IDL:
 *   Kotlin  AudioTypes.kt:12          (pcm, wav, mp3, opus, aac, flac, ogg, pcm_16bit)
 *   Kotlin  ComponentTypes.kt:39      (pcm, wav, mp3, aac, ogg, opus, flac)  ← duplicate
 *   Swift   AudioTypes.swift:17       (pcm, wav, mp3, opus, aac, flac)
 *   Dart    audio_format.dart:3       (wav, mp3, m4a, flac, pcm, opus)
 *   RN      TTSTypes.ts:36            ('pcm' | 'wav' | 'mp3')
 * ---------------------------------------------------------------------------
 */
export enum AudioFormat {
  AUDIO_FORMAT_UNSPECIFIED = 0,
  AUDIO_FORMAT_PCM = 1,
  AUDIO_FORMAT_WAV = 2,
  AUDIO_FORMAT_MP3 = 3,
  AUDIO_FORMAT_OPUS = 4,
  AUDIO_FORMAT_AAC = 5,
  AUDIO_FORMAT_FLAC = 6,
  AUDIO_FORMAT_OGG = 7,
  /** AUDIO_FORMAT_M4A - iOS / Dart, container of AAC */
  AUDIO_FORMAT_M4A = 8,
  /** AUDIO_FORMAT_PCM_S16LE - Android "pcm_16bit" — signed 16-bit LE PCM */
  AUDIO_FORMAT_PCM_S16LE = 9,
  UNRECOGNIZED = -1,
}

export function audioFormatFromJSON(object: any): AudioFormat {
  switch (object) {
    case 0:
    case "AUDIO_FORMAT_UNSPECIFIED":
      return AudioFormat.AUDIO_FORMAT_UNSPECIFIED;
    case 1:
    case "AUDIO_FORMAT_PCM":
      return AudioFormat.AUDIO_FORMAT_PCM;
    case 2:
    case "AUDIO_FORMAT_WAV":
      return AudioFormat.AUDIO_FORMAT_WAV;
    case 3:
    case "AUDIO_FORMAT_MP3":
      return AudioFormat.AUDIO_FORMAT_MP3;
    case 4:
    case "AUDIO_FORMAT_OPUS":
      return AudioFormat.AUDIO_FORMAT_OPUS;
    case 5:
    case "AUDIO_FORMAT_AAC":
      return AudioFormat.AUDIO_FORMAT_AAC;
    case 6:
    case "AUDIO_FORMAT_FLAC":
      return AudioFormat.AUDIO_FORMAT_FLAC;
    case 7:
    case "AUDIO_FORMAT_OGG":
      return AudioFormat.AUDIO_FORMAT_OGG;
    case 8:
    case "AUDIO_FORMAT_M4A":
      return AudioFormat.AUDIO_FORMAT_M4A;
    case 9:
    case "AUDIO_FORMAT_PCM_S16LE":
      return AudioFormat.AUDIO_FORMAT_PCM_S16LE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AudioFormat.UNRECOGNIZED;
  }
}

export function audioFormatToJSON(object: AudioFormat): string {
  switch (object) {
    case AudioFormat.AUDIO_FORMAT_UNSPECIFIED:
      return "AUDIO_FORMAT_UNSPECIFIED";
    case AudioFormat.AUDIO_FORMAT_PCM:
      return "AUDIO_FORMAT_PCM";
    case AudioFormat.AUDIO_FORMAT_WAV:
      return "AUDIO_FORMAT_WAV";
    case AudioFormat.AUDIO_FORMAT_MP3:
      return "AUDIO_FORMAT_MP3";
    case AudioFormat.AUDIO_FORMAT_OPUS:
      return "AUDIO_FORMAT_OPUS";
    case AudioFormat.AUDIO_FORMAT_AAC:
      return "AUDIO_FORMAT_AAC";
    case AudioFormat.AUDIO_FORMAT_FLAC:
      return "AUDIO_FORMAT_FLAC";
    case AudioFormat.AUDIO_FORMAT_OGG:
      return "AUDIO_FORMAT_OGG";
    case AudioFormat.AUDIO_FORMAT_M4A:
      return "AUDIO_FORMAT_M4A";
    case AudioFormat.AUDIO_FORMAT_PCM_S16LE:
      return "AUDIO_FORMAT_PCM_S16LE";
    case AudioFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Model file format — union across all SDKs.
 * Sources pre-IDL:
 *   Swift  ModelTypes.swift:27        (onnx, ort, gguf, bin, coreml, unknown)
 *   Kotlin ModelTypes.kt:41           (ONNX, ORT, GGUF, BIN, QNN_CONTEXT, UNKNOWN)
 *   Dart   model_types.dart:34        (onnx, ort, gguf, bin, unknown)
 *   RN     enums.ts:115               (12-case superset incl. MLModel, MLPackage, TFLite,
 *                                       SafeTensors, Zip, Folder, Proprietary)
 *   Web    enums.ts:56                (copy of RN)
 * ---------------------------------------------------------------------------
 */
export enum ModelFormat {
  MODEL_FORMAT_UNSPECIFIED = 0,
  MODEL_FORMAT_GGUF = 1,
  MODEL_FORMAT_GGML = 2,
  MODEL_FORMAT_ONNX = 3,
  MODEL_FORMAT_ORT = 4,
  MODEL_FORMAT_BIN = 5,
  /** MODEL_FORMAT_COREML - Apple platforms only */
  MODEL_FORMAT_COREML = 6,
  /** MODEL_FORMAT_MLMODEL - Apple platforms only */
  MODEL_FORMAT_MLMODEL = 7,
  /** MODEL_FORMAT_MLPACKAGE - Apple platforms only */
  MODEL_FORMAT_MLPACKAGE = 8,
  MODEL_FORMAT_TFLITE = 9,
  MODEL_FORMAT_SAFETENSORS = 10,
  /** MODEL_FORMAT_QNN_CONTEXT - Qualcomm Hexagon NPU context */
  MODEL_FORMAT_QNN_CONTEXT = 11,
  /** MODEL_FORMAT_ZIP - Archive wrapping one of the above */
  MODEL_FORMAT_ZIP = 12,
  MODEL_FORMAT_FOLDER = 13,
  /** MODEL_FORMAT_PROPRIETARY - Built-in system models */
  MODEL_FORMAT_PROPRIETARY = 14,
  MODEL_FORMAT_UNKNOWN = 15,
  UNRECOGNIZED = -1,
}

export function modelFormatFromJSON(object: any): ModelFormat {
  switch (object) {
    case 0:
    case "MODEL_FORMAT_UNSPECIFIED":
      return ModelFormat.MODEL_FORMAT_UNSPECIFIED;
    case 1:
    case "MODEL_FORMAT_GGUF":
      return ModelFormat.MODEL_FORMAT_GGUF;
    case 2:
    case "MODEL_FORMAT_GGML":
      return ModelFormat.MODEL_FORMAT_GGML;
    case 3:
    case "MODEL_FORMAT_ONNX":
      return ModelFormat.MODEL_FORMAT_ONNX;
    case 4:
    case "MODEL_FORMAT_ORT":
      return ModelFormat.MODEL_FORMAT_ORT;
    case 5:
    case "MODEL_FORMAT_BIN":
      return ModelFormat.MODEL_FORMAT_BIN;
    case 6:
    case "MODEL_FORMAT_COREML":
      return ModelFormat.MODEL_FORMAT_COREML;
    case 7:
    case "MODEL_FORMAT_MLMODEL":
      return ModelFormat.MODEL_FORMAT_MLMODEL;
    case 8:
    case "MODEL_FORMAT_MLPACKAGE":
      return ModelFormat.MODEL_FORMAT_MLPACKAGE;
    case 9:
    case "MODEL_FORMAT_TFLITE":
      return ModelFormat.MODEL_FORMAT_TFLITE;
    case 10:
    case "MODEL_FORMAT_SAFETENSORS":
      return ModelFormat.MODEL_FORMAT_SAFETENSORS;
    case 11:
    case "MODEL_FORMAT_QNN_CONTEXT":
      return ModelFormat.MODEL_FORMAT_QNN_CONTEXT;
    case 12:
    case "MODEL_FORMAT_ZIP":
      return ModelFormat.MODEL_FORMAT_ZIP;
    case 13:
    case "MODEL_FORMAT_FOLDER":
      return ModelFormat.MODEL_FORMAT_FOLDER;
    case 14:
    case "MODEL_FORMAT_PROPRIETARY":
      return ModelFormat.MODEL_FORMAT_PROPRIETARY;
    case 15:
    case "MODEL_FORMAT_UNKNOWN":
      return ModelFormat.MODEL_FORMAT_UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelFormat.UNRECOGNIZED;
  }
}

export function modelFormatToJSON(object: ModelFormat): string {
  switch (object) {
    case ModelFormat.MODEL_FORMAT_UNSPECIFIED:
      return "MODEL_FORMAT_UNSPECIFIED";
    case ModelFormat.MODEL_FORMAT_GGUF:
      return "MODEL_FORMAT_GGUF";
    case ModelFormat.MODEL_FORMAT_GGML:
      return "MODEL_FORMAT_GGML";
    case ModelFormat.MODEL_FORMAT_ONNX:
      return "MODEL_FORMAT_ONNX";
    case ModelFormat.MODEL_FORMAT_ORT:
      return "MODEL_FORMAT_ORT";
    case ModelFormat.MODEL_FORMAT_BIN:
      return "MODEL_FORMAT_BIN";
    case ModelFormat.MODEL_FORMAT_COREML:
      return "MODEL_FORMAT_COREML";
    case ModelFormat.MODEL_FORMAT_MLMODEL:
      return "MODEL_FORMAT_MLMODEL";
    case ModelFormat.MODEL_FORMAT_MLPACKAGE:
      return "MODEL_FORMAT_MLPACKAGE";
    case ModelFormat.MODEL_FORMAT_TFLITE:
      return "MODEL_FORMAT_TFLITE";
    case ModelFormat.MODEL_FORMAT_SAFETENSORS:
      return "MODEL_FORMAT_SAFETENSORS";
    case ModelFormat.MODEL_FORMAT_QNN_CONTEXT:
      return "MODEL_FORMAT_QNN_CONTEXT";
    case ModelFormat.MODEL_FORMAT_ZIP:
      return "MODEL_FORMAT_ZIP";
    case ModelFormat.MODEL_FORMAT_FOLDER:
      return "MODEL_FORMAT_FOLDER";
    case ModelFormat.MODEL_FORMAT_PROPRIETARY:
      return "MODEL_FORMAT_PROPRIETARY";
    case ModelFormat.MODEL_FORMAT_UNKNOWN:
      return "MODEL_FORMAT_UNKNOWN";
    case ModelFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Inference framework / runtime. Same name used across all SDKs (RN names it
 * LLMFramework; we canonicalize on InferenceFramework).
 * Sources pre-IDL:
 *   Swift  ModelTypes.swift:76        (12 cases incl. coreml, mlx, whisperKitCoreML)
 *   Kotlin ComponentTypes.kt:122      (9 cases; no coreml / mlx / whisperKit)
 *   Dart   model_types.dart:106       (9 cases, matches Kotlin)
 *   RN     enums.ts:30 (LLMFramework) (16 cases)
 *   Web    enums.ts:21 (LLMFramework) (16 cases, copy of RN)
 * ---------------------------------------------------------------------------
 */
export enum InferenceFramework {
  INFERENCE_FRAMEWORK_UNSPECIFIED = 0,
  INFERENCE_FRAMEWORK_ONNX = 1,
  INFERENCE_FRAMEWORK_LLAMA_CPP = 2,
  /** INFERENCE_FRAMEWORK_FOUNDATION_MODELS - Apple on-device LLM */
  INFERENCE_FRAMEWORK_FOUNDATION_MODELS = 3,
  INFERENCE_FRAMEWORK_SYSTEM_TTS = 4,
  INFERENCE_FRAMEWORK_FLUID_AUDIO = 5,
  /** INFERENCE_FRAMEWORK_COREML - Apple */
  INFERENCE_FRAMEWORK_COREML = 6,
  /** INFERENCE_FRAMEWORK_MLX - Apple Silicon */
  INFERENCE_FRAMEWORK_MLX = 7,
  INFERENCE_FRAMEWORK_TFLITE = 11,
  INFERENCE_FRAMEWORK_EXECUTORCH = 12,
  INFERENCE_FRAMEWORK_MEDIAPIPE = 13,
  INFERENCE_FRAMEWORK_MLC = 14,
  INFERENCE_FRAMEWORK_PICO_LLM = 15,
  INFERENCE_FRAMEWORK_PIPER_TTS = 16,
  INFERENCE_FRAMEWORK_SWIFT_TRANSFORMERS = 19,
  /** INFERENCE_FRAMEWORK_BUILT_IN - rule-based, no model */
  INFERENCE_FRAMEWORK_BUILT_IN = 20,
  INFERENCE_FRAMEWORK_NONE = 21,
  INFERENCE_FRAMEWORK_UNKNOWN = 22,
  /** INFERENCE_FRAMEWORK_SHERPA - Sherpa-ONNX speech engine (STT/TTS/VAD) */
  INFERENCE_FRAMEWORK_SHERPA = 23,
  /** INFERENCE_FRAMEWORK_QHEXRT - Qualcomm Hexagon NPU (QHexRT runtime) */
  INFERENCE_FRAMEWORK_QHEXRT = 24,
  UNRECOGNIZED = -1,
}

export function inferenceFrameworkFromJSON(object: any): InferenceFramework {
  switch (object) {
    case 0:
    case "INFERENCE_FRAMEWORK_UNSPECIFIED":
      return InferenceFramework.INFERENCE_FRAMEWORK_UNSPECIFIED;
    case 1:
    case "INFERENCE_FRAMEWORK_ONNX":
      return InferenceFramework.INFERENCE_FRAMEWORK_ONNX;
    case 2:
    case "INFERENCE_FRAMEWORK_LLAMA_CPP":
      return InferenceFramework.INFERENCE_FRAMEWORK_LLAMA_CPP;
    case 3:
    case "INFERENCE_FRAMEWORK_FOUNDATION_MODELS":
      return InferenceFramework.INFERENCE_FRAMEWORK_FOUNDATION_MODELS;
    case 4:
    case "INFERENCE_FRAMEWORK_SYSTEM_TTS":
      return InferenceFramework.INFERENCE_FRAMEWORK_SYSTEM_TTS;
    case 5:
    case "INFERENCE_FRAMEWORK_FLUID_AUDIO":
      return InferenceFramework.INFERENCE_FRAMEWORK_FLUID_AUDIO;
    case 6:
    case "INFERENCE_FRAMEWORK_COREML":
      return InferenceFramework.INFERENCE_FRAMEWORK_COREML;
    case 7:
    case "INFERENCE_FRAMEWORK_MLX":
      return InferenceFramework.INFERENCE_FRAMEWORK_MLX;
    case 11:
    case "INFERENCE_FRAMEWORK_TFLITE":
      return InferenceFramework.INFERENCE_FRAMEWORK_TFLITE;
    case 12:
    case "INFERENCE_FRAMEWORK_EXECUTORCH":
      return InferenceFramework.INFERENCE_FRAMEWORK_EXECUTORCH;
    case 13:
    case "INFERENCE_FRAMEWORK_MEDIAPIPE":
      return InferenceFramework.INFERENCE_FRAMEWORK_MEDIAPIPE;
    case 14:
    case "INFERENCE_FRAMEWORK_MLC":
      return InferenceFramework.INFERENCE_FRAMEWORK_MLC;
    case 15:
    case "INFERENCE_FRAMEWORK_PICO_LLM":
      return InferenceFramework.INFERENCE_FRAMEWORK_PICO_LLM;
    case 16:
    case "INFERENCE_FRAMEWORK_PIPER_TTS":
      return InferenceFramework.INFERENCE_FRAMEWORK_PIPER_TTS;
    case 19:
    case "INFERENCE_FRAMEWORK_SWIFT_TRANSFORMERS":
      return InferenceFramework.INFERENCE_FRAMEWORK_SWIFT_TRANSFORMERS;
    case 20:
    case "INFERENCE_FRAMEWORK_BUILT_IN":
      return InferenceFramework.INFERENCE_FRAMEWORK_BUILT_IN;
    case 21:
    case "INFERENCE_FRAMEWORK_NONE":
      return InferenceFramework.INFERENCE_FRAMEWORK_NONE;
    case 22:
    case "INFERENCE_FRAMEWORK_UNKNOWN":
      return InferenceFramework.INFERENCE_FRAMEWORK_UNKNOWN;
    case 23:
    case "INFERENCE_FRAMEWORK_SHERPA":
      return InferenceFramework.INFERENCE_FRAMEWORK_SHERPA;
    case 24:
    case "INFERENCE_FRAMEWORK_QHEXRT":
      return InferenceFramework.INFERENCE_FRAMEWORK_QHEXRT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InferenceFramework.UNRECOGNIZED;
  }
}

export function inferenceFrameworkToJSON(object: InferenceFramework): string {
  switch (object) {
    case InferenceFramework.INFERENCE_FRAMEWORK_UNSPECIFIED:
      return "INFERENCE_FRAMEWORK_UNSPECIFIED";
    case InferenceFramework.INFERENCE_FRAMEWORK_ONNX:
      return "INFERENCE_FRAMEWORK_ONNX";
    case InferenceFramework.INFERENCE_FRAMEWORK_LLAMA_CPP:
      return "INFERENCE_FRAMEWORK_LLAMA_CPP";
    case InferenceFramework.INFERENCE_FRAMEWORK_FOUNDATION_MODELS:
      return "INFERENCE_FRAMEWORK_FOUNDATION_MODELS";
    case InferenceFramework.INFERENCE_FRAMEWORK_SYSTEM_TTS:
      return "INFERENCE_FRAMEWORK_SYSTEM_TTS";
    case InferenceFramework.INFERENCE_FRAMEWORK_FLUID_AUDIO:
      return "INFERENCE_FRAMEWORK_FLUID_AUDIO";
    case InferenceFramework.INFERENCE_FRAMEWORK_COREML:
      return "INFERENCE_FRAMEWORK_COREML";
    case InferenceFramework.INFERENCE_FRAMEWORK_MLX:
      return "INFERENCE_FRAMEWORK_MLX";
    case InferenceFramework.INFERENCE_FRAMEWORK_TFLITE:
      return "INFERENCE_FRAMEWORK_TFLITE";
    case InferenceFramework.INFERENCE_FRAMEWORK_EXECUTORCH:
      return "INFERENCE_FRAMEWORK_EXECUTORCH";
    case InferenceFramework.INFERENCE_FRAMEWORK_MEDIAPIPE:
      return "INFERENCE_FRAMEWORK_MEDIAPIPE";
    case InferenceFramework.INFERENCE_FRAMEWORK_MLC:
      return "INFERENCE_FRAMEWORK_MLC";
    case InferenceFramework.INFERENCE_FRAMEWORK_PICO_LLM:
      return "INFERENCE_FRAMEWORK_PICO_LLM";
    case InferenceFramework.INFERENCE_FRAMEWORK_PIPER_TTS:
      return "INFERENCE_FRAMEWORK_PIPER_TTS";
    case InferenceFramework.INFERENCE_FRAMEWORK_SWIFT_TRANSFORMERS:
      return "INFERENCE_FRAMEWORK_SWIFT_TRANSFORMERS";
    case InferenceFramework.INFERENCE_FRAMEWORK_BUILT_IN:
      return "INFERENCE_FRAMEWORK_BUILT_IN";
    case InferenceFramework.INFERENCE_FRAMEWORK_NONE:
      return "INFERENCE_FRAMEWORK_NONE";
    case InferenceFramework.INFERENCE_FRAMEWORK_UNKNOWN:
      return "INFERENCE_FRAMEWORK_UNKNOWN";
    case InferenceFramework.INFERENCE_FRAMEWORK_SHERPA:
      return "INFERENCE_FRAMEWORK_SHERPA";
    case InferenceFramework.INFERENCE_FRAMEWORK_QHEXRT:
      return "INFERENCE_FRAMEWORK_QHEXRT";
    case InferenceFramework.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Model category / modality class. Sources pre-IDL:
 *   Swift ModelTypes.swift:39         (9 cases incl. voiceActivityDetection + audio)
 *   Kotlin ModelTypes.kt:147          (8 cases, no VAD)
 *   Dart  model_types.dart:55         (8 cases, no VAD)
 *   RN    enums.ts:75                 (8 cases, no VAD, Audio labeled as VAD)
 *   Web   enums.ts:39                 (7 cases, Audio labeled as VAD)
 * ---------------------------------------------------------------------------
 */
export enum ModelCategory {
  MODEL_CATEGORY_UNSPECIFIED = 0,
  MODEL_CATEGORY_LANGUAGE = 1,
  MODEL_CATEGORY_SPEECH_RECOGNITION = 2,
  MODEL_CATEGORY_SPEECH_SYNTHESIS = 3,
  MODEL_CATEGORY_VISION = 4,
  MODEL_CATEGORY_IMAGE_GENERATION = 5,
  MODEL_CATEGORY_MULTIMODAL = 6,
  MODEL_CATEGORY_AUDIO = 7,
  MODEL_CATEGORY_EMBEDDING = 8,
  /** MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION - present in Swift only pre-IDL */
  MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION = 9,
  UNRECOGNIZED = -1,
}

export function modelCategoryFromJSON(object: any): ModelCategory {
  switch (object) {
    case 0:
    case "MODEL_CATEGORY_UNSPECIFIED":
      return ModelCategory.MODEL_CATEGORY_UNSPECIFIED;
    case 1:
    case "MODEL_CATEGORY_LANGUAGE":
      return ModelCategory.MODEL_CATEGORY_LANGUAGE;
    case 2:
    case "MODEL_CATEGORY_SPEECH_RECOGNITION":
      return ModelCategory.MODEL_CATEGORY_SPEECH_RECOGNITION;
    case 3:
    case "MODEL_CATEGORY_SPEECH_SYNTHESIS":
      return ModelCategory.MODEL_CATEGORY_SPEECH_SYNTHESIS;
    case 4:
    case "MODEL_CATEGORY_VISION":
      return ModelCategory.MODEL_CATEGORY_VISION;
    case 5:
    case "MODEL_CATEGORY_IMAGE_GENERATION":
      return ModelCategory.MODEL_CATEGORY_IMAGE_GENERATION;
    case 6:
    case "MODEL_CATEGORY_MULTIMODAL":
      return ModelCategory.MODEL_CATEGORY_MULTIMODAL;
    case 7:
    case "MODEL_CATEGORY_AUDIO":
      return ModelCategory.MODEL_CATEGORY_AUDIO;
    case 8:
    case "MODEL_CATEGORY_EMBEDDING":
      return ModelCategory.MODEL_CATEGORY_EMBEDDING;
    case 9:
    case "MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION":
      return ModelCategory.MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelCategory.UNRECOGNIZED;
  }
}

export function modelCategoryToJSON(object: ModelCategory): string {
  switch (object) {
    case ModelCategory.MODEL_CATEGORY_UNSPECIFIED:
      return "MODEL_CATEGORY_UNSPECIFIED";
    case ModelCategory.MODEL_CATEGORY_LANGUAGE:
      return "MODEL_CATEGORY_LANGUAGE";
    case ModelCategory.MODEL_CATEGORY_SPEECH_RECOGNITION:
      return "MODEL_CATEGORY_SPEECH_RECOGNITION";
    case ModelCategory.MODEL_CATEGORY_SPEECH_SYNTHESIS:
      return "MODEL_CATEGORY_SPEECH_SYNTHESIS";
    case ModelCategory.MODEL_CATEGORY_VISION:
      return "MODEL_CATEGORY_VISION";
    case ModelCategory.MODEL_CATEGORY_IMAGE_GENERATION:
      return "MODEL_CATEGORY_IMAGE_GENERATION";
    case ModelCategory.MODEL_CATEGORY_MULTIMODAL:
      return "MODEL_CATEGORY_MULTIMODAL";
    case ModelCategory.MODEL_CATEGORY_AUDIO:
      return "MODEL_CATEGORY_AUDIO";
    case ModelCategory.MODEL_CATEGORY_EMBEDDING:
      return "MODEL_CATEGORY_EMBEDDING";
    case ModelCategory.MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION:
      return "MODEL_CATEGORY_VOICE_ACTIVITY_DETECTION";
    case ModelCategory.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * SDK environment. Sources pre-IDL:
 *   Swift  SDKEnvironment.swift:5     (development, staging, production)
 *   Kotlin RunAnywhere.kt:47          (DEVELOPMENT, STAGING, PRODUCTION, cEnvironment)
 *   Kotlin SDKLogger.kt:159           (DEVELOPMENT, STAGING, PRODUCTION) ← duplicate
 *   Dart   sdk_environment.dart:5     (development, staging, production)
 *   RN     enums.ts:11                (Development, Staging, Production)
 *   Web    enums.ts:9                 (Development, Staging, Production)
 * ---------------------------------------------------------------------------
 */
export enum SDKEnvironment {
  SDK_ENVIRONMENT_UNSPECIFIED = 0,
  SDK_ENVIRONMENT_DEVELOPMENT = 1,
  SDK_ENVIRONMENT_STAGING = 2,
  SDK_ENVIRONMENT_PRODUCTION = 3,
  UNRECOGNIZED = -1,
}

export function sDKEnvironmentFromJSON(object: any): SDKEnvironment {
  switch (object) {
    case 0:
    case "SDK_ENVIRONMENT_UNSPECIFIED":
      return SDKEnvironment.SDK_ENVIRONMENT_UNSPECIFIED;
    case 1:
    case "SDK_ENVIRONMENT_DEVELOPMENT":
      return SDKEnvironment.SDK_ENVIRONMENT_DEVELOPMENT;
    case 2:
    case "SDK_ENVIRONMENT_STAGING":
      return SDKEnvironment.SDK_ENVIRONMENT_STAGING;
    case 3:
    case "SDK_ENVIRONMENT_PRODUCTION":
      return SDKEnvironment.SDK_ENVIRONMENT_PRODUCTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SDKEnvironment.UNRECOGNIZED;
  }
}

export function sDKEnvironmentToJSON(object: SDKEnvironment): string {
  switch (object) {
    case SDKEnvironment.SDK_ENVIRONMENT_UNSPECIFIED:
      return "SDK_ENVIRONMENT_UNSPECIFIED";
    case SDKEnvironment.SDK_ENVIRONMENT_DEVELOPMENT:
      return "SDK_ENVIRONMENT_DEVELOPMENT";
    case SDKEnvironment.SDK_ENVIRONMENT_STAGING:
      return "SDK_ENVIRONMENT_STAGING";
    case SDKEnvironment.SDK_ENVIRONMENT_PRODUCTION:
      return "SDK_ENVIRONMENT_PRODUCTION";
    case SDKEnvironment.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Model source — where the catalog entry came from.
 * ---------------------------------------------------------------------------
 */
export enum ModelSource {
  MODEL_SOURCE_UNSPECIFIED = 0,
  /** MODEL_SOURCE_REMOTE - Downloaded from a URL */
  MODEL_SOURCE_REMOTE = 1,
  /** MODEL_SOURCE_LOCAL - Bundled or user-imported */
  MODEL_SOURCE_LOCAL = 2,
  /** MODEL_SOURCE_BUILT_IN - Platform/system model with no portable download artifact */
  MODEL_SOURCE_BUILT_IN = 3,
  UNRECOGNIZED = -1,
}

export function modelSourceFromJSON(object: any): ModelSource {
  switch (object) {
    case 0:
    case "MODEL_SOURCE_UNSPECIFIED":
      return ModelSource.MODEL_SOURCE_UNSPECIFIED;
    case 1:
    case "MODEL_SOURCE_REMOTE":
      return ModelSource.MODEL_SOURCE_REMOTE;
    case 2:
    case "MODEL_SOURCE_LOCAL":
      return ModelSource.MODEL_SOURCE_LOCAL;
    case 3:
    case "MODEL_SOURCE_BUILT_IN":
      return ModelSource.MODEL_SOURCE_BUILT_IN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelSource.UNRECOGNIZED;
  }
}

export function modelSourceToJSON(object: ModelSource): string {
  switch (object) {
    case ModelSource.MODEL_SOURCE_UNSPECIFIED:
      return "MODEL_SOURCE_UNSPECIFIED";
    case ModelSource.MODEL_SOURCE_REMOTE:
      return "MODEL_SOURCE_REMOTE";
    case ModelSource.MODEL_SOURCE_LOCAL:
      return "MODEL_SOURCE_LOCAL";
    case ModelSource.MODEL_SOURCE_BUILT_IN:
      return "MODEL_SOURCE_BUILT_IN";
    case ModelSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Archive types for multi-file model packages. Sources pre-IDL:
 *   Swift  ModelTypes.swift:195       (zip, tarBz2, tarGz, tarXz)
 *   Kotlin ModelTypes.kt:176          (ZIP, TAR_BZ2, TAR_GZ, TAR_XZ)
 *   Dart   model_types.dart:141       (zip, tarBz2, tarGz, tarXz)
 * ---------------------------------------------------------------------------
 */
export enum ArchiveType {
  ARCHIVE_TYPE_UNSPECIFIED = 0,
  ARCHIVE_TYPE_ZIP = 1,
  ARCHIVE_TYPE_TAR_BZ2 = 2,
  ARCHIVE_TYPE_TAR_GZ = 3,
  ARCHIVE_TYPE_TAR_XZ = 4,
  UNRECOGNIZED = -1,
}

export function archiveTypeFromJSON(object: any): ArchiveType {
  switch (object) {
    case 0:
    case "ARCHIVE_TYPE_UNSPECIFIED":
      return ArchiveType.ARCHIVE_TYPE_UNSPECIFIED;
    case 1:
    case "ARCHIVE_TYPE_ZIP":
      return ArchiveType.ARCHIVE_TYPE_ZIP;
    case 2:
    case "ARCHIVE_TYPE_TAR_BZ2":
      return ArchiveType.ARCHIVE_TYPE_TAR_BZ2;
    case 3:
    case "ARCHIVE_TYPE_TAR_GZ":
      return ArchiveType.ARCHIVE_TYPE_TAR_GZ;
    case 4:
    case "ARCHIVE_TYPE_TAR_XZ":
      return ArchiveType.ARCHIVE_TYPE_TAR_XZ;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ArchiveType.UNRECOGNIZED;
  }
}

export function archiveTypeToJSON(object: ArchiveType): string {
  switch (object) {
    case ArchiveType.ARCHIVE_TYPE_UNSPECIFIED:
      return "ARCHIVE_TYPE_UNSPECIFIED";
    case ArchiveType.ARCHIVE_TYPE_ZIP:
      return "ARCHIVE_TYPE_ZIP";
    case ArchiveType.ARCHIVE_TYPE_TAR_BZ2:
      return "ARCHIVE_TYPE_TAR_BZ2";
    case ArchiveType.ARCHIVE_TYPE_TAR_GZ:
      return "ARCHIVE_TYPE_TAR_GZ";
    case ArchiveType.ARCHIVE_TYPE_TAR_XZ:
      return "ARCHIVE_TYPE_TAR_XZ";
    case ArchiveType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ArchiveStructure {
  ARCHIVE_STRUCTURE_UNSPECIFIED = 0,
  ARCHIVE_STRUCTURE_SINGLE_FILE_NESTED = 1,
  ARCHIVE_STRUCTURE_DIRECTORY_BASED = 2,
  ARCHIVE_STRUCTURE_NESTED_DIRECTORY = 3,
  ARCHIVE_STRUCTURE_UNKNOWN = 4,
  UNRECOGNIZED = -1,
}

export function archiveStructureFromJSON(object: any): ArchiveStructure {
  switch (object) {
    case 0:
    case "ARCHIVE_STRUCTURE_UNSPECIFIED":
      return ArchiveStructure.ARCHIVE_STRUCTURE_UNSPECIFIED;
    case 1:
    case "ARCHIVE_STRUCTURE_SINGLE_FILE_NESTED":
      return ArchiveStructure.ARCHIVE_STRUCTURE_SINGLE_FILE_NESTED;
    case 2:
    case "ARCHIVE_STRUCTURE_DIRECTORY_BASED":
      return ArchiveStructure.ARCHIVE_STRUCTURE_DIRECTORY_BASED;
    case 3:
    case "ARCHIVE_STRUCTURE_NESTED_DIRECTORY":
      return ArchiveStructure.ARCHIVE_STRUCTURE_NESTED_DIRECTORY;
    case 4:
    case "ARCHIVE_STRUCTURE_UNKNOWN":
      return ArchiveStructure.ARCHIVE_STRUCTURE_UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ArchiveStructure.UNRECOGNIZED;
  }
}

export function archiveStructureToJSON(object: ArchiveStructure): string {
  switch (object) {
    case ArchiveStructure.ARCHIVE_STRUCTURE_UNSPECIFIED:
      return "ARCHIVE_STRUCTURE_UNSPECIFIED";
    case ArchiveStructure.ARCHIVE_STRUCTURE_SINGLE_FILE_NESTED:
      return "ARCHIVE_STRUCTURE_SINGLE_FILE_NESTED";
    case ArchiveStructure.ARCHIVE_STRUCTURE_DIRECTORY_BASED:
      return "ARCHIVE_STRUCTURE_DIRECTORY_BASED";
    case ArchiveStructure.ARCHIVE_STRUCTURE_NESTED_DIRECTORY:
      return "ARCHIVE_STRUCTURE_NESTED_DIRECTORY";
    case ArchiveStructure.ARCHIVE_STRUCTURE_UNKNOWN:
      return "ARCHIVE_STRUCTURE_UNKNOWN";
    case ArchiveStructure.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * High-level artifact classification — what KIND of bundle a model ships as.
 * Distinct from ModelFormat (the on-disk file format) and ArchiveType (the
 * compression flavor). Sources pre-IDL:
 *   Swift  ModelTypes.swift:~200            (singleFile, archive, multiFile, custom)
 *   Web    types.ts:149                     (SingleFile / Archive / MultiFile / Custom)
 *   Kotlin sealed class ModelArtifactType   (SingleFile / Archive / MultiFile / Custom)
 * ---------------------------------------------------------------------------
 */
export enum ModelArtifactType {
  MODEL_ARTIFACT_TYPE_UNSPECIFIED = 0,
  MODEL_ARTIFACT_TYPE_SINGLE_FILE = 1,
  MODEL_ARTIFACT_TYPE_TAR_GZ_ARCHIVE = 2,
  MODEL_ARTIFACT_TYPE_DIRECTORY = 3,
  MODEL_ARTIFACT_TYPE_ZIP_ARCHIVE = 4,
  MODEL_ARTIFACT_TYPE_CUSTOM = 5,
  MODEL_ARTIFACT_TYPE_ARCHIVE = 6,
  MODEL_ARTIFACT_TYPE_MULTI_FILE = 7,
  MODEL_ARTIFACT_TYPE_BUILT_IN = 8,
  MODEL_ARTIFACT_TYPE_TAR_BZ2_ARCHIVE = 9,
  MODEL_ARTIFACT_TYPE_TAR_XZ_ARCHIVE = 10,
  UNRECOGNIZED = -1,
}

export function modelArtifactTypeFromJSON(object: any): ModelArtifactType {
  switch (object) {
    case 0:
    case "MODEL_ARTIFACT_TYPE_UNSPECIFIED":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_UNSPECIFIED;
    case 1:
    case "MODEL_ARTIFACT_TYPE_SINGLE_FILE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_SINGLE_FILE;
    case 2:
    case "MODEL_ARTIFACT_TYPE_TAR_GZ_ARCHIVE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_GZ_ARCHIVE;
    case 3:
    case "MODEL_ARTIFACT_TYPE_DIRECTORY":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_DIRECTORY;
    case 4:
    case "MODEL_ARTIFACT_TYPE_ZIP_ARCHIVE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_ZIP_ARCHIVE;
    case 5:
    case "MODEL_ARTIFACT_TYPE_CUSTOM":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_CUSTOM;
    case 6:
    case "MODEL_ARTIFACT_TYPE_ARCHIVE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_ARCHIVE;
    case 7:
    case "MODEL_ARTIFACT_TYPE_MULTI_FILE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_MULTI_FILE;
    case 8:
    case "MODEL_ARTIFACT_TYPE_BUILT_IN":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_BUILT_IN;
    case 9:
    case "MODEL_ARTIFACT_TYPE_TAR_BZ2_ARCHIVE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_BZ2_ARCHIVE;
    case 10:
    case "MODEL_ARTIFACT_TYPE_TAR_XZ_ARCHIVE":
      return ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_XZ_ARCHIVE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelArtifactType.UNRECOGNIZED;
  }
}

export function modelArtifactTypeToJSON(object: ModelArtifactType): string {
  switch (object) {
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_UNSPECIFIED:
      return "MODEL_ARTIFACT_TYPE_UNSPECIFIED";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_SINGLE_FILE:
      return "MODEL_ARTIFACT_TYPE_SINGLE_FILE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_GZ_ARCHIVE:
      return "MODEL_ARTIFACT_TYPE_TAR_GZ_ARCHIVE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_DIRECTORY:
      return "MODEL_ARTIFACT_TYPE_DIRECTORY";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_ZIP_ARCHIVE:
      return "MODEL_ARTIFACT_TYPE_ZIP_ARCHIVE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_CUSTOM:
      return "MODEL_ARTIFACT_TYPE_CUSTOM";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_ARCHIVE:
      return "MODEL_ARTIFACT_TYPE_ARCHIVE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_MULTI_FILE:
      return "MODEL_ARTIFACT_TYPE_MULTI_FILE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_BUILT_IN:
      return "MODEL_ARTIFACT_TYPE_BUILT_IN";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_BZ2_ARCHIVE:
      return "MODEL_ARTIFACT_TYPE_TAR_BZ2_ARCHIVE";
    case ModelArtifactType.MODEL_ARTIFACT_TYPE_TAR_XZ_ARCHIVE:
      return "MODEL_ARTIFACT_TYPE_TAR_XZ_ARCHIVE";
    case ModelArtifactType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Model registry lifecycle state. This is durable/catalog state, not a live
 * transfer progress stream. Per-download byte counters and transient progress
 * events stay in download_service.proto.
 * Sources pre-IDL:
 *   Web ModelRegistry.ts ManagedModel.status (registered/downloading/downloaded/loading/loaded/error)
 *   RN  ModelInfo.isDownloaded/isAvailable and registry query criteria
 * ---------------------------------------------------------------------------
 */
export enum ModelRegistryStatus {
  MODEL_REGISTRY_STATUS_UNSPECIFIED = 0,
  MODEL_REGISTRY_STATUS_REGISTERED = 1,
  MODEL_REGISTRY_STATUS_DOWNLOADING = 2,
  MODEL_REGISTRY_STATUS_DOWNLOADED = 3,
  MODEL_REGISTRY_STATUS_LOADING = 4,
  MODEL_REGISTRY_STATUS_LOADED = 5,
  MODEL_REGISTRY_STATUS_ERROR = 6,
  UNRECOGNIZED = -1,
}

export function modelRegistryStatusFromJSON(object: any): ModelRegistryStatus {
  switch (object) {
    case 0:
    case "MODEL_REGISTRY_STATUS_UNSPECIFIED":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_UNSPECIFIED;
    case 1:
    case "MODEL_REGISTRY_STATUS_REGISTERED":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_REGISTERED;
    case 2:
    case "MODEL_REGISTRY_STATUS_DOWNLOADING":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_DOWNLOADING;
    case 3:
    case "MODEL_REGISTRY_STATUS_DOWNLOADED":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_DOWNLOADED;
    case 4:
    case "MODEL_REGISTRY_STATUS_LOADING":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_LOADING;
    case 5:
    case "MODEL_REGISTRY_STATUS_LOADED":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_LOADED;
    case 6:
    case "MODEL_REGISTRY_STATUS_ERROR":
      return ModelRegistryStatus.MODEL_REGISTRY_STATUS_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelRegistryStatus.UNRECOGNIZED;
  }
}

export function modelRegistryStatusToJSON(object: ModelRegistryStatus): string {
  switch (object) {
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_UNSPECIFIED:
      return "MODEL_REGISTRY_STATUS_UNSPECIFIED";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_REGISTERED:
      return "MODEL_REGISTRY_STATUS_REGISTERED";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_DOWNLOADING:
      return "MODEL_REGISTRY_STATUS_DOWNLOADING";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_DOWNLOADED:
      return "MODEL_REGISTRY_STATUS_DOWNLOADED";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_LOADING:
      return "MODEL_REGISTRY_STATUS_LOADING";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_LOADED:
      return "MODEL_REGISTRY_STATUS_LOADED";
    case ModelRegistryStatus.MODEL_REGISTRY_STATUS_ERROR:
      return "MODEL_REGISTRY_STATUS_ERROR";
    case ModelRegistryStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ModelQuerySortField {
  MODEL_QUERY_SORT_FIELD_UNSPECIFIED = 0,
  MODEL_QUERY_SORT_FIELD_NAME = 1,
  MODEL_QUERY_SORT_FIELD_CREATED_AT_UNIX_MS = 2,
  MODEL_QUERY_SORT_FIELD_UPDATED_AT_UNIX_MS = 3,
  MODEL_QUERY_SORT_FIELD_DOWNLOAD_SIZE_BYTES = 4,
  MODEL_QUERY_SORT_FIELD_LAST_USED_AT_UNIX_MS = 5,
  MODEL_QUERY_SORT_FIELD_USAGE_COUNT = 6,
  UNRECOGNIZED = -1,
}

export function modelQuerySortFieldFromJSON(object: any): ModelQuerySortField {
  switch (object) {
    case 0:
    case "MODEL_QUERY_SORT_FIELD_UNSPECIFIED":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_UNSPECIFIED;
    case 1:
    case "MODEL_QUERY_SORT_FIELD_NAME":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_NAME;
    case 2:
    case "MODEL_QUERY_SORT_FIELD_CREATED_AT_UNIX_MS":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_CREATED_AT_UNIX_MS;
    case 3:
    case "MODEL_QUERY_SORT_FIELD_UPDATED_AT_UNIX_MS":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_UPDATED_AT_UNIX_MS;
    case 4:
    case "MODEL_QUERY_SORT_FIELD_DOWNLOAD_SIZE_BYTES":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_DOWNLOAD_SIZE_BYTES;
    case 5:
    case "MODEL_QUERY_SORT_FIELD_LAST_USED_AT_UNIX_MS":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_LAST_USED_AT_UNIX_MS;
    case 6:
    case "MODEL_QUERY_SORT_FIELD_USAGE_COUNT":
      return ModelQuerySortField.MODEL_QUERY_SORT_FIELD_USAGE_COUNT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelQuerySortField.UNRECOGNIZED;
  }
}

export function modelQuerySortFieldToJSON(object: ModelQuerySortField): string {
  switch (object) {
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_UNSPECIFIED:
      return "MODEL_QUERY_SORT_FIELD_UNSPECIFIED";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_NAME:
      return "MODEL_QUERY_SORT_FIELD_NAME";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_CREATED_AT_UNIX_MS:
      return "MODEL_QUERY_SORT_FIELD_CREATED_AT_UNIX_MS";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_UPDATED_AT_UNIX_MS:
      return "MODEL_QUERY_SORT_FIELD_UPDATED_AT_UNIX_MS";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_DOWNLOAD_SIZE_BYTES:
      return "MODEL_QUERY_SORT_FIELD_DOWNLOAD_SIZE_BYTES";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_LAST_USED_AT_UNIX_MS:
      return "MODEL_QUERY_SORT_FIELD_LAST_USED_AT_UNIX_MS";
    case ModelQuerySortField.MODEL_QUERY_SORT_FIELD_USAGE_COUNT:
      return "MODEL_QUERY_SORT_FIELD_USAGE_COUNT";
    case ModelQuerySortField.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ModelQuerySortOrder {
  MODEL_QUERY_SORT_ORDER_UNSPECIFIED = 0,
  MODEL_QUERY_SORT_ORDER_ASCENDING = 1,
  MODEL_QUERY_SORT_ORDER_DESCENDING = 2,
  UNRECOGNIZED = -1,
}

export function modelQuerySortOrderFromJSON(object: any): ModelQuerySortOrder {
  switch (object) {
    case 0:
    case "MODEL_QUERY_SORT_ORDER_UNSPECIFIED":
      return ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_UNSPECIFIED;
    case 1:
    case "MODEL_QUERY_SORT_ORDER_ASCENDING":
      return ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_ASCENDING;
    case 2:
    case "MODEL_QUERY_SORT_ORDER_DESCENDING":
      return ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_DESCENDING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelQuerySortOrder.UNRECOGNIZED;
  }
}

export function modelQuerySortOrderToJSON(object: ModelQuerySortOrder): string {
  switch (object) {
    case ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_UNSPECIFIED:
      return "MODEL_QUERY_SORT_ORDER_UNSPECIFIED";
    case ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_ASCENDING:
      return "MODEL_QUERY_SORT_ORDER_ASCENDING";
    case ModelQuerySortOrder.MODEL_QUERY_SORT_ORDER_DESCENDING:
      return "MODEL_QUERY_SORT_ORDER_DESCENDING";
    case ModelQuerySortOrder.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Role of a file inside a single/multi-file artifact. The generic COMPANION
 * role covers arbitrary sidecars; specific roles document common public
 * catalog files such as VLM mmproj files and tokenizer/config assets.
 */
export enum ModelFileRole {
  MODEL_FILE_ROLE_UNSPECIFIED = 0,
  MODEL_FILE_ROLE_PRIMARY_MODEL = 1,
  MODEL_FILE_ROLE_COMPANION = 2,
  /** MODEL_FILE_ROLE_VISION_PROJECTOR - llama.cpp VLM mmproj*.gguf */
  MODEL_FILE_ROLE_VISION_PROJECTOR = 3,
  /** MODEL_FILE_ROLE_TOKENIZER - tokenizer model/data files */
  MODEL_FILE_ROLE_TOKENIZER = 4,
  /** MODEL_FILE_ROLE_CONFIG - config.json or framework config */
  MODEL_FILE_ROLE_CONFIG = 5,
  /** MODEL_FILE_ROLE_VOCABULARY - vocab.txt / vocab.json */
  MODEL_FILE_ROLE_VOCABULARY = 6,
  /** MODEL_FILE_ROLE_MERGES - merges.txt */
  MODEL_FILE_ROLE_MERGES = 7,
  MODEL_FILE_ROLE_LABELS = 8,
  UNRECOGNIZED = -1,
}

export function modelFileRoleFromJSON(object: any): ModelFileRole {
  switch (object) {
    case 0:
    case "MODEL_FILE_ROLE_UNSPECIFIED":
      return ModelFileRole.MODEL_FILE_ROLE_UNSPECIFIED;
    case 1:
    case "MODEL_FILE_ROLE_PRIMARY_MODEL":
      return ModelFileRole.MODEL_FILE_ROLE_PRIMARY_MODEL;
    case 2:
    case "MODEL_FILE_ROLE_COMPANION":
      return ModelFileRole.MODEL_FILE_ROLE_COMPANION;
    case 3:
    case "MODEL_FILE_ROLE_VISION_PROJECTOR":
      return ModelFileRole.MODEL_FILE_ROLE_VISION_PROJECTOR;
    case 4:
    case "MODEL_FILE_ROLE_TOKENIZER":
      return ModelFileRole.MODEL_FILE_ROLE_TOKENIZER;
    case 5:
    case "MODEL_FILE_ROLE_CONFIG":
      return ModelFileRole.MODEL_FILE_ROLE_CONFIG;
    case 6:
    case "MODEL_FILE_ROLE_VOCABULARY":
      return ModelFileRole.MODEL_FILE_ROLE_VOCABULARY;
    case 7:
    case "MODEL_FILE_ROLE_MERGES":
      return ModelFileRole.MODEL_FILE_ROLE_MERGES;
    case 8:
    case "MODEL_FILE_ROLE_LABELS":
      return ModelFileRole.MODEL_FILE_ROLE_LABELS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ModelFileRole.UNRECOGNIZED;
  }
}

export function modelFileRoleToJSON(object: ModelFileRole): string {
  switch (object) {
    case ModelFileRole.MODEL_FILE_ROLE_UNSPECIFIED:
      return "MODEL_FILE_ROLE_UNSPECIFIED";
    case ModelFileRole.MODEL_FILE_ROLE_PRIMARY_MODEL:
      return "MODEL_FILE_ROLE_PRIMARY_MODEL";
    case ModelFileRole.MODEL_FILE_ROLE_COMPANION:
      return "MODEL_FILE_ROLE_COMPANION";
    case ModelFileRole.MODEL_FILE_ROLE_VISION_PROJECTOR:
      return "MODEL_FILE_ROLE_VISION_PROJECTOR";
    case ModelFileRole.MODEL_FILE_ROLE_TOKENIZER:
      return "MODEL_FILE_ROLE_TOKENIZER";
    case ModelFileRole.MODEL_FILE_ROLE_CONFIG:
      return "MODEL_FILE_ROLE_CONFIG";
    case ModelFileRole.MODEL_FILE_ROLE_VOCABULARY:
      return "MODEL_FILE_ROLE_VOCABULARY";
    case ModelFileRole.MODEL_FILE_ROLE_MERGES:
      return "MODEL_FILE_ROLE_MERGES";
    case ModelFileRole.MODEL_FILE_ROLE_LABELS:
      return "MODEL_FILE_ROLE_LABELS";
    case ModelFileRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * Routing policy for hybrid (on-device vs cloud) inference. Sources pre-IDL:
 *   Web    enums.ts (RoutingPolicy)
 *          OnDevicePreferred / CloudPreferred / OnDeviceOnly / CloudOnly /
 *          Hybrid / CostOptimized / LatencyOptimized / PrivacyOptimized
 *   Swift  extensions (RoutingPolicy)
 * Canonical short-form below; specific PreferLocal/PreferCloud cover the
 * "preferred" cases, MANUAL covers explicit user override.
 * ---------------------------------------------------------------------------
 */
export enum RoutingPolicy {
  ROUTING_POLICY_UNSPECIFIED = 0,
  ROUTING_POLICY_PREFER_LOCAL = 1,
  ROUTING_POLICY_PREFER_CLOUD = 2,
  ROUTING_POLICY_COST_OPTIMIZED = 3,
  ROUTING_POLICY_LATENCY_OPTIMIZED = 4,
  ROUTING_POLICY_MANUAL = 5,
  UNRECOGNIZED = -1,
}

export function routingPolicyFromJSON(object: any): RoutingPolicy {
  switch (object) {
    case 0:
    case "ROUTING_POLICY_UNSPECIFIED":
      return RoutingPolicy.ROUTING_POLICY_UNSPECIFIED;
    case 1:
    case "ROUTING_POLICY_PREFER_LOCAL":
      return RoutingPolicy.ROUTING_POLICY_PREFER_LOCAL;
    case 2:
    case "ROUTING_POLICY_PREFER_CLOUD":
      return RoutingPolicy.ROUTING_POLICY_PREFER_CLOUD;
    case 3:
    case "ROUTING_POLICY_COST_OPTIMIZED":
      return RoutingPolicy.ROUTING_POLICY_COST_OPTIMIZED;
    case 4:
    case "ROUTING_POLICY_LATENCY_OPTIMIZED":
      return RoutingPolicy.ROUTING_POLICY_LATENCY_OPTIMIZED;
    case 5:
    case "ROUTING_POLICY_MANUAL":
      return RoutingPolicy.ROUTING_POLICY_MANUAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RoutingPolicy.UNRECOGNIZED;
  }
}

export function routingPolicyToJSON(object: RoutingPolicy): string {
  switch (object) {
    case RoutingPolicy.ROUTING_POLICY_UNSPECIFIED:
      return "ROUTING_POLICY_UNSPECIFIED";
    case RoutingPolicy.ROUTING_POLICY_PREFER_LOCAL:
      return "ROUTING_POLICY_PREFER_LOCAL";
    case RoutingPolicy.ROUTING_POLICY_PREFER_CLOUD:
      return "ROUTING_POLICY_PREFER_CLOUD";
    case RoutingPolicy.ROUTING_POLICY_COST_OPTIMIZED:
      return "ROUTING_POLICY_COST_OPTIMIZED";
    case RoutingPolicy.ROUTING_POLICY_LATENCY_OPTIMIZED:
      return "ROUTING_POLICY_LATENCY_OPTIMIZED";
    case RoutingPolicy.ROUTING_POLICY_MANUAL:
      return "ROUTING_POLICY_MANUAL";
    case RoutingPolicy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ModelInfoMetadata {
  description: string;
  author: string;
  license: string;
  tags: string[];
  version: string;
}

export interface ModelRuntimeCompatibility {
  compatibleFrameworks: InferenceFramework[];
  compatibleFormats: ModelFormat[];
}

/**
 * ---------------------------------------------------------------------------
 * Core metadata for a model entry.
 * Sources pre-IDL:
 *   Swift  ModelTypes.swift:393       (16 fields)
 *   Kotlin ModelTypes.kt:332          (16 fields, Long vs Int drift on download size)
 *   Dart   model_types.dart:335       (similar shape, nullable divergences)
 *   RN     HybridRunAnywhereCore.cpp:995-1010 (13 fields, string-typed category/format)
 * ---------------------------------------------------------------------------
 */
export interface ModelInfo {
  id: string;
  name: string;
  category: ModelCategory;
  format: ModelFormat;
  framework: InferenceFramework;
  /**
   * Portable URL/URI string for catalog metadata and download planning.
   * SDK/platform adapters own native HTTP execution, authentication/session
   * state, browser fetch handles, URLSession/background-transfer objects,
   * and permission prompts.
   */
  downloadUrl: string;
  /**
   * Stable path or URI string after platform adapters have normalized native
   * file handles. Do not place Android SAF/content URI permissions, iOS
   * security-scoped bookmarks, browser FileSystemHandle objects, or other
   * OS-governed capabilities in this C++-owned metadata field.
   */
  localPath: string;
  downloadSizeBytes: number;
  contextLength: number;
  supportsThinking: boolean;
  supportsLora: boolean;
  source: ModelSource;
  createdAtUnixMs: number;
  updatedAtUnixMs: number;
  /**
   * Separate from download_size_bytes: this is the estimated runtime RAM
   * requirement used by compatibility checks and model selection UIs.
   */
  memoryRequiredBytes?:
    | number
    | undefined;
  /**
   * Lowercase hex SHA-256 checksum for the primary artifact. Per-file
   * checksums for multi-file artifacts live on ModelFileDescriptor.
   */
  checksumSha256?:
    | string
    | undefined;
  /**
   * Thinking/reasoning metadata. `supports_thinking` remains the boolean
   * capability flag; this optional pattern declares model-specific tags.
   */
  thinkingPattern?:
    | ThinkingTagPattern
    | undefined;
  /** Structured public catalog metadata, including the model description. */
  metadata?: ModelInfoMetadata | undefined;
  singleFile?: SingleFileArtifact | undefined;
  archive?: ArchiveArtifact | undefined;
  multiFile?: MultiFileArtifact | undefined;
  customStrategyId?: string | undefined;
  builtIn?:
    | boolean
    | undefined;
  /**
   * High-level artifact classification, complementary to the `artifact`
   * oneof above. Allows catalog entries to carry a coarse type tag without
   * resolving the full strategy variant.
   */
  artifactType?:
    | ModelArtifactType
    | undefined;
  /** Manifest of files that are expected on disk after fetch/extraction. */
  expectedFiles?:
    | ExpectedModelFiles
    | undefined;
  /** Preferred hardware acceleration backend for this model. */
  accelerationPreference?:
    | AccelerationPreference
    | undefined;
  /** Hybrid (on-device vs cloud) routing policy for this entry. */
  routingPolicy?:
    | RoutingPolicy
    | undefined;
  /**
   * Framework/format compatibility declarations. `framework` (field 5) is
   * the canonical/preferred runtime when no explicit preferred_framework is set.
   */
  compatibility?: ModelRuntimeCompatibility | undefined;
  preferredFramework?:
    | InferenceFramework
    | undefined;
  /**
   * Durable registry state. Live byte progress belongs to
   * download_service.DownloadProgress, not ModelInfo.
   */
  registryStatus?: ModelRegistryStatus | undefined;
  isDownloaded?: boolean | undefined;
  isAvailable?: boolean | undefined;
  lastUsedAtUnixMs?: number | undefined;
  usageCount?: number | undefined;
  syncPending?: boolean | undefined;
  statusMessage?: string | undefined;
}

/**
 * Repeated model registry responses use this wrapper because protobuf cannot
 * serialize a bare repeated field as a top-level message.
 */
export interface ModelInfoList {
  models: ModelInfo[];
}

export interface SingleFileArtifact {
  requiredPatterns: string[];
  optionalPatterns: string[];
  /**
   * Full manifest form for SDK-local wrappers that attach expected files to
   * a single-file artifact. The pattern fields above remain for existing
   * generated consumers.
   */
  expectedFiles?: ExpectedModelFiles | undefined;
}

export interface ArchiveArtifact {
  type: ArchiveType;
  structure: ArchiveStructure;
  requiredPatterns: string[];
  optionalPatterns: string[];
  /**
   * Full manifest form for archive artifacts after extraction. Archive
   * extraction policy is portable; native filesystem permissions and handles
   * remain adapter-owned.
   */
  expectedFiles?: ExpectedModelFiles | undefined;
}

export interface ModelFileDescriptor {
  url: string;
  filename: string;
  isRequired: boolean;
  /**
   * Extended descriptor fields (Flutter model_types.dart:~350,
   * Swift ModelTypes.swift:~350). `is_required` (field 3) remains the
   * canonical "required" flag — the documented `required` boolean from
   * newer SDK sources maps onto it (default true, mirrored in Swift).
   */
  sizeBytes?:
    | number
    | undefined;
  /**
   * Path fields used by SDK-local wrappers/catalogs. `filename` is the
   * storage name for simple cases; relative_path/destination_path preserve
   * directory layouts for archive and multi-file artifacts.
   */
  relativePath?: string | undefined;
  destinationPath?: string | undefined;
  role?: ModelFileRole | undefined;
  localPath?: string | undefined;
  checksumSha256?: string | undefined;
}

export interface MultiFileArtifact {
  files: ModelFileDescriptor[];
}

/**
 * ---------------------------------------------------------------------------
 * Declarative manifest of files a multi-file / directory model is expected
 * to contain on disk after download/extraction. Used for verification before
 * hand-off to the inference framework. Sources pre-IDL:
 *   Flutter core/types/model_types.dart:420
 *   Swift   ModelTypes.swift:~300
 * ---------------------------------------------------------------------------
 */
export interface ExpectedModelFiles {
  files: ModelFileDescriptor[];
  rootDirectory?: string | undefined;
  requiredPatterns: string[];
  optionalPatterns: string[];
  description?: string | undefined;
}

/**
 * Registry/query filters shared by SDK model-management APIs. UI-only
 * presentation state and platform filesystem handles are intentionally not
 * represented here.
 */
export interface ModelQuery {
  framework?: InferenceFramework | undefined;
  category?: ModelCategory | undefined;
  format?: ModelFormat | undefined;
  downloadedOnly?: boolean | undefined;
  availableOnly?: boolean | undefined;
  maxSizeBytes?: number | undefined;
  searchQuery: string;
  source?: ModelSource | undefined;
  sortField?: ModelQuerySortField | undefined;
  sortOrder?: ModelQuerySortOrder | undefined;
  registryStatus?: ModelRegistryStatus | undefined;
}

export interface ModelRegistryRefreshRequest {
  /** Fetch or merge a remote catalog through the platform/network adapter. */
  includeRemoteCatalog: boolean;
  /** Scan managed model directories and link valid on-disk artifacts. */
  rescanLocal: boolean;
  /** Clear downloaded/available state for registry rows whose files vanished. */
  pruneOrphans: boolean;
  /** Optional post-refresh filter for the returned model list. */
  query?:
    | ModelQuery
    | undefined;
  /**
   * Portable catalog selector. Auth state, cookies, native HTTP clients, and
   * background transfer handles are supplied by platform adapters.
   */
  catalogUri: string;
  /** Ignore cached catalog metadata and force a fresh adapter-backed refresh. */
  forceRefresh: boolean;
  /** Include local downloaded/available state reconciliation in the refresh. */
  includeDownloadedState: boolean;
}

export interface ModelRegistryRefreshResult {
  success: boolean;
  models?: ModelInfoList | undefined;
  registeredCount: number;
  updatedCount: number;
  discoveredCount: number;
  prunedCount: number;
  refreshedAtUnixMs: number;
  warnings: string[];
  errorMessage: string;
  downloadedCount: number;
  availableCount: number;
  errorCount: number;
}

export interface ModelListRequest {
  /** Set query.downloaded_only for downloaded-only lists. */
  query?:
    | ModelQuery
    | undefined;
  /** Include denormalized counts in ModelListResult. */
  includeCounts: boolean;
}

export interface ModelListResult {
  success: boolean;
  models?: ModelInfoList | undefined;
  errorMessage: string;
  totalCount: number;
  downloadedCount: number;
  availableCount: number;
  filteredCount: number;
}

export interface ModelGetRequest {
  modelId: string;
}

export interface ModelGetResult {
  found: boolean;
  model?: ModelInfo | undefined;
  errorMessage: string;
}

export interface ModelImportRequest {
  /**
   * Catalog metadata to register or merge. If absent, discovery may infer a
   * minimal ModelInfo from the file name and detected format.
   */
  model?:
    | ModelInfo
    | undefined;
  /**
   * Normalized path under platform control. Do not place transient OS file
   * picker handles in this field; adapters should first copy/link/authorize
   * them and provide a stable path visible to the C++ workflow.
   */
  sourcePath: string;
  copyIntoManagedStorage: boolean;
  overwriteExisting: boolean;
  files: ModelFileDescriptor[];
  /** Validate format, expected files, and checksums before registry mutation. */
  validateBeforeRegister: boolean;
}

export interface ModelImportResult {
  success: boolean;
  model?: ModelInfo | undefined;
  localPath: string;
  importedBytes: number;
  warnings: string[];
  errorMessage: string;
  registered: boolean;
  copiedIntoManagedStorage: boolean;
}

export interface ModelDiscoveryRequest {
  /**
   * Platform adapters own permission and sandbox traversal. These are stable
   * roots that C++ may inspect using registered filesystem callbacks.
   */
  searchRoots: string[];
  recursive: boolean;
  linkDownloaded: boolean;
  purgeInvalid: boolean;
  query?: ModelQuery | undefined;
  includeBuiltIn: boolean;
  includeUserImports: boolean;
}

export interface DiscoveredModel {
  modelId: string;
  localPath: string;
  matchedRegistry: boolean;
  model?: ModelInfo | undefined;
  sizeBytes: number;
  warnings: string[];
}

export interface ModelDiscoveryResult {
  success: boolean;
  discoveredModels: DiscoveredModel[];
  linkedCount: number;
  purgedCount: number;
  warnings: string[];
  errorMessage: string;
  scannedCount: number;
  importedCount: number;
}

export interface ModelLoadRequest {
  modelId: string;
  category?: ModelCategory | undefined;
  framework?: InferenceFramework | undefined;
  forceReload: boolean;
  validateAvailability: boolean;
}

export interface ModelLoadResult {
  success: boolean;
  modelId: string;
  category: ModelCategory;
  framework: InferenceFramework;
  resolvedPath: string;
  loadedAtUnixMs: number;
  errorMessage: string;
  warnings: string[];
  alreadyLoaded: boolean;
  /**
   * Concrete artifacts selected by C++ model path resolution. The primary
   * model entry mirrors resolved_path; companion entries carry explicit
   * ModelFileRole values such as MODEL_FILE_ROLE_VISION_PROJECTOR.
   */
  resolvedArtifacts: ModelFileDescriptor[];
}

export interface ModelUnloadRequest {
  modelId: string;
  category?: ModelCategory | undefined;
  unloadAll: boolean;
  framework?: InferenceFramework | undefined;
}

export interface ModelUnloadResult {
  success: boolean;
  unloadedModelIds: string[];
  errorMessage: string;
  unloadedAtUnixMs: number;
  warnings: string[];
}

export interface CurrentModelRequest {
  category?: ModelCategory | undefined;
  framework?: InferenceFramework | undefined;
  includeModelMetadata: boolean;
}

export interface CurrentModelResult {
  modelId: string;
  model?: ModelInfo | undefined;
  loadedAtUnixMs: number;
  found: boolean;
  errorMessage: string;
  category: ModelCategory;
  framework: InferenceFramework;
  resolvedPath: string;
  resolvedArtifacts: ModelFileDescriptor[];
}

export interface ModelDeleteRequest {
  modelId: string;
  deleteFiles: boolean;
  unregister: boolean;
  unloadIfLoaded: boolean;
}

export interface ModelDeleteResult {
  success: boolean;
  modelId: string;
  deletedBytes: number;
  filesDeleted: boolean;
  registryUpdated: boolean;
  wasLoaded: boolean;
  errorMessage: string;
  warnings: string[];
}

/**
 * ---------------------------------------------------------------------------
 * Compatibility check request/result. Mirrors the public SDK
 * `checkCompatibility(modelId)` calls (RN CompatibilityBridge,
 * Kotlin compat path, Web ModelManager). The platform adapter supplies
 * available_ram_bytes / available_storage_bytes; commons looks up the
 * registry entry, computes the compatibility verdict (canRun / canFit),
 * and returns reasons / suggested alternative model ids.
 * ---------------------------------------------------------------------------
 */
export interface ModelCompatibilityRequest {
  /** Required. Model identifier to evaluate. */
  modelId: string;
  /**
   * Optional cached hardware profile from the platform adapter. If
   * unset, commons will read whatever it has cached internally; the
   * RAM/storage values below remain authoritative for the verdict.
   */
  hardwareProfile?:
    | HardwareProfile
    | undefined;
  /**
   * Available RAM in bytes (from device probe). 0 = unknown — commons
   * will treat the requirement as satisfied.
   */
  availableRamBytes: number;
  /** Available storage in bytes (from filesystem probe). 0 = unknown. */
  availableStorageBytes: number;
  /**
   * Optional caller preferences (acceleration, framework). Reserved for
   * future use; today's verdict is based on memory/storage alone.
   */
  acceleratorPreference?: AccelerationPreference | undefined;
  preferredFramework?: InferenceFramework | undefined;
}

export interface ModelCompatibilityResult {
  /**
   * Mirrors the existing struct fields so SDKs can keep using the same
   * field names; populated from rac_model_compatibility_result_t.
   */
  isCompatible: boolean;
  canRun: boolean;
  canFit: boolean;
  requiredMemoryBytes: number;
  availableMemoryBytes: number;
  requiredStorageBytes: number;
  availableStorageBytes: number;
  /**
   * Human-readable reasons populated when the verdict is negative
   * (e.g. "insufficient RAM: requires X, available Y").
   */
  reasons: string[];
  /**
   * Optional suggested alternative model ids that *would* be compatible.
   * The current implementation leaves this empty; reserved for future
   * compatibility-aware suggestions.
   */
  suggestedAlternatives: string[];
  /**
   * Echo of the looked-up model id so callers can correlate batched
   * checks with their request id.
   */
  modelId: string;
  /**
   * Negative on failure; mirrors rac_result_t. Empty error_message on
   * success.
   */
  errorCode: number;
  errorMessage: string;
}

/**
 * ---------------------------------------------------------------------------
 * URL → ModelFormat inference request/result. Moves the Dart/Kotlin-side
 * URL-suffix heuristic (".gguf" → GGUF, ".onnx" → ONNX, ".tar.gz" wrapping an
 * inner format, ...) into commons so every SDK uses one implementation.
 * ---------------------------------------------------------------------------
 */
export interface ModelFormatFromUrlRequest {
  /**
   * Portable URL or file path string. Only the trailing file-extension
   * suffix is inspected; query strings and fragments are ignored.
   */
  url: string;
}

export interface ModelFormatFromUrlResult {
  /**
   * Primary detected format. For archive URLs this is the archive-wrapper
   * format (for example MODEL_FORMAT_ZIP); the extracted model format is
   * in inner_format below.
   */
  format: ModelFormat;
  /**
   * For archive URLs, the format of the primary file inside the archive
   * when it can be inferred from the URL (for example
   * "whisper-base.en.tar.gz" → inner_format = MODEL_FORMAT_ONNX). When the
   * archive content is unknown this is MODEL_FORMAT_UNSPECIFIED.
   */
  innerFormat: ModelFormat;
}

/**
 * ---------------------------------------------------------------------------
 * URL → ModelArtifactType inference request/result. Replaces Dart
 * withInferredArtifact and Kotlin inferArtifactFields with a single commons
 * call.
 * ---------------------------------------------------------------------------
 */
export interface ArtifactInferFromUrlRequest {
  /** Portable URL or file path string. */
  url: string;
  /**
   * Optional model identifier. Commons does not consult the registry with
   * this value today; it is carried for logging and telemetry only.
   */
  modelId: string;
}

export interface ArtifactInferFromUrlResult {
  /** Inferred artifact-type classification. */
  artifactType: ModelArtifactType;
  /**
   * For archive artifacts, the concrete archive format (ZIP, TAR_GZ, ...).
   * For single-file or directory artifacts this is
   * ARCHIVE_TYPE_UNSPECIFIED.
   */
  archiveType: ArchiveType;
  /**
   * For archive artifacts the known or inferred internal structure after
   * extraction. Defaults to ARCHIVE_STRUCTURE_UNKNOWN.
   */
  archiveStructure: ArchiveStructure;
  /**
   * When the URL suggests an archive wrapping a known primary file (for
   * example a Whisper model bundle containing encoder.onnx), this field
   * carries the relative path inside the archive when it can be inferred.
   * Empty otherwise.
   */
  primaryRelpath: string;
  /**
   * Inner file format for archive artifacts. MODEL_FORMAT_UNSPECIFIED when
   * the archive contents are unknown.
   */
  innerFormat: ModelFormat;
}

/**
 * ---------------------------------------------------------------------------
 * FetchAssignments request/result. Replaces the JSON shim
 * racModelRegistryFetchAssignments and the Web SDK's offline-friendly
 * fetchModelAssignments() entry point. The platform adapter owns HTTP
 * transport; commons consumes the cached / fetched entries and returns a
 * canonical proto byte payload.
 * ---------------------------------------------------------------------------
 */
export interface ModelRegistryFetchAssignmentsRequest {
  /**
   * Optional device identifier (forwarded to the platform adapter for
   * any auth headers it needs). May be empty when callers rely on
   * adapter-side auth state alone.
   */
  deviceId: string;
  /**
   * Optional environment selector; commons does not branch on this
   * value today, but it is preserved for adapter routing and telemetry.
   */
  environment?:
    | SDKEnvironment
    | undefined;
  /** Bypass the assignment cache and force a fresh fetch. */
  forceRefresh: boolean;
}

export interface ModelRegistryFetchAssignmentsResult {
  success: boolean;
  models?: ModelInfoList | undefined;
  modelCount: number;
  fetchedAtUnixMs: number;
  errorCode: number;
  errorMessage: string;
}

/**
 * ---------------------------------------------------------------------------
 * Inputs for the canonical RAModelInfo factory. Replaces Swift's
 * `RAModelInfo.make(...)` ~370 LOC of field-defaulting and artifact-inference
 * logic with a commons-owned implementation. Commons fills 18 ModelInfo fields
 * (id, name, category/format/framework defaults, context-length defaults,
 * thinking gating + default pattern, artifact inference, source mark,
 * timestamps, and is_downloaded probe).
 * ---------------------------------------------------------------------------
 */
export interface ModelInfoMakeRequest {
  /**
   * Required. Download URL or file path. Used both as the metadata field
   * and as input to artifact-type inference (zip/tar.gz/tgz/... → archive,
   * anything else → single_file).
   */
  url: string;
  /**
   * Optional human-readable name. When empty commons derives it from the
   * URL via rac_model_generate_name() (replaces underscores/dashes with
   * spaces).
   */
  name: string;
  /**
   * Optional inference framework. UNSPECIFIED triggers detection from the
   * URL extension; commons looks up the format and maps to a default
   * framework via rac_model_detect_framework_from_format().
   */
  framework?:
    | InferenceFramework
    | undefined;
  /**
   * Optional category. UNSPECIFIED falls back to the framework default
   * (rac_model_category_from_framework()).
   */
  category?:
    | ModelCategory
    | undefined;
  /** Optional source. UNSPECIFIED is treated as MODEL_SOURCE_REMOTE. */
  source?: ModelSource | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * Inputs for the canonical "register a model from a URL" entry point.
 * Composes ModelInfoMakeRequest with the existing registry save path
 * so SDKs replace ~60 LOC of build-and-save glue with a single ABI call.
 * Produces the saved ModelInfo (matches rac_model_registry_register_proto_buffer
 * shape).
 * ---------------------------------------------------------------------------
 */
export interface RegisterModelFromUrlRequest {
  /**
   * Required. Download URL or file path. Routed straight into
   * ModelInfoMakeRequest.url; format/artifact inference and id/name
   * generation reuse the same factory semantics.
   */
  url: string;
  /** Optional human-readable name. Empty → derived from URL. */
  name: string;
  /**
   * Optional inference framework. UNSPECIFIED triggers detection from the
   * URL extension (rac_model_detect_framework_from_format).
   */
  framework?:
    | InferenceFramework
    | undefined;
  /** Optional category. UNSPECIFIED falls back to the framework default. */
  category?:
    | ModelCategory
    | undefined;
  /** Optional source. UNSPECIFIED is treated as MODEL_SOURCE_REMOTE. */
  source?:
    | ModelSource
    | undefined;
  /**
   * Caller-supplied capability fields. When set, the register-from-url C++
   * path honors them on the saved ModelInfo instead of its inference
   * defaults (which hardcode supports_lora=false, download_size=0, infer
   * artifact_type from the URL). This lets every SDK drop the post-register
   * "patch + resave" pass. Tags 6-13 are free (1-5 stay wire-compatible with
   * ModelInfoMakeRequest).
   */
  memoryRequiredBytes?: number | undefined;
  supportsThinking?: boolean | undefined;
  supportsLora?: boolean | undefined;
  artifactType?: ModelArtifactType | undefined;
  contextLength?: number | undefined;
  description?: string | undefined;
  downloadSizeBytes?:
    | number
    | undefined;
  /** Explicit id override. Empty -> derived from URL/name. */
  id?: string | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * Inputs for registering a multi-file model (each file carries its own URL,
 * so there is no model-level URL). Replaces the hand-built MultiFileArtifact
 * ModelInfo every SDK assembles today. Produces the saved ModelInfo.
 * ---------------------------------------------------------------------------
 */
export interface RegisterMultiFileModelRequest {
  id: string;
  name: string;
  framework: InferenceFramework;
  files: ModelFileDescriptor[];
  category?: ModelCategory | undefined;
  format?: ModelFormat | undefined;
  memoryRequiredBytes?: number | undefined;
  downloadSizeBytes?: number | undefined;
  contextLength?: number | undefined;
  supportsThinking?: boolean | undefined;
  supportsLora?: boolean | undefined;
  description?: string | undefined;
  source?: ModelSource | undefined;
}

function createBaseModelInfoMetadata(): ModelInfoMetadata {
  return { description: "", author: "", license: "", tags: [], version: "" };
}

export const ModelInfoMetadata: MessageFns<ModelInfoMetadata> = {
  encode(message: ModelInfoMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    if (message.author !== "") {
      writer.uint32(18).string(message.author);
    }
    if (message.license !== "") {
      writer.uint32(26).string(message.license);
    }
    for (const v of message.tags) {
      writer.uint32(34).string(v!);
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelInfoMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelInfoMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.license = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelInfoMetadata {
    return {
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      license: isSet(object.license) ? globalThis.String(object.license) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: ModelInfoMetadata): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.license !== "") {
      obj.license = message.license;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelInfoMetadata>, I>>(base?: I): ModelInfoMetadata {
    return ModelInfoMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelInfoMetadata>, I>>(object: I): ModelInfoMetadata {
    const message = createBaseModelInfoMetadata();
    message.description = object.description ?? "";
    message.author = object.author ?? "";
    message.license = object.license ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseModelRuntimeCompatibility(): ModelRuntimeCompatibility {
  return { compatibleFrameworks: [], compatibleFormats: [] };
}

export const ModelRuntimeCompatibility: MessageFns<ModelRuntimeCompatibility> = {
  encode(message: ModelRuntimeCompatibility, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.compatibleFrameworks) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.compatibleFormats) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelRuntimeCompatibility {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelRuntimeCompatibility();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.compatibleFrameworks.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.compatibleFrameworks.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.compatibleFormats.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.compatibleFormats.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelRuntimeCompatibility {
    return {
      compatibleFrameworks: globalThis.Array.isArray(object?.compatibleFrameworks)
        ? object.compatibleFrameworks.map((e: any) => inferenceFrameworkFromJSON(e))
        : globalThis.Array.isArray(object?.compatible_frameworks)
        ? object.compatible_frameworks.map((e: any) => inferenceFrameworkFromJSON(e))
        : [],
      compatibleFormats: globalThis.Array.isArray(object?.compatibleFormats)
        ? object.compatibleFormats.map((e: any) => modelFormatFromJSON(e))
        : globalThis.Array.isArray(object?.compatible_formats)
        ? object.compatible_formats.map((e: any) => modelFormatFromJSON(e))
        : [],
    };
  },

  toJSON(message: ModelRuntimeCompatibility): unknown {
    const obj: any = {};
    if (message.compatibleFrameworks?.length) {
      obj.compatibleFrameworks = message.compatibleFrameworks.map((e) => inferenceFrameworkToJSON(e));
    }
    if (message.compatibleFormats?.length) {
      obj.compatibleFormats = message.compatibleFormats.map((e) => modelFormatToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelRuntimeCompatibility>, I>>(base?: I): ModelRuntimeCompatibility {
    return ModelRuntimeCompatibility.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelRuntimeCompatibility>, I>>(object: I): ModelRuntimeCompatibility {
    const message = createBaseModelRuntimeCompatibility();
    message.compatibleFrameworks = object.compatibleFrameworks?.map((e) => e) || [];
    message.compatibleFormats = object.compatibleFormats?.map((e) => e) || [];
    return message;
  },
};

function createBaseModelInfo(): ModelInfo {
  return {
    id: "",
    name: "",
    category: 0,
    format: 0,
    framework: 0,
    downloadUrl: "",
    localPath: "",
    downloadSizeBytes: 0,
    contextLength: 0,
    supportsThinking: false,
    supportsLora: false,
    source: 0,
    createdAtUnixMs: 0,
    updatedAtUnixMs: 0,
    memoryRequiredBytes: undefined,
    checksumSha256: undefined,
    thinkingPattern: undefined,
    metadata: undefined,
    singleFile: undefined,
    archive: undefined,
    multiFile: undefined,
    customStrategyId: undefined,
    builtIn: undefined,
    artifactType: undefined,
    expectedFiles: undefined,
    accelerationPreference: undefined,
    routingPolicy: undefined,
    compatibility: undefined,
    preferredFramework: undefined,
    registryStatus: undefined,
    isDownloaded: undefined,
    isAvailable: undefined,
    lastUsedAtUnixMs: undefined,
    usageCount: undefined,
    syncPending: undefined,
    statusMessage: undefined,
  };
}

export const ModelInfo: MessageFns<ModelInfo> = {
  encode(message: ModelInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.category !== 0) {
      writer.uint32(24).int32(message.category);
    }
    if (message.format !== 0) {
      writer.uint32(32).int32(message.format);
    }
    if (message.framework !== 0) {
      writer.uint32(40).int32(message.framework);
    }
    if (message.downloadUrl !== "") {
      writer.uint32(50).string(message.downloadUrl);
    }
    if (message.localPath !== "") {
      writer.uint32(58).string(message.localPath);
    }
    if (message.downloadSizeBytes !== 0) {
      writer.uint32(64).int64(message.downloadSizeBytes);
    }
    if (message.contextLength !== 0) {
      writer.uint32(72).int32(message.contextLength);
    }
    if (message.supportsThinking !== false) {
      writer.uint32(80).bool(message.supportsThinking);
    }
    if (message.supportsLora !== false) {
      writer.uint32(88).bool(message.supportsLora);
    }
    if (message.source !== 0) {
      writer.uint32(104).int32(message.source);
    }
    if (message.createdAtUnixMs !== 0) {
      writer.uint32(112).int64(message.createdAtUnixMs);
    }
    if (message.updatedAtUnixMs !== 0) {
      writer.uint32(120).int64(message.updatedAtUnixMs);
    }
    if (message.memoryRequiredBytes !== undefined) {
      writer.uint32(128).int64(message.memoryRequiredBytes);
    }
    if (message.checksumSha256 !== undefined) {
      writer.uint32(138).string(message.checksumSha256);
    }
    if (message.thinkingPattern !== undefined) {
      ThinkingTagPattern.encode(message.thinkingPattern, writer.uint32(146).fork()).join();
    }
    if (message.metadata !== undefined) {
      ModelInfoMetadata.encode(message.metadata, writer.uint32(154).fork()).join();
    }
    if (message.singleFile !== undefined) {
      SingleFileArtifact.encode(message.singleFile, writer.uint32(162).fork()).join();
    }
    if (message.archive !== undefined) {
      ArchiveArtifact.encode(message.archive, writer.uint32(170).fork()).join();
    }
    if (message.multiFile !== undefined) {
      MultiFileArtifact.encode(message.multiFile, writer.uint32(178).fork()).join();
    }
    if (message.customStrategyId !== undefined) {
      writer.uint32(186).string(message.customStrategyId);
    }
    if (message.builtIn !== undefined) {
      writer.uint32(192).bool(message.builtIn);
    }
    if (message.artifactType !== undefined) {
      writer.uint32(200).int32(message.artifactType);
    }
    if (message.expectedFiles !== undefined) {
      ExpectedModelFiles.encode(message.expectedFiles, writer.uint32(210).fork()).join();
    }
    if (message.accelerationPreference !== undefined) {
      writer.uint32(216).int32(message.accelerationPreference);
    }
    if (message.routingPolicy !== undefined) {
      writer.uint32(224).int32(message.routingPolicy);
    }
    if (message.compatibility !== undefined) {
      ModelRuntimeCompatibility.encode(message.compatibility, writer.uint32(234).fork()).join();
    }
    if (message.preferredFramework !== undefined) {
      writer.uint32(240).int32(message.preferredFramework);
    }
    if (message.registryStatus !== undefined) {
      writer.uint32(248).int32(message.registryStatus);
    }
    if (message.isDownloaded !== undefined) {
      writer.uint32(256).bool(message.isDownloaded);
    }
    if (message.isAvailable !== undefined) {
      writer.uint32(264).bool(message.isAvailable);
    }
    if (message.lastUsedAtUnixMs !== undefined) {
      writer.uint32(272).int64(message.lastUsedAtUnixMs);
    }
    if (message.usageCount !== undefined) {
      writer.uint32(280).int32(message.usageCount);
    }
    if (message.syncPending !== undefined) {
      writer.uint32(288).bool(message.syncPending);
    }
    if (message.statusMessage !== undefined) {
      writer.uint32(298).string(message.statusMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.localPath = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.downloadSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.contextLength = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.supportsThinking = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.supportsLora = reader.bool();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.createdAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.updatedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.memoryRequiredBytes = longToNumber(reader.int64());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.checksumSha256 = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.thinkingPattern = ThinkingTagPattern.decode(reader, reader.uint32());
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.metadata = ModelInfoMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.singleFile = SingleFileArtifact.decode(reader, reader.uint32());
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.archive = ArchiveArtifact.decode(reader, reader.uint32());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.multiFile = MultiFileArtifact.decode(reader, reader.uint32());
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.customStrategyId = reader.string();
          continue;
        }
        case 24: {
          if (tag !== 192) {
            break;
          }

          message.builtIn = reader.bool();
          continue;
        }
        case 25: {
          if (tag !== 200) {
            break;
          }

          message.artifactType = reader.int32() as any;
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.expectedFiles = ExpectedModelFiles.decode(reader, reader.uint32());
          continue;
        }
        case 27: {
          if (tag !== 216) {
            break;
          }

          message.accelerationPreference = reader.int32() as any;
          continue;
        }
        case 28: {
          if (tag !== 224) {
            break;
          }

          message.routingPolicy = reader.int32() as any;
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.compatibility = ModelRuntimeCompatibility.decode(reader, reader.uint32());
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.preferredFramework = reader.int32() as any;
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }

          message.registryStatus = reader.int32() as any;
          continue;
        }
        case 32: {
          if (tag !== 256) {
            break;
          }

          message.isDownloaded = reader.bool();
          continue;
        }
        case 33: {
          if (tag !== 264) {
            break;
          }

          message.isAvailable = reader.bool();
          continue;
        }
        case 34: {
          if (tag !== 272) {
            break;
          }

          message.lastUsedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 35: {
          if (tag !== 280) {
            break;
          }

          message.usageCount = reader.int32();
          continue;
        }
        case 36: {
          if (tag !== 288) {
            break;
          }

          message.syncPending = reader.bool();
          continue;
        }
        case 37: {
          if (tag !== 298) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : 0,
      format: isSet(object.format) ? modelFormatFromJSON(object.format) : 0,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : 0,
      downloadUrl: isSet(object.downloadUrl)
        ? globalThis.String(object.downloadUrl)
        : isSet(object.download_url)
        ? globalThis.String(object.download_url)
        : "",
      localPath: isSet(object.localPath)
        ? globalThis.String(object.localPath)
        : isSet(object.local_path)
        ? globalThis.String(object.local_path)
        : "",
      downloadSizeBytes: isSet(object.downloadSizeBytes)
        ? globalThis.Number(object.downloadSizeBytes)
        : isSet(object.download_size_bytes)
        ? globalThis.Number(object.download_size_bytes)
        : 0,
      contextLength: isSet(object.contextLength)
        ? globalThis.Number(object.contextLength)
        : isSet(object.context_length)
        ? globalThis.Number(object.context_length)
        : 0,
      supportsThinking: isSet(object.supportsThinking)
        ? globalThis.Boolean(object.supportsThinking)
        : isSet(object.supports_thinking)
        ? globalThis.Boolean(object.supports_thinking)
        : false,
      supportsLora: isSet(object.supportsLora)
        ? globalThis.Boolean(object.supportsLora)
        : isSet(object.supports_lora)
        ? globalThis.Boolean(object.supports_lora)
        : false,
      source: isSet(object.source) ? modelSourceFromJSON(object.source) : 0,
      createdAtUnixMs: isSet(object.createdAtUnixMs)
        ? globalThis.Number(object.createdAtUnixMs)
        : isSet(object.created_at_unix_ms)
        ? globalThis.Number(object.created_at_unix_ms)
        : 0,
      updatedAtUnixMs: isSet(object.updatedAtUnixMs)
        ? globalThis.Number(object.updatedAtUnixMs)
        : isSet(object.updated_at_unix_ms)
        ? globalThis.Number(object.updated_at_unix_ms)
        : 0,
      memoryRequiredBytes: isSet(object.memoryRequiredBytes)
        ? globalThis.Number(object.memoryRequiredBytes)
        : isSet(object.memory_required_bytes)
        ? globalThis.Number(object.memory_required_bytes)
        : undefined,
      checksumSha256: isSet(object.checksumSha256)
        ? globalThis.String(object.checksumSha256)
        : isSet(object.checksum_sha256)
        ? globalThis.String(object.checksum_sha256)
        : undefined,
      thinkingPattern: isSet(object.thinkingPattern)
        ? ThinkingTagPattern.fromJSON(object.thinkingPattern)
        : isSet(object.thinking_pattern)
        ? ThinkingTagPattern.fromJSON(object.thinking_pattern)
        : undefined,
      metadata: isSet(object.metadata) ? ModelInfoMetadata.fromJSON(object.metadata) : undefined,
      singleFile: isSet(object.singleFile)
        ? SingleFileArtifact.fromJSON(object.singleFile)
        : isSet(object.single_file)
        ? SingleFileArtifact.fromJSON(object.single_file)
        : undefined,
      archive: isSet(object.archive) ? ArchiveArtifact.fromJSON(object.archive) : undefined,
      multiFile: isSet(object.multiFile)
        ? MultiFileArtifact.fromJSON(object.multiFile)
        : isSet(object.multi_file)
        ? MultiFileArtifact.fromJSON(object.multi_file)
        : undefined,
      customStrategyId: isSet(object.customStrategyId)
        ? globalThis.String(object.customStrategyId)
        : isSet(object.custom_strategy_id)
        ? globalThis.String(object.custom_strategy_id)
        : undefined,
      builtIn: isSet(object.builtIn)
        ? globalThis.Boolean(object.builtIn)
        : isSet(object.built_in)
        ? globalThis.Boolean(object.built_in)
        : undefined,
      artifactType: isSet(object.artifactType)
        ? modelArtifactTypeFromJSON(object.artifactType)
        : isSet(object.artifact_type)
        ? modelArtifactTypeFromJSON(object.artifact_type)
        : undefined,
      expectedFiles: isSet(object.expectedFiles)
        ? ExpectedModelFiles.fromJSON(object.expectedFiles)
        : isSet(object.expected_files)
        ? ExpectedModelFiles.fromJSON(object.expected_files)
        : undefined,
      accelerationPreference: isSet(object.accelerationPreference)
        ? accelerationPreferenceFromJSON(object.accelerationPreference)
        : isSet(object.acceleration_preference)
        ? accelerationPreferenceFromJSON(object.acceleration_preference)
        : undefined,
      routingPolicy: isSet(object.routingPolicy)
        ? routingPolicyFromJSON(object.routingPolicy)
        : isSet(object.routing_policy)
        ? routingPolicyFromJSON(object.routing_policy)
        : undefined,
      compatibility: isSet(object.compatibility) ? ModelRuntimeCompatibility.fromJSON(object.compatibility) : undefined,
      preferredFramework: isSet(object.preferredFramework)
        ? inferenceFrameworkFromJSON(object.preferredFramework)
        : isSet(object.preferred_framework)
        ? inferenceFrameworkFromJSON(object.preferred_framework)
        : undefined,
      registryStatus: isSet(object.registryStatus)
        ? modelRegistryStatusFromJSON(object.registryStatus)
        : isSet(object.registry_status)
        ? modelRegistryStatusFromJSON(object.registry_status)
        : undefined,
      isDownloaded: isSet(object.isDownloaded)
        ? globalThis.Boolean(object.isDownloaded)
        : isSet(object.is_downloaded)
        ? globalThis.Boolean(object.is_downloaded)
        : undefined,
      isAvailable: isSet(object.isAvailable)
        ? globalThis.Boolean(object.isAvailable)
        : isSet(object.is_available)
        ? globalThis.Boolean(object.is_available)
        : undefined,
      lastUsedAtUnixMs: isSet(object.lastUsedAtUnixMs)
        ? globalThis.Number(object.lastUsedAtUnixMs)
        : isSet(object.last_used_at_unix_ms)
        ? globalThis.Number(object.last_used_at_unix_ms)
        : undefined,
      usageCount: isSet(object.usageCount)
        ? globalThis.Number(object.usageCount)
        : isSet(object.usage_count)
        ? globalThis.Number(object.usage_count)
        : undefined,
      syncPending: isSet(object.syncPending)
        ? globalThis.Boolean(object.syncPending)
        : isSet(object.sync_pending)
        ? globalThis.Boolean(object.sync_pending)
        : undefined,
      statusMessage: isSet(object.statusMessage)
        ? globalThis.String(object.statusMessage)
        : isSet(object.status_message)
        ? globalThis.String(object.status_message)
        : undefined,
    };
  },

  toJSON(message: ModelInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.category !== 0) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.format !== 0) {
      obj.format = modelFormatToJSON(message.format);
    }
    if (message.framework !== 0) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.downloadUrl !== "") {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.localPath !== "") {
      obj.localPath = message.localPath;
    }
    if (message.downloadSizeBytes !== 0) {
      obj.downloadSizeBytes = Math.round(message.downloadSizeBytes);
    }
    if (message.contextLength !== 0) {
      obj.contextLength = Math.round(message.contextLength);
    }
    if (message.supportsThinking !== false) {
      obj.supportsThinking = message.supportsThinking;
    }
    if (message.supportsLora !== false) {
      obj.supportsLora = message.supportsLora;
    }
    if (message.source !== 0) {
      obj.source = modelSourceToJSON(message.source);
    }
    if (message.createdAtUnixMs !== 0) {
      obj.createdAtUnixMs = Math.round(message.createdAtUnixMs);
    }
    if (message.updatedAtUnixMs !== 0) {
      obj.updatedAtUnixMs = Math.round(message.updatedAtUnixMs);
    }
    if (message.memoryRequiredBytes !== undefined) {
      obj.memoryRequiredBytes = Math.round(message.memoryRequiredBytes);
    }
    if (message.checksumSha256 !== undefined) {
      obj.checksumSha256 = message.checksumSha256;
    }
    if (message.thinkingPattern !== undefined) {
      obj.thinkingPattern = ThinkingTagPattern.toJSON(message.thinkingPattern);
    }
    if (message.metadata !== undefined) {
      obj.metadata = ModelInfoMetadata.toJSON(message.metadata);
    }
    if (message.singleFile !== undefined) {
      obj.singleFile = SingleFileArtifact.toJSON(message.singleFile);
    }
    if (message.archive !== undefined) {
      obj.archive = ArchiveArtifact.toJSON(message.archive);
    }
    if (message.multiFile !== undefined) {
      obj.multiFile = MultiFileArtifact.toJSON(message.multiFile);
    }
    if (message.customStrategyId !== undefined) {
      obj.customStrategyId = message.customStrategyId;
    }
    if (message.builtIn !== undefined) {
      obj.builtIn = message.builtIn;
    }
    if (message.artifactType !== undefined) {
      obj.artifactType = modelArtifactTypeToJSON(message.artifactType);
    }
    if (message.expectedFiles !== undefined) {
      obj.expectedFiles = ExpectedModelFiles.toJSON(message.expectedFiles);
    }
    if (message.accelerationPreference !== undefined) {
      obj.accelerationPreference = accelerationPreferenceToJSON(message.accelerationPreference);
    }
    if (message.routingPolicy !== undefined) {
      obj.routingPolicy = routingPolicyToJSON(message.routingPolicy);
    }
    if (message.compatibility !== undefined) {
      obj.compatibility = ModelRuntimeCompatibility.toJSON(message.compatibility);
    }
    if (message.preferredFramework !== undefined) {
      obj.preferredFramework = inferenceFrameworkToJSON(message.preferredFramework);
    }
    if (message.registryStatus !== undefined) {
      obj.registryStatus = modelRegistryStatusToJSON(message.registryStatus);
    }
    if (message.isDownloaded !== undefined) {
      obj.isDownloaded = message.isDownloaded;
    }
    if (message.isAvailable !== undefined) {
      obj.isAvailable = message.isAvailable;
    }
    if (message.lastUsedAtUnixMs !== undefined) {
      obj.lastUsedAtUnixMs = Math.round(message.lastUsedAtUnixMs);
    }
    if (message.usageCount !== undefined) {
      obj.usageCount = Math.round(message.usageCount);
    }
    if (message.syncPending !== undefined) {
      obj.syncPending = message.syncPending;
    }
    if (message.statusMessage !== undefined) {
      obj.statusMessage = message.statusMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelInfo>, I>>(base?: I): ModelInfo {
    return ModelInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelInfo>, I>>(object: I): ModelInfo {
    const message = createBaseModelInfo();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.category = object.category ?? 0;
    message.format = object.format ?? 0;
    message.framework = object.framework ?? 0;
    message.downloadUrl = object.downloadUrl ?? "";
    message.localPath = object.localPath ?? "";
    message.downloadSizeBytes = object.downloadSizeBytes ?? 0;
    message.contextLength = object.contextLength ?? 0;
    message.supportsThinking = object.supportsThinking ?? false;
    message.supportsLora = object.supportsLora ?? false;
    message.source = object.source ?? 0;
    message.createdAtUnixMs = object.createdAtUnixMs ?? 0;
    message.updatedAtUnixMs = object.updatedAtUnixMs ?? 0;
    message.memoryRequiredBytes = object.memoryRequiredBytes ?? undefined;
    message.checksumSha256 = object.checksumSha256 ?? undefined;
    message.thinkingPattern = (object.thinkingPattern !== undefined && object.thinkingPattern !== null)
      ? ThinkingTagPattern.fromPartial(object.thinkingPattern)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? ModelInfoMetadata.fromPartial(object.metadata)
      : undefined;
    message.singleFile = (object.singleFile !== undefined && object.singleFile !== null)
      ? SingleFileArtifact.fromPartial(object.singleFile)
      : undefined;
    message.archive = (object.archive !== undefined && object.archive !== null)
      ? ArchiveArtifact.fromPartial(object.archive)
      : undefined;
    message.multiFile = (object.multiFile !== undefined && object.multiFile !== null)
      ? MultiFileArtifact.fromPartial(object.multiFile)
      : undefined;
    message.customStrategyId = object.customStrategyId ?? undefined;
    message.builtIn = object.builtIn ?? undefined;
    message.artifactType = object.artifactType ?? undefined;
    message.expectedFiles = (object.expectedFiles !== undefined && object.expectedFiles !== null)
      ? ExpectedModelFiles.fromPartial(object.expectedFiles)
      : undefined;
    message.accelerationPreference = object.accelerationPreference ?? undefined;
    message.routingPolicy = object.routingPolicy ?? undefined;
    message.compatibility = (object.compatibility !== undefined && object.compatibility !== null)
      ? ModelRuntimeCompatibility.fromPartial(object.compatibility)
      : undefined;
    message.preferredFramework = object.preferredFramework ?? undefined;
    message.registryStatus = object.registryStatus ?? undefined;
    message.isDownloaded = object.isDownloaded ?? undefined;
    message.isAvailable = object.isAvailable ?? undefined;
    message.lastUsedAtUnixMs = object.lastUsedAtUnixMs ?? undefined;
    message.usageCount = object.usageCount ?? undefined;
    message.syncPending = object.syncPending ?? undefined;
    message.statusMessage = object.statusMessage ?? undefined;
    return message;
  },
};

function createBaseModelInfoList(): ModelInfoList {
  return { models: [] };
}

export const ModelInfoList: MessageFns<ModelInfoList> = {
  encode(message: ModelInfoList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.models) {
      ModelInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelInfoList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelInfoList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.models.push(ModelInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelInfoList {
    return {
      models: globalThis.Array.isArray(object?.models) ? object.models.map((e: any) => ModelInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ModelInfoList): unknown {
    const obj: any = {};
    if (message.models?.length) {
      obj.models = message.models.map((e) => ModelInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelInfoList>, I>>(base?: I): ModelInfoList {
    return ModelInfoList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelInfoList>, I>>(object: I): ModelInfoList {
    const message = createBaseModelInfoList();
    message.models = object.models?.map((e) => ModelInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSingleFileArtifact(): SingleFileArtifact {
  return { requiredPatterns: [], optionalPatterns: [], expectedFiles: undefined };
}

export const SingleFileArtifact: MessageFns<SingleFileArtifact> = {
  encode(message: SingleFileArtifact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requiredPatterns) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.optionalPatterns) {
      writer.uint32(18).string(v!);
    }
    if (message.expectedFiles !== undefined) {
      ExpectedModelFiles.encode(message.expectedFiles, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleFileArtifact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleFileArtifact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requiredPatterns.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.optionalPatterns.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expectedFiles = ExpectedModelFiles.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SingleFileArtifact {
    return {
      requiredPatterns: globalThis.Array.isArray(object?.requiredPatterns)
        ? object.requiredPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.required_patterns)
        ? object.required_patterns.map((e: any) => globalThis.String(e))
        : [],
      optionalPatterns: globalThis.Array.isArray(object?.optionalPatterns)
        ? object.optionalPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.optional_patterns)
        ? object.optional_patterns.map((e: any) => globalThis.String(e))
        : [],
      expectedFiles: isSet(object.expectedFiles)
        ? ExpectedModelFiles.fromJSON(object.expectedFiles)
        : isSet(object.expected_files)
        ? ExpectedModelFiles.fromJSON(object.expected_files)
        : undefined,
    };
  },

  toJSON(message: SingleFileArtifact): unknown {
    const obj: any = {};
    if (message.requiredPatterns?.length) {
      obj.requiredPatterns = message.requiredPatterns;
    }
    if (message.optionalPatterns?.length) {
      obj.optionalPatterns = message.optionalPatterns;
    }
    if (message.expectedFiles !== undefined) {
      obj.expectedFiles = ExpectedModelFiles.toJSON(message.expectedFiles);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SingleFileArtifact>, I>>(base?: I): SingleFileArtifact {
    return SingleFileArtifact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SingleFileArtifact>, I>>(object: I): SingleFileArtifact {
    const message = createBaseSingleFileArtifact();
    message.requiredPatterns = object.requiredPatterns?.map((e) => e) || [];
    message.optionalPatterns = object.optionalPatterns?.map((e) => e) || [];
    message.expectedFiles = (object.expectedFiles !== undefined && object.expectedFiles !== null)
      ? ExpectedModelFiles.fromPartial(object.expectedFiles)
      : undefined;
    return message;
  },
};

function createBaseArchiveArtifact(): ArchiveArtifact {
  return { type: 0, structure: 0, requiredPatterns: [], optionalPatterns: [], expectedFiles: undefined };
}

export const ArchiveArtifact: MessageFns<ArchiveArtifact> = {
  encode(message: ArchiveArtifact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.structure !== 0) {
      writer.uint32(16).int32(message.structure);
    }
    for (const v of message.requiredPatterns) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.optionalPatterns) {
      writer.uint32(34).string(v!);
    }
    if (message.expectedFiles !== undefined) {
      ExpectedModelFiles.encode(message.expectedFiles, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArchiveArtifact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArchiveArtifact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.structure = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requiredPatterns.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.optionalPatterns.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expectedFiles = ExpectedModelFiles.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArchiveArtifact {
    return {
      type: isSet(object.type) ? archiveTypeFromJSON(object.type) : 0,
      structure: isSet(object.structure) ? archiveStructureFromJSON(object.structure) : 0,
      requiredPatterns: globalThis.Array.isArray(object?.requiredPatterns)
        ? object.requiredPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.required_patterns)
        ? object.required_patterns.map((e: any) => globalThis.String(e))
        : [],
      optionalPatterns: globalThis.Array.isArray(object?.optionalPatterns)
        ? object.optionalPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.optional_patterns)
        ? object.optional_patterns.map((e: any) => globalThis.String(e))
        : [],
      expectedFiles: isSet(object.expectedFiles)
        ? ExpectedModelFiles.fromJSON(object.expectedFiles)
        : isSet(object.expected_files)
        ? ExpectedModelFiles.fromJSON(object.expected_files)
        : undefined,
    };
  },

  toJSON(message: ArchiveArtifact): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = archiveTypeToJSON(message.type);
    }
    if (message.structure !== 0) {
      obj.structure = archiveStructureToJSON(message.structure);
    }
    if (message.requiredPatterns?.length) {
      obj.requiredPatterns = message.requiredPatterns;
    }
    if (message.optionalPatterns?.length) {
      obj.optionalPatterns = message.optionalPatterns;
    }
    if (message.expectedFiles !== undefined) {
      obj.expectedFiles = ExpectedModelFiles.toJSON(message.expectedFiles);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArchiveArtifact>, I>>(base?: I): ArchiveArtifact {
    return ArchiveArtifact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArchiveArtifact>, I>>(object: I): ArchiveArtifact {
    const message = createBaseArchiveArtifact();
    message.type = object.type ?? 0;
    message.structure = object.structure ?? 0;
    message.requiredPatterns = object.requiredPatterns?.map((e) => e) || [];
    message.optionalPatterns = object.optionalPatterns?.map((e) => e) || [];
    message.expectedFiles = (object.expectedFiles !== undefined && object.expectedFiles !== null)
      ? ExpectedModelFiles.fromPartial(object.expectedFiles)
      : undefined;
    return message;
  },
};

function createBaseModelFileDescriptor(): ModelFileDescriptor {
  return {
    url: "",
    filename: "",
    isRequired: false,
    sizeBytes: undefined,
    relativePath: undefined,
    destinationPath: undefined,
    role: undefined,
    localPath: undefined,
    checksumSha256: undefined,
  };
}

export const ModelFileDescriptor: MessageFns<ModelFileDescriptor> = {
  encode(message: ModelFileDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.filename !== "") {
      writer.uint32(18).string(message.filename);
    }
    if (message.isRequired !== false) {
      writer.uint32(24).bool(message.isRequired);
    }
    if (message.sizeBytes !== undefined) {
      writer.uint32(32).int64(message.sizeBytes);
    }
    if (message.relativePath !== undefined) {
      writer.uint32(50).string(message.relativePath);
    }
    if (message.destinationPath !== undefined) {
      writer.uint32(58).string(message.destinationPath);
    }
    if (message.role !== undefined) {
      writer.uint32(64).int32(message.role);
    }
    if (message.localPath !== undefined) {
      writer.uint32(74).string(message.localPath);
    }
    if (message.checksumSha256 !== undefined) {
      writer.uint32(82).string(message.checksumSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelFileDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelFileDescriptor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isRequired = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.relativePath = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.destinationPath = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.localPath = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.checksumSha256 = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelFileDescriptor {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      isRequired: isSet(object.isRequired)
        ? globalThis.Boolean(object.isRequired)
        : isSet(object.is_required)
        ? globalThis.Boolean(object.is_required)
        : false,
      sizeBytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : undefined,
      relativePath: isSet(object.relativePath)
        ? globalThis.String(object.relativePath)
        : isSet(object.relative_path)
        ? globalThis.String(object.relative_path)
        : undefined,
      destinationPath: isSet(object.destinationPath)
        ? globalThis.String(object.destinationPath)
        : isSet(object.destination_path)
        ? globalThis.String(object.destination_path)
        : undefined,
      role: isSet(object.role) ? modelFileRoleFromJSON(object.role) : undefined,
      localPath: isSet(object.localPath)
        ? globalThis.String(object.localPath)
        : isSet(object.local_path)
        ? globalThis.String(object.local_path)
        : undefined,
      checksumSha256: isSet(object.checksumSha256)
        ? globalThis.String(object.checksumSha256)
        : isSet(object.checksum_sha256)
        ? globalThis.String(object.checksum_sha256)
        : undefined,
    };
  },

  toJSON(message: ModelFileDescriptor): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.isRequired !== false) {
      obj.isRequired = message.isRequired;
    }
    if (message.sizeBytes !== undefined) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    if (message.relativePath !== undefined) {
      obj.relativePath = message.relativePath;
    }
    if (message.destinationPath !== undefined) {
      obj.destinationPath = message.destinationPath;
    }
    if (message.role !== undefined) {
      obj.role = modelFileRoleToJSON(message.role);
    }
    if (message.localPath !== undefined) {
      obj.localPath = message.localPath;
    }
    if (message.checksumSha256 !== undefined) {
      obj.checksumSha256 = message.checksumSha256;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelFileDescriptor>, I>>(base?: I): ModelFileDescriptor {
    return ModelFileDescriptor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelFileDescriptor>, I>>(object: I): ModelFileDescriptor {
    const message = createBaseModelFileDescriptor();
    message.url = object.url ?? "";
    message.filename = object.filename ?? "";
    message.isRequired = object.isRequired ?? false;
    message.sizeBytes = object.sizeBytes ?? undefined;
    message.relativePath = object.relativePath ?? undefined;
    message.destinationPath = object.destinationPath ?? undefined;
    message.role = object.role ?? undefined;
    message.localPath = object.localPath ?? undefined;
    message.checksumSha256 = object.checksumSha256 ?? undefined;
    return message;
  },
};

function createBaseMultiFileArtifact(): MultiFileArtifact {
  return { files: [] };
}

export const MultiFileArtifact: MessageFns<MultiFileArtifact> = {
  encode(message: MultiFileArtifact, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      ModelFileDescriptor.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultiFileArtifact {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultiFileArtifact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultiFileArtifact {
    return {
      files: globalThis.Array.isArray(object?.files)
        ? object.files.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MultiFileArtifact): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => ModelFileDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultiFileArtifact>, I>>(base?: I): MultiFileArtifact {
    return MultiFileArtifact.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultiFileArtifact>, I>>(object: I): MultiFileArtifact {
    const message = createBaseMultiFileArtifact();
    message.files = object.files?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseExpectedModelFiles(): ExpectedModelFiles {
  return { files: [], rootDirectory: undefined, requiredPatterns: [], optionalPatterns: [], description: undefined };
}

export const ExpectedModelFiles: MessageFns<ExpectedModelFiles> = {
  encode(message: ExpectedModelFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      ModelFileDescriptor.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.rootDirectory !== undefined) {
      writer.uint32(18).string(message.rootDirectory);
    }
    for (const v of message.requiredPatterns) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.optionalPatterns) {
      writer.uint32(34).string(v!);
    }
    if (message.description !== undefined) {
      writer.uint32(42).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExpectedModelFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpectedModelFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootDirectory = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requiredPatterns.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.optionalPatterns.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExpectedModelFiles {
    return {
      files: globalThis.Array.isArray(object?.files)
        ? object.files.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
      rootDirectory: isSet(object.rootDirectory)
        ? globalThis.String(object.rootDirectory)
        : isSet(object.root_directory)
        ? globalThis.String(object.root_directory)
        : undefined,
      requiredPatterns: globalThis.Array.isArray(object?.requiredPatterns)
        ? object.requiredPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.required_patterns)
        ? object.required_patterns.map((e: any) => globalThis.String(e))
        : [],
      optionalPatterns: globalThis.Array.isArray(object?.optionalPatterns)
        ? object.optionalPatterns.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.optional_patterns)
        ? object.optional_patterns.map((e: any) => globalThis.String(e))
        : [],
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: ExpectedModelFiles): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => ModelFileDescriptor.toJSON(e));
    }
    if (message.rootDirectory !== undefined) {
      obj.rootDirectory = message.rootDirectory;
    }
    if (message.requiredPatterns?.length) {
      obj.requiredPatterns = message.requiredPatterns;
    }
    if (message.optionalPatterns?.length) {
      obj.optionalPatterns = message.optionalPatterns;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExpectedModelFiles>, I>>(base?: I): ExpectedModelFiles {
    return ExpectedModelFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExpectedModelFiles>, I>>(object: I): ExpectedModelFiles {
    const message = createBaseExpectedModelFiles();
    message.files = object.files?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    message.rootDirectory = object.rootDirectory ?? undefined;
    message.requiredPatterns = object.requiredPatterns?.map((e) => e) || [];
    message.optionalPatterns = object.optionalPatterns?.map((e) => e) || [];
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseModelQuery(): ModelQuery {
  return {
    framework: undefined,
    category: undefined,
    format: undefined,
    downloadedOnly: undefined,
    availableOnly: undefined,
    maxSizeBytes: undefined,
    searchQuery: "",
    source: undefined,
    sortField: undefined,
    sortOrder: undefined,
    registryStatus: undefined,
  };
}

export const ModelQuery: MessageFns<ModelQuery> = {
  encode(message: ModelQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.framework !== undefined) {
      writer.uint32(8).int32(message.framework);
    }
    if (message.category !== undefined) {
      writer.uint32(16).int32(message.category);
    }
    if (message.format !== undefined) {
      writer.uint32(24).int32(message.format);
    }
    if (message.downloadedOnly !== undefined) {
      writer.uint32(32).bool(message.downloadedOnly);
    }
    if (message.availableOnly !== undefined) {
      writer.uint32(40).bool(message.availableOnly);
    }
    if (message.maxSizeBytes !== undefined) {
      writer.uint32(48).int64(message.maxSizeBytes);
    }
    if (message.searchQuery !== "") {
      writer.uint32(58).string(message.searchQuery);
    }
    if (message.source !== undefined) {
      writer.uint32(64).int32(message.source);
    }
    if (message.sortField !== undefined) {
      writer.uint32(72).int32(message.sortField);
    }
    if (message.sortOrder !== undefined) {
      writer.uint32(80).int32(message.sortOrder);
    }
    if (message.registryStatus !== undefined) {
      writer.uint32(88).int32(message.registryStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.downloadedOnly = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.availableOnly = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.searchQuery = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sortField = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.sortOrder = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.registryStatus = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelQuery {
    return {
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      format: isSet(object.format) ? modelFormatFromJSON(object.format) : undefined,
      downloadedOnly: isSet(object.downloadedOnly)
        ? globalThis.Boolean(object.downloadedOnly)
        : isSet(object.downloaded_only)
        ? globalThis.Boolean(object.downloaded_only)
        : undefined,
      availableOnly: isSet(object.availableOnly)
        ? globalThis.Boolean(object.availableOnly)
        : isSet(object.available_only)
        ? globalThis.Boolean(object.available_only)
        : undefined,
      maxSizeBytes: isSet(object.maxSizeBytes)
        ? globalThis.Number(object.maxSizeBytes)
        : isSet(object.max_size_bytes)
        ? globalThis.Number(object.max_size_bytes)
        : undefined,
      searchQuery: isSet(object.searchQuery)
        ? globalThis.String(object.searchQuery)
        : isSet(object.search_query)
        ? globalThis.String(object.search_query)
        : "",
      source: isSet(object.source) ? modelSourceFromJSON(object.source) : undefined,
      sortField: isSet(object.sortField)
        ? modelQuerySortFieldFromJSON(object.sortField)
        : isSet(object.sort_field)
        ? modelQuerySortFieldFromJSON(object.sort_field)
        : undefined,
      sortOrder: isSet(object.sortOrder)
        ? modelQuerySortOrderFromJSON(object.sortOrder)
        : isSet(object.sort_order)
        ? modelQuerySortOrderFromJSON(object.sort_order)
        : undefined,
      registryStatus: isSet(object.registryStatus)
        ? modelRegistryStatusFromJSON(object.registryStatus)
        : isSet(object.registry_status)
        ? modelRegistryStatusFromJSON(object.registry_status)
        : undefined,
    };
  },

  toJSON(message: ModelQuery): unknown {
    const obj: any = {};
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.format !== undefined) {
      obj.format = modelFormatToJSON(message.format);
    }
    if (message.downloadedOnly !== undefined) {
      obj.downloadedOnly = message.downloadedOnly;
    }
    if (message.availableOnly !== undefined) {
      obj.availableOnly = message.availableOnly;
    }
    if (message.maxSizeBytes !== undefined) {
      obj.maxSizeBytes = Math.round(message.maxSizeBytes);
    }
    if (message.searchQuery !== "") {
      obj.searchQuery = message.searchQuery;
    }
    if (message.source !== undefined) {
      obj.source = modelSourceToJSON(message.source);
    }
    if (message.sortField !== undefined) {
      obj.sortField = modelQuerySortFieldToJSON(message.sortField);
    }
    if (message.sortOrder !== undefined) {
      obj.sortOrder = modelQuerySortOrderToJSON(message.sortOrder);
    }
    if (message.registryStatus !== undefined) {
      obj.registryStatus = modelRegistryStatusToJSON(message.registryStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelQuery>, I>>(base?: I): ModelQuery {
    return ModelQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelQuery>, I>>(object: I): ModelQuery {
    const message = createBaseModelQuery();
    message.framework = object.framework ?? undefined;
    message.category = object.category ?? undefined;
    message.format = object.format ?? undefined;
    message.downloadedOnly = object.downloadedOnly ?? undefined;
    message.availableOnly = object.availableOnly ?? undefined;
    message.maxSizeBytes = object.maxSizeBytes ?? undefined;
    message.searchQuery = object.searchQuery ?? "";
    message.source = object.source ?? undefined;
    message.sortField = object.sortField ?? undefined;
    message.sortOrder = object.sortOrder ?? undefined;
    message.registryStatus = object.registryStatus ?? undefined;
    return message;
  },
};

function createBaseModelRegistryRefreshRequest(): ModelRegistryRefreshRequest {
  return {
    includeRemoteCatalog: false,
    rescanLocal: false,
    pruneOrphans: false,
    query: undefined,
    catalogUri: "",
    forceRefresh: false,
    includeDownloadedState: false,
  };
}

export const ModelRegistryRefreshRequest: MessageFns<ModelRegistryRefreshRequest> = {
  encode(message: ModelRegistryRefreshRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.includeRemoteCatalog !== false) {
      writer.uint32(8).bool(message.includeRemoteCatalog);
    }
    if (message.rescanLocal !== false) {
      writer.uint32(16).bool(message.rescanLocal);
    }
    if (message.pruneOrphans !== false) {
      writer.uint32(24).bool(message.pruneOrphans);
    }
    if (message.query !== undefined) {
      ModelQuery.encode(message.query, writer.uint32(34).fork()).join();
    }
    if (message.catalogUri !== "") {
      writer.uint32(42).string(message.catalogUri);
    }
    if (message.forceRefresh !== false) {
      writer.uint32(48).bool(message.forceRefresh);
    }
    if (message.includeDownloadedState !== false) {
      writer.uint32(56).bool(message.includeDownloadedState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelRegistryRefreshRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelRegistryRefreshRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.includeRemoteCatalog = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rescanLocal = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pruneOrphans = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.query = ModelQuery.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.catalogUri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.forceRefresh = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.includeDownloadedState = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelRegistryRefreshRequest {
    return {
      includeRemoteCatalog: isSet(object.includeRemoteCatalog)
        ? globalThis.Boolean(object.includeRemoteCatalog)
        : isSet(object.include_remote_catalog)
        ? globalThis.Boolean(object.include_remote_catalog)
        : false,
      rescanLocal: isSet(object.rescanLocal)
        ? globalThis.Boolean(object.rescanLocal)
        : isSet(object.rescan_local)
        ? globalThis.Boolean(object.rescan_local)
        : false,
      pruneOrphans: isSet(object.pruneOrphans)
        ? globalThis.Boolean(object.pruneOrphans)
        : isSet(object.prune_orphans)
        ? globalThis.Boolean(object.prune_orphans)
        : false,
      query: isSet(object.query) ? ModelQuery.fromJSON(object.query) : undefined,
      catalogUri: isSet(object.catalogUri)
        ? globalThis.String(object.catalogUri)
        : isSet(object.catalog_uri)
        ? globalThis.String(object.catalog_uri)
        : "",
      forceRefresh: isSet(object.forceRefresh)
        ? globalThis.Boolean(object.forceRefresh)
        : isSet(object.force_refresh)
        ? globalThis.Boolean(object.force_refresh)
        : false,
      includeDownloadedState: isSet(object.includeDownloadedState)
        ? globalThis.Boolean(object.includeDownloadedState)
        : isSet(object.include_downloaded_state)
        ? globalThis.Boolean(object.include_downloaded_state)
        : false,
    };
  },

  toJSON(message: ModelRegistryRefreshRequest): unknown {
    const obj: any = {};
    if (message.includeRemoteCatalog !== false) {
      obj.includeRemoteCatalog = message.includeRemoteCatalog;
    }
    if (message.rescanLocal !== false) {
      obj.rescanLocal = message.rescanLocal;
    }
    if (message.pruneOrphans !== false) {
      obj.pruneOrphans = message.pruneOrphans;
    }
    if (message.query !== undefined) {
      obj.query = ModelQuery.toJSON(message.query);
    }
    if (message.catalogUri !== "") {
      obj.catalogUri = message.catalogUri;
    }
    if (message.forceRefresh !== false) {
      obj.forceRefresh = message.forceRefresh;
    }
    if (message.includeDownloadedState !== false) {
      obj.includeDownloadedState = message.includeDownloadedState;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelRegistryRefreshRequest>, I>>(base?: I): ModelRegistryRefreshRequest {
    return ModelRegistryRefreshRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelRegistryRefreshRequest>, I>>(object: I): ModelRegistryRefreshRequest {
    const message = createBaseModelRegistryRefreshRequest();
    message.includeRemoteCatalog = object.includeRemoteCatalog ?? false;
    message.rescanLocal = object.rescanLocal ?? false;
    message.pruneOrphans = object.pruneOrphans ?? false;
    message.query = (object.query !== undefined && object.query !== null)
      ? ModelQuery.fromPartial(object.query)
      : undefined;
    message.catalogUri = object.catalogUri ?? "";
    message.forceRefresh = object.forceRefresh ?? false;
    message.includeDownloadedState = object.includeDownloadedState ?? false;
    return message;
  },
};

function createBaseModelRegistryRefreshResult(): ModelRegistryRefreshResult {
  return {
    success: false,
    models: undefined,
    registeredCount: 0,
    updatedCount: 0,
    discoveredCount: 0,
    prunedCount: 0,
    refreshedAtUnixMs: 0,
    warnings: [],
    errorMessage: "",
    downloadedCount: 0,
    availableCount: 0,
    errorCount: 0,
  };
}

export const ModelRegistryRefreshResult: MessageFns<ModelRegistryRefreshResult> = {
  encode(message: ModelRegistryRefreshResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.models !== undefined) {
      ModelInfoList.encode(message.models, writer.uint32(18).fork()).join();
    }
    if (message.registeredCount !== 0) {
      writer.uint32(24).int32(message.registeredCount);
    }
    if (message.updatedCount !== 0) {
      writer.uint32(32).int32(message.updatedCount);
    }
    if (message.discoveredCount !== 0) {
      writer.uint32(40).int32(message.discoveredCount);
    }
    if (message.prunedCount !== 0) {
      writer.uint32(48).int32(message.prunedCount);
    }
    if (message.refreshedAtUnixMs !== 0) {
      writer.uint32(56).int64(message.refreshedAtUnixMs);
    }
    for (const v of message.warnings) {
      writer.uint32(66).string(v!);
    }
    if (message.errorMessage !== "") {
      writer.uint32(74).string(message.errorMessage);
    }
    if (message.downloadedCount !== 0) {
      writer.uint32(80).int32(message.downloadedCount);
    }
    if (message.availableCount !== 0) {
      writer.uint32(88).int32(message.availableCount);
    }
    if (message.errorCount !== 0) {
      writer.uint32(96).int32(message.errorCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelRegistryRefreshResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelRegistryRefreshResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.models = ModelInfoList.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.registeredCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.updatedCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.discoveredCount = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.prunedCount = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.refreshedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.downloadedCount = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.availableCount = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.errorCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelRegistryRefreshResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      models: isSet(object.models) ? ModelInfoList.fromJSON(object.models) : undefined,
      registeredCount: isSet(object.registeredCount)
        ? globalThis.Number(object.registeredCount)
        : isSet(object.registered_count)
        ? globalThis.Number(object.registered_count)
        : 0,
      updatedCount: isSet(object.updatedCount)
        ? globalThis.Number(object.updatedCount)
        : isSet(object.updated_count)
        ? globalThis.Number(object.updated_count)
        : 0,
      discoveredCount: isSet(object.discoveredCount)
        ? globalThis.Number(object.discoveredCount)
        : isSet(object.discovered_count)
        ? globalThis.Number(object.discovered_count)
        : 0,
      prunedCount: isSet(object.prunedCount)
        ? globalThis.Number(object.prunedCount)
        : isSet(object.pruned_count)
        ? globalThis.Number(object.pruned_count)
        : 0,
      refreshedAtUnixMs: isSet(object.refreshedAtUnixMs)
        ? globalThis.Number(object.refreshedAtUnixMs)
        : isSet(object.refreshed_at_unix_ms)
        ? globalThis.Number(object.refreshed_at_unix_ms)
        : 0,
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => globalThis.String(e))
        : [],
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      downloadedCount: isSet(object.downloadedCount)
        ? globalThis.Number(object.downloadedCount)
        : isSet(object.downloaded_count)
        ? globalThis.Number(object.downloaded_count)
        : 0,
      availableCount: isSet(object.availableCount)
        ? globalThis.Number(object.availableCount)
        : isSet(object.available_count)
        ? globalThis.Number(object.available_count)
        : 0,
      errorCount: isSet(object.errorCount)
        ? globalThis.Number(object.errorCount)
        : isSet(object.error_count)
        ? globalThis.Number(object.error_count)
        : 0,
    };
  },

  toJSON(message: ModelRegistryRefreshResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.models !== undefined) {
      obj.models = ModelInfoList.toJSON(message.models);
    }
    if (message.registeredCount !== 0) {
      obj.registeredCount = Math.round(message.registeredCount);
    }
    if (message.updatedCount !== 0) {
      obj.updatedCount = Math.round(message.updatedCount);
    }
    if (message.discoveredCount !== 0) {
      obj.discoveredCount = Math.round(message.discoveredCount);
    }
    if (message.prunedCount !== 0) {
      obj.prunedCount = Math.round(message.prunedCount);
    }
    if (message.refreshedAtUnixMs !== 0) {
      obj.refreshedAtUnixMs = Math.round(message.refreshedAtUnixMs);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.downloadedCount !== 0) {
      obj.downloadedCount = Math.round(message.downloadedCount);
    }
    if (message.availableCount !== 0) {
      obj.availableCount = Math.round(message.availableCount);
    }
    if (message.errorCount !== 0) {
      obj.errorCount = Math.round(message.errorCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelRegistryRefreshResult>, I>>(base?: I): ModelRegistryRefreshResult {
    return ModelRegistryRefreshResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelRegistryRefreshResult>, I>>(object: I): ModelRegistryRefreshResult {
    const message = createBaseModelRegistryRefreshResult();
    message.success = object.success ?? false;
    message.models = (object.models !== undefined && object.models !== null)
      ? ModelInfoList.fromPartial(object.models)
      : undefined;
    message.registeredCount = object.registeredCount ?? 0;
    message.updatedCount = object.updatedCount ?? 0;
    message.discoveredCount = object.discoveredCount ?? 0;
    message.prunedCount = object.prunedCount ?? 0;
    message.refreshedAtUnixMs = object.refreshedAtUnixMs ?? 0;
    message.warnings = object.warnings?.map((e) => e) || [];
    message.errorMessage = object.errorMessage ?? "";
    message.downloadedCount = object.downloadedCount ?? 0;
    message.availableCount = object.availableCount ?? 0;
    message.errorCount = object.errorCount ?? 0;
    return message;
  },
};

function createBaseModelListRequest(): ModelListRequest {
  return { query: undefined, includeCounts: false };
}

export const ModelListRequest: MessageFns<ModelListRequest> = {
  encode(message: ModelListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      ModelQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.includeCounts !== false) {
      writer.uint32(16).bool(message.includeCounts);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = ModelQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.includeCounts = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelListRequest {
    return {
      query: isSet(object.query) ? ModelQuery.fromJSON(object.query) : undefined,
      includeCounts: isSet(object.includeCounts)
        ? globalThis.Boolean(object.includeCounts)
        : isSet(object.include_counts)
        ? globalThis.Boolean(object.include_counts)
        : false,
    };
  },

  toJSON(message: ModelListRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = ModelQuery.toJSON(message.query);
    }
    if (message.includeCounts !== false) {
      obj.includeCounts = message.includeCounts;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelListRequest>, I>>(base?: I): ModelListRequest {
    return ModelListRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelListRequest>, I>>(object: I): ModelListRequest {
    const message = createBaseModelListRequest();
    message.query = (object.query !== undefined && object.query !== null)
      ? ModelQuery.fromPartial(object.query)
      : undefined;
    message.includeCounts = object.includeCounts ?? false;
    return message;
  },
};

function createBaseModelListResult(): ModelListResult {
  return {
    success: false,
    models: undefined,
    errorMessage: "",
    totalCount: 0,
    downloadedCount: 0,
    availableCount: 0,
    filteredCount: 0,
  };
}

export const ModelListResult: MessageFns<ModelListResult> = {
  encode(message: ModelListResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.models !== undefined) {
      ModelInfoList.encode(message.models, writer.uint32(18).fork()).join();
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    if (message.totalCount !== 0) {
      writer.uint32(32).int32(message.totalCount);
    }
    if (message.downloadedCount !== 0) {
      writer.uint32(40).int32(message.downloadedCount);
    }
    if (message.availableCount !== 0) {
      writer.uint32(48).int32(message.availableCount);
    }
    if (message.filteredCount !== 0) {
      writer.uint32(56).int32(message.filteredCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelListResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelListResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.models = ModelInfoList.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.downloadedCount = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.availableCount = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.filteredCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelListResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      models: isSet(object.models) ? ModelInfoList.fromJSON(object.models) : undefined,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      totalCount: isSet(object.totalCount)
        ? globalThis.Number(object.totalCount)
        : isSet(object.total_count)
        ? globalThis.Number(object.total_count)
        : 0,
      downloadedCount: isSet(object.downloadedCount)
        ? globalThis.Number(object.downloadedCount)
        : isSet(object.downloaded_count)
        ? globalThis.Number(object.downloaded_count)
        : 0,
      availableCount: isSet(object.availableCount)
        ? globalThis.Number(object.availableCount)
        : isSet(object.available_count)
        ? globalThis.Number(object.available_count)
        : 0,
      filteredCount: isSet(object.filteredCount)
        ? globalThis.Number(object.filteredCount)
        : isSet(object.filtered_count)
        ? globalThis.Number(object.filtered_count)
        : 0,
    };
  },

  toJSON(message: ModelListResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.models !== undefined) {
      obj.models = ModelInfoList.toJSON(message.models);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.totalCount !== 0) {
      obj.totalCount = Math.round(message.totalCount);
    }
    if (message.downloadedCount !== 0) {
      obj.downloadedCount = Math.round(message.downloadedCount);
    }
    if (message.availableCount !== 0) {
      obj.availableCount = Math.round(message.availableCount);
    }
    if (message.filteredCount !== 0) {
      obj.filteredCount = Math.round(message.filteredCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelListResult>, I>>(base?: I): ModelListResult {
    return ModelListResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelListResult>, I>>(object: I): ModelListResult {
    const message = createBaseModelListResult();
    message.success = object.success ?? false;
    message.models = (object.models !== undefined && object.models !== null)
      ? ModelInfoList.fromPartial(object.models)
      : undefined;
    message.errorMessage = object.errorMessage ?? "";
    message.totalCount = object.totalCount ?? 0;
    message.downloadedCount = object.downloadedCount ?? 0;
    message.availableCount = object.availableCount ?? 0;
    message.filteredCount = object.filteredCount ?? 0;
    return message;
  },
};

function createBaseModelGetRequest(): ModelGetRequest {
  return { modelId: "" };
}

export const ModelGetRequest: MessageFns<ModelGetRequest> = {
  encode(message: ModelGetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelGetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelGetRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
    };
  },

  toJSON(message: ModelGetRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelGetRequest>, I>>(base?: I): ModelGetRequest {
    return ModelGetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelGetRequest>, I>>(object: I): ModelGetRequest {
    const message = createBaseModelGetRequest();
    message.modelId = object.modelId ?? "";
    return message;
  },
};

function createBaseModelGetResult(): ModelGetResult {
  return { found: false, model: undefined, errorMessage: "" };
}

export const ModelGetResult: MessageFns<ModelGetResult> = {
  encode(message: ModelGetResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    if (message.model !== undefined) {
      ModelInfo.encode(message.model, writer.uint32(18).fork()).join();
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelGetResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelGetResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = ModelInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelGetResult {
    return {
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      model: isSet(object.model) ? ModelInfo.fromJSON(object.model) : undefined,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ModelGetResult): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.model !== undefined) {
      obj.model = ModelInfo.toJSON(message.model);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelGetResult>, I>>(base?: I): ModelGetResult {
    return ModelGetResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelGetResult>, I>>(object: I): ModelGetResult {
    const message = createBaseModelGetResult();
    message.found = object.found ?? false;
    message.model = (object.model !== undefined && object.model !== null)
      ? ModelInfo.fromPartial(object.model)
      : undefined;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseModelImportRequest(): ModelImportRequest {
  return {
    model: undefined,
    sourcePath: "",
    copyIntoManagedStorage: false,
    overwriteExisting: false,
    files: [],
    validateBeforeRegister: false,
  };
}

export const ModelImportRequest: MessageFns<ModelImportRequest> = {
  encode(message: ModelImportRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.model !== undefined) {
      ModelInfo.encode(message.model, writer.uint32(10).fork()).join();
    }
    if (message.sourcePath !== "") {
      writer.uint32(18).string(message.sourcePath);
    }
    if (message.copyIntoManagedStorage !== false) {
      writer.uint32(24).bool(message.copyIntoManagedStorage);
    }
    if (message.overwriteExisting !== false) {
      writer.uint32(32).bool(message.overwriteExisting);
    }
    for (const v of message.files) {
      ModelFileDescriptor.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.validateBeforeRegister !== false) {
      writer.uint32(48).bool(message.validateBeforeRegister);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelImportRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelImportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.model = ModelInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sourcePath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.copyIntoManagedStorage = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.overwriteExisting = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.files.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.validateBeforeRegister = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelImportRequest {
    return {
      model: isSet(object.model) ? ModelInfo.fromJSON(object.model) : undefined,
      sourcePath: isSet(object.sourcePath)
        ? globalThis.String(object.sourcePath)
        : isSet(object.source_path)
        ? globalThis.String(object.source_path)
        : "",
      copyIntoManagedStorage: isSet(object.copyIntoManagedStorage)
        ? globalThis.Boolean(object.copyIntoManagedStorage)
        : isSet(object.copy_into_managed_storage)
        ? globalThis.Boolean(object.copy_into_managed_storage)
        : false,
      overwriteExisting: isSet(object.overwriteExisting)
        ? globalThis.Boolean(object.overwriteExisting)
        : isSet(object.overwrite_existing)
        ? globalThis.Boolean(object.overwrite_existing)
        : false,
      files: globalThis.Array.isArray(object?.files)
        ? object.files.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
      validateBeforeRegister: isSet(object.validateBeforeRegister)
        ? globalThis.Boolean(object.validateBeforeRegister)
        : isSet(object.validate_before_register)
        ? globalThis.Boolean(object.validate_before_register)
        : false,
    };
  },

  toJSON(message: ModelImportRequest): unknown {
    const obj: any = {};
    if (message.model !== undefined) {
      obj.model = ModelInfo.toJSON(message.model);
    }
    if (message.sourcePath !== "") {
      obj.sourcePath = message.sourcePath;
    }
    if (message.copyIntoManagedStorage !== false) {
      obj.copyIntoManagedStorage = message.copyIntoManagedStorage;
    }
    if (message.overwriteExisting !== false) {
      obj.overwriteExisting = message.overwriteExisting;
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => ModelFileDescriptor.toJSON(e));
    }
    if (message.validateBeforeRegister !== false) {
      obj.validateBeforeRegister = message.validateBeforeRegister;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelImportRequest>, I>>(base?: I): ModelImportRequest {
    return ModelImportRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelImportRequest>, I>>(object: I): ModelImportRequest {
    const message = createBaseModelImportRequest();
    message.model = (object.model !== undefined && object.model !== null)
      ? ModelInfo.fromPartial(object.model)
      : undefined;
    message.sourcePath = object.sourcePath ?? "";
    message.copyIntoManagedStorage = object.copyIntoManagedStorage ?? false;
    message.overwriteExisting = object.overwriteExisting ?? false;
    message.files = object.files?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    message.validateBeforeRegister = object.validateBeforeRegister ?? false;
    return message;
  },
};

function createBaseModelImportResult(): ModelImportResult {
  return {
    success: false,
    model: undefined,
    localPath: "",
    importedBytes: 0,
    warnings: [],
    errorMessage: "",
    registered: false,
    copiedIntoManagedStorage: false,
  };
}

export const ModelImportResult: MessageFns<ModelImportResult> = {
  encode(message: ModelImportResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.model !== undefined) {
      ModelInfo.encode(message.model, writer.uint32(18).fork()).join();
    }
    if (message.localPath !== "") {
      writer.uint32(26).string(message.localPath);
    }
    if (message.importedBytes !== 0) {
      writer.uint32(32).int64(message.importedBytes);
    }
    for (const v of message.warnings) {
      writer.uint32(42).string(v!);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    if (message.registered !== false) {
      writer.uint32(56).bool(message.registered);
    }
    if (message.copiedIntoManagedStorage !== false) {
      writer.uint32(64).bool(message.copiedIntoManagedStorage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelImportResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelImportResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.model = ModelInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.localPath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.importedBytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.registered = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.copiedIntoManagedStorage = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelImportResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      model: isSet(object.model) ? ModelInfo.fromJSON(object.model) : undefined,
      localPath: isSet(object.localPath)
        ? globalThis.String(object.localPath)
        : isSet(object.local_path)
        ? globalThis.String(object.local_path)
        : "",
      importedBytes: isSet(object.importedBytes)
        ? globalThis.Number(object.importedBytes)
        : isSet(object.imported_bytes)
        ? globalThis.Number(object.imported_bytes)
        : 0,
      warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e: any) => globalThis.String(e)) : [],
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      registered: isSet(object.registered) ? globalThis.Boolean(object.registered) : false,
      copiedIntoManagedStorage: isSet(object.copiedIntoManagedStorage)
        ? globalThis.Boolean(object.copiedIntoManagedStorage)
        : isSet(object.copied_into_managed_storage)
        ? globalThis.Boolean(object.copied_into_managed_storage)
        : false,
    };
  },

  toJSON(message: ModelImportResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.model !== undefined) {
      obj.model = ModelInfo.toJSON(message.model);
    }
    if (message.localPath !== "") {
      obj.localPath = message.localPath;
    }
    if (message.importedBytes !== 0) {
      obj.importedBytes = Math.round(message.importedBytes);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.registered !== false) {
      obj.registered = message.registered;
    }
    if (message.copiedIntoManagedStorage !== false) {
      obj.copiedIntoManagedStorage = message.copiedIntoManagedStorage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelImportResult>, I>>(base?: I): ModelImportResult {
    return ModelImportResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelImportResult>, I>>(object: I): ModelImportResult {
    const message = createBaseModelImportResult();
    message.success = object.success ?? false;
    message.model = (object.model !== undefined && object.model !== null)
      ? ModelInfo.fromPartial(object.model)
      : undefined;
    message.localPath = object.localPath ?? "";
    message.importedBytes = object.importedBytes ?? 0;
    message.warnings = object.warnings?.map((e) => e) || [];
    message.errorMessage = object.errorMessage ?? "";
    message.registered = object.registered ?? false;
    message.copiedIntoManagedStorage = object.copiedIntoManagedStorage ?? false;
    return message;
  },
};

function createBaseModelDiscoveryRequest(): ModelDiscoveryRequest {
  return {
    searchRoots: [],
    recursive: false,
    linkDownloaded: false,
    purgeInvalid: false,
    query: undefined,
    includeBuiltIn: false,
    includeUserImports: false,
  };
}

export const ModelDiscoveryRequest: MessageFns<ModelDiscoveryRequest> = {
  encode(message: ModelDiscoveryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.searchRoots) {
      writer.uint32(10).string(v!);
    }
    if (message.recursive !== false) {
      writer.uint32(16).bool(message.recursive);
    }
    if (message.linkDownloaded !== false) {
      writer.uint32(24).bool(message.linkDownloaded);
    }
    if (message.purgeInvalid !== false) {
      writer.uint32(32).bool(message.purgeInvalid);
    }
    if (message.query !== undefined) {
      ModelQuery.encode(message.query, writer.uint32(42).fork()).join();
    }
    if (message.includeBuiltIn !== false) {
      writer.uint32(48).bool(message.includeBuiltIn);
    }
    if (message.includeUserImports !== false) {
      writer.uint32(56).bool(message.includeUserImports);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelDiscoveryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelDiscoveryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.searchRoots.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.linkDownloaded = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.purgeInvalid = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.query = ModelQuery.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.includeBuiltIn = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.includeUserImports = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelDiscoveryRequest {
    return {
      searchRoots: globalThis.Array.isArray(object?.searchRoots)
        ? object.searchRoots.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.search_roots)
        ? object.search_roots.map((e: any) => globalThis.String(e))
        : [],
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
      linkDownloaded: isSet(object.linkDownloaded)
        ? globalThis.Boolean(object.linkDownloaded)
        : isSet(object.link_downloaded)
        ? globalThis.Boolean(object.link_downloaded)
        : false,
      purgeInvalid: isSet(object.purgeInvalid)
        ? globalThis.Boolean(object.purgeInvalid)
        : isSet(object.purge_invalid)
        ? globalThis.Boolean(object.purge_invalid)
        : false,
      query: isSet(object.query) ? ModelQuery.fromJSON(object.query) : undefined,
      includeBuiltIn: isSet(object.includeBuiltIn)
        ? globalThis.Boolean(object.includeBuiltIn)
        : isSet(object.include_built_in)
        ? globalThis.Boolean(object.include_built_in)
        : false,
      includeUserImports: isSet(object.includeUserImports)
        ? globalThis.Boolean(object.includeUserImports)
        : isSet(object.include_user_imports)
        ? globalThis.Boolean(object.include_user_imports)
        : false,
    };
  },

  toJSON(message: ModelDiscoveryRequest): unknown {
    const obj: any = {};
    if (message.searchRoots?.length) {
      obj.searchRoots = message.searchRoots;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    if (message.linkDownloaded !== false) {
      obj.linkDownloaded = message.linkDownloaded;
    }
    if (message.purgeInvalid !== false) {
      obj.purgeInvalid = message.purgeInvalid;
    }
    if (message.query !== undefined) {
      obj.query = ModelQuery.toJSON(message.query);
    }
    if (message.includeBuiltIn !== false) {
      obj.includeBuiltIn = message.includeBuiltIn;
    }
    if (message.includeUserImports !== false) {
      obj.includeUserImports = message.includeUserImports;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelDiscoveryRequest>, I>>(base?: I): ModelDiscoveryRequest {
    return ModelDiscoveryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelDiscoveryRequest>, I>>(object: I): ModelDiscoveryRequest {
    const message = createBaseModelDiscoveryRequest();
    message.searchRoots = object.searchRoots?.map((e) => e) || [];
    message.recursive = object.recursive ?? false;
    message.linkDownloaded = object.linkDownloaded ?? false;
    message.purgeInvalid = object.purgeInvalid ?? false;
    message.query = (object.query !== undefined && object.query !== null)
      ? ModelQuery.fromPartial(object.query)
      : undefined;
    message.includeBuiltIn = object.includeBuiltIn ?? false;
    message.includeUserImports = object.includeUserImports ?? false;
    return message;
  },
};

function createBaseDiscoveredModel(): DiscoveredModel {
  return { modelId: "", localPath: "", matchedRegistry: false, model: undefined, sizeBytes: 0, warnings: [] };
}

export const DiscoveredModel: MessageFns<DiscoveredModel> = {
  encode(message: DiscoveredModel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.localPath !== "") {
      writer.uint32(18).string(message.localPath);
    }
    if (message.matchedRegistry !== false) {
      writer.uint32(24).bool(message.matchedRegistry);
    }
    if (message.model !== undefined) {
      ModelInfo.encode(message.model, writer.uint32(34).fork()).join();
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(40).int64(message.sizeBytes);
    }
    for (const v of message.warnings) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscoveredModel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscoveredModel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.localPath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.matchedRegistry = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.model = ModelInfo.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscoveredModel {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      localPath: isSet(object.localPath)
        ? globalThis.String(object.localPath)
        : isSet(object.local_path)
        ? globalThis.String(object.local_path)
        : "",
      matchedRegistry: isSet(object.matchedRegistry)
        ? globalThis.Boolean(object.matchedRegistry)
        : isSet(object.matched_registry)
        ? globalThis.Boolean(object.matched_registry)
        : false,
      model: isSet(object.model) ? ModelInfo.fromJSON(object.model) : undefined,
      sizeBytes: isSet(object.sizeBytes)
        ? globalThis.Number(object.sizeBytes)
        : isSet(object.size_bytes)
        ? globalThis.Number(object.size_bytes)
        : 0,
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: DiscoveredModel): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.localPath !== "") {
      obj.localPath = message.localPath;
    }
    if (message.matchedRegistry !== false) {
      obj.matchedRegistry = message.matchedRegistry;
    }
    if (message.model !== undefined) {
      obj.model = ModelInfo.toJSON(message.model);
    }
    if (message.sizeBytes !== 0) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscoveredModel>, I>>(base?: I): DiscoveredModel {
    return DiscoveredModel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscoveredModel>, I>>(object: I): DiscoveredModel {
    const message = createBaseDiscoveredModel();
    message.modelId = object.modelId ?? "";
    message.localPath = object.localPath ?? "";
    message.matchedRegistry = object.matchedRegistry ?? false;
    message.model = (object.model !== undefined && object.model !== null)
      ? ModelInfo.fromPartial(object.model)
      : undefined;
    message.sizeBytes = object.sizeBytes ?? 0;
    message.warnings = object.warnings?.map((e) => e) || [];
    return message;
  },
};

function createBaseModelDiscoveryResult(): ModelDiscoveryResult {
  return {
    success: false,
    discoveredModels: [],
    linkedCount: 0,
    purgedCount: 0,
    warnings: [],
    errorMessage: "",
    scannedCount: 0,
    importedCount: 0,
  };
}

export const ModelDiscoveryResult: MessageFns<ModelDiscoveryResult> = {
  encode(message: ModelDiscoveryResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    for (const v of message.discoveredModels) {
      DiscoveredModel.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.linkedCount !== 0) {
      writer.uint32(24).int32(message.linkedCount);
    }
    if (message.purgedCount !== 0) {
      writer.uint32(32).int32(message.purgedCount);
    }
    for (const v of message.warnings) {
      writer.uint32(42).string(v!);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    if (message.scannedCount !== 0) {
      writer.uint32(56).int32(message.scannedCount);
    }
    if (message.importedCount !== 0) {
      writer.uint32(64).int32(message.importedCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelDiscoveryResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelDiscoveryResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.discoveredModels.push(DiscoveredModel.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.linkedCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.purgedCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.scannedCount = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.importedCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelDiscoveryResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      discoveredModels: globalThis.Array.isArray(object?.discoveredModels)
        ? object.discoveredModels.map((e: any) => DiscoveredModel.fromJSON(e))
        : globalThis.Array.isArray(object?.discovered_models)
        ? object.discovered_models.map((e: any) => DiscoveredModel.fromJSON(e))
        : [],
      linkedCount: isSet(object.linkedCount)
        ? globalThis.Number(object.linkedCount)
        : isSet(object.linked_count)
        ? globalThis.Number(object.linked_count)
        : 0,
      purgedCount: isSet(object.purgedCount)
        ? globalThis.Number(object.purgedCount)
        : isSet(object.purged_count)
        ? globalThis.Number(object.purged_count)
        : 0,
      warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e: any) => globalThis.String(e)) : [],
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      scannedCount: isSet(object.scannedCount)
        ? globalThis.Number(object.scannedCount)
        : isSet(object.scanned_count)
        ? globalThis.Number(object.scanned_count)
        : 0,
      importedCount: isSet(object.importedCount)
        ? globalThis.Number(object.importedCount)
        : isSet(object.imported_count)
        ? globalThis.Number(object.imported_count)
        : 0,
    };
  },

  toJSON(message: ModelDiscoveryResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.discoveredModels?.length) {
      obj.discoveredModels = message.discoveredModels.map((e) => DiscoveredModel.toJSON(e));
    }
    if (message.linkedCount !== 0) {
      obj.linkedCount = Math.round(message.linkedCount);
    }
    if (message.purgedCount !== 0) {
      obj.purgedCount = Math.round(message.purgedCount);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.scannedCount !== 0) {
      obj.scannedCount = Math.round(message.scannedCount);
    }
    if (message.importedCount !== 0) {
      obj.importedCount = Math.round(message.importedCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelDiscoveryResult>, I>>(base?: I): ModelDiscoveryResult {
    return ModelDiscoveryResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelDiscoveryResult>, I>>(object: I): ModelDiscoveryResult {
    const message = createBaseModelDiscoveryResult();
    message.success = object.success ?? false;
    message.discoveredModels = object.discoveredModels?.map((e) => DiscoveredModel.fromPartial(e)) || [];
    message.linkedCount = object.linkedCount ?? 0;
    message.purgedCount = object.purgedCount ?? 0;
    message.warnings = object.warnings?.map((e) => e) || [];
    message.errorMessage = object.errorMessage ?? "";
    message.scannedCount = object.scannedCount ?? 0;
    message.importedCount = object.importedCount ?? 0;
    return message;
  },
};

function createBaseModelLoadRequest(): ModelLoadRequest {
  return { modelId: "", category: undefined, framework: undefined, forceReload: false, validateAvailability: false };
}

export const ModelLoadRequest: MessageFns<ModelLoadRequest> = {
  encode(message: ModelLoadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.category !== undefined) {
      writer.uint32(16).int32(message.category);
    }
    if (message.framework !== undefined) {
      writer.uint32(24).int32(message.framework);
    }
    if (message.forceReload !== false) {
      writer.uint32(32).bool(message.forceReload);
    }
    if (message.validateAvailability !== false) {
      writer.uint32(40).bool(message.validateAvailability);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelLoadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelLoadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.forceReload = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.validateAvailability = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelLoadRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
      forceReload: isSet(object.forceReload)
        ? globalThis.Boolean(object.forceReload)
        : isSet(object.force_reload)
        ? globalThis.Boolean(object.force_reload)
        : false,
      validateAvailability: isSet(object.validateAvailability)
        ? globalThis.Boolean(object.validateAvailability)
        : isSet(object.validate_availability)
        ? globalThis.Boolean(object.validate_availability)
        : false,
    };
  },

  toJSON(message: ModelLoadRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.forceReload !== false) {
      obj.forceReload = message.forceReload;
    }
    if (message.validateAvailability !== false) {
      obj.validateAvailability = message.validateAvailability;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelLoadRequest>, I>>(base?: I): ModelLoadRequest {
    return ModelLoadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelLoadRequest>, I>>(object: I): ModelLoadRequest {
    const message = createBaseModelLoadRequest();
    message.modelId = object.modelId ?? "";
    message.category = object.category ?? undefined;
    message.framework = object.framework ?? undefined;
    message.forceReload = object.forceReload ?? false;
    message.validateAvailability = object.validateAvailability ?? false;
    return message;
  },
};

function createBaseModelLoadResult(): ModelLoadResult {
  return {
    success: false,
    modelId: "",
    category: 0,
    framework: 0,
    resolvedPath: "",
    loadedAtUnixMs: 0,
    errorMessage: "",
    warnings: [],
    alreadyLoaded: false,
    resolvedArtifacts: [],
  };
}

export const ModelLoadResult: MessageFns<ModelLoadResult> = {
  encode(message: ModelLoadResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.modelId !== "") {
      writer.uint32(18).string(message.modelId);
    }
    if (message.category !== 0) {
      writer.uint32(24).int32(message.category);
    }
    if (message.framework !== 0) {
      writer.uint32(32).int32(message.framework);
    }
    if (message.resolvedPath !== "") {
      writer.uint32(42).string(message.resolvedPath);
    }
    if (message.loadedAtUnixMs !== 0) {
      writer.uint32(48).int64(message.loadedAtUnixMs);
    }
    if (message.errorMessage !== "") {
      writer.uint32(58).string(message.errorMessage);
    }
    for (const v of message.warnings) {
      writer.uint32(66).string(v!);
    }
    if (message.alreadyLoaded !== false) {
      writer.uint32(72).bool(message.alreadyLoaded);
    }
    for (const v of message.resolvedArtifacts) {
      ModelFileDescriptor.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelLoadResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelLoadResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resolvedPath = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.loadedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.alreadyLoaded = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.resolvedArtifacts.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelLoadResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : 0,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : 0,
      resolvedPath: isSet(object.resolvedPath)
        ? globalThis.String(object.resolvedPath)
        : isSet(object.resolved_path)
        ? globalThis.String(object.resolved_path)
        : "",
      loadedAtUnixMs: isSet(object.loadedAtUnixMs)
        ? globalThis.Number(object.loadedAtUnixMs)
        : isSet(object.loaded_at_unix_ms)
        ? globalThis.Number(object.loaded_at_unix_ms)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => globalThis.String(e))
        : [],
      alreadyLoaded: isSet(object.alreadyLoaded)
        ? globalThis.Boolean(object.alreadyLoaded)
        : isSet(object.already_loaded)
        ? globalThis.Boolean(object.already_loaded)
        : false,
      resolvedArtifacts: globalThis.Array.isArray(object?.resolvedArtifacts)
        ? object.resolvedArtifacts.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : globalThis.Array.isArray(object?.resolved_artifacts)
        ? object.resolved_artifacts.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ModelLoadResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.category !== 0) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.framework !== 0) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.resolvedPath !== "") {
      obj.resolvedPath = message.resolvedPath;
    }
    if (message.loadedAtUnixMs !== 0) {
      obj.loadedAtUnixMs = Math.round(message.loadedAtUnixMs);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    if (message.alreadyLoaded !== false) {
      obj.alreadyLoaded = message.alreadyLoaded;
    }
    if (message.resolvedArtifacts?.length) {
      obj.resolvedArtifacts = message.resolvedArtifacts.map((e) => ModelFileDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelLoadResult>, I>>(base?: I): ModelLoadResult {
    return ModelLoadResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelLoadResult>, I>>(object: I): ModelLoadResult {
    const message = createBaseModelLoadResult();
    message.success = object.success ?? false;
    message.modelId = object.modelId ?? "";
    message.category = object.category ?? 0;
    message.framework = object.framework ?? 0;
    message.resolvedPath = object.resolvedPath ?? "";
    message.loadedAtUnixMs = object.loadedAtUnixMs ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    message.warnings = object.warnings?.map((e) => e) || [];
    message.alreadyLoaded = object.alreadyLoaded ?? false;
    message.resolvedArtifacts = object.resolvedArtifacts?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModelUnloadRequest(): ModelUnloadRequest {
  return { modelId: "", category: undefined, unloadAll: false, framework: undefined };
}

export const ModelUnloadRequest: MessageFns<ModelUnloadRequest> = {
  encode(message: ModelUnloadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.category !== undefined) {
      writer.uint32(16).int32(message.category);
    }
    if (message.unloadAll !== false) {
      writer.uint32(24).bool(message.unloadAll);
    }
    if (message.framework !== undefined) {
      writer.uint32(32).int32(message.framework);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelUnloadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelUnloadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.unloadAll = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelUnloadRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      unloadAll: isSet(object.unloadAll)
        ? globalThis.Boolean(object.unloadAll)
        : isSet(object.unload_all)
        ? globalThis.Boolean(object.unload_all)
        : false,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
    };
  },

  toJSON(message: ModelUnloadRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.unloadAll !== false) {
      obj.unloadAll = message.unloadAll;
    }
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelUnloadRequest>, I>>(base?: I): ModelUnloadRequest {
    return ModelUnloadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelUnloadRequest>, I>>(object: I): ModelUnloadRequest {
    const message = createBaseModelUnloadRequest();
    message.modelId = object.modelId ?? "";
    message.category = object.category ?? undefined;
    message.unloadAll = object.unloadAll ?? false;
    message.framework = object.framework ?? undefined;
    return message;
  },
};

function createBaseModelUnloadResult(): ModelUnloadResult {
  return { success: false, unloadedModelIds: [], errorMessage: "", unloadedAtUnixMs: 0, warnings: [] };
}

export const ModelUnloadResult: MessageFns<ModelUnloadResult> = {
  encode(message: ModelUnloadResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    for (const v of message.unloadedModelIds) {
      writer.uint32(18).string(v!);
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    if (message.unloadedAtUnixMs !== 0) {
      writer.uint32(32).int64(message.unloadedAtUnixMs);
    }
    for (const v of message.warnings) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelUnloadResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelUnloadResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.unloadedModelIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.unloadedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelUnloadResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      unloadedModelIds: globalThis.Array.isArray(object?.unloadedModelIds)
        ? object.unloadedModelIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.unloaded_model_ids)
        ? object.unloaded_model_ids.map((e: any) => globalThis.String(e))
        : [],
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      unloadedAtUnixMs: isSet(object.unloadedAtUnixMs)
        ? globalThis.Number(object.unloadedAtUnixMs)
        : isSet(object.unloaded_at_unix_ms)
        ? globalThis.Number(object.unloaded_at_unix_ms)
        : 0,
      warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ModelUnloadResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.unloadedModelIds?.length) {
      obj.unloadedModelIds = message.unloadedModelIds;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.unloadedAtUnixMs !== 0) {
      obj.unloadedAtUnixMs = Math.round(message.unloadedAtUnixMs);
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelUnloadResult>, I>>(base?: I): ModelUnloadResult {
    return ModelUnloadResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelUnloadResult>, I>>(object: I): ModelUnloadResult {
    const message = createBaseModelUnloadResult();
    message.success = object.success ?? false;
    message.unloadedModelIds = object.unloadedModelIds?.map((e) => e) || [];
    message.errorMessage = object.errorMessage ?? "";
    message.unloadedAtUnixMs = object.unloadedAtUnixMs ?? 0;
    message.warnings = object.warnings?.map((e) => e) || [];
    return message;
  },
};

function createBaseCurrentModelRequest(): CurrentModelRequest {
  return { category: undefined, framework: undefined, includeModelMetadata: false };
}

export const CurrentModelRequest: MessageFns<CurrentModelRequest> = {
  encode(message: CurrentModelRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.category !== undefined) {
      writer.uint32(8).int32(message.category);
    }
    if (message.framework !== undefined) {
      writer.uint32(16).int32(message.framework);
    }
    if (message.includeModelMetadata !== false) {
      writer.uint32(24).bool(message.includeModelMetadata);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CurrentModelRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCurrentModelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.includeModelMetadata = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CurrentModelRequest {
    return {
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
      includeModelMetadata: isSet(object.includeModelMetadata)
        ? globalThis.Boolean(object.includeModelMetadata)
        : isSet(object.include_model_metadata)
        ? globalThis.Boolean(object.include_model_metadata)
        : false,
    };
  },

  toJSON(message: CurrentModelRequest): unknown {
    const obj: any = {};
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.includeModelMetadata !== false) {
      obj.includeModelMetadata = message.includeModelMetadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CurrentModelRequest>, I>>(base?: I): CurrentModelRequest {
    return CurrentModelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CurrentModelRequest>, I>>(object: I): CurrentModelRequest {
    const message = createBaseCurrentModelRequest();
    message.category = object.category ?? undefined;
    message.framework = object.framework ?? undefined;
    message.includeModelMetadata = object.includeModelMetadata ?? false;
    return message;
  },
};

function createBaseCurrentModelResult(): CurrentModelResult {
  return {
    modelId: "",
    model: undefined,
    loadedAtUnixMs: 0,
    found: false,
    errorMessage: "",
    category: 0,
    framework: 0,
    resolvedPath: "",
    resolvedArtifacts: [],
  };
}

export const CurrentModelResult: MessageFns<CurrentModelResult> = {
  encode(message: CurrentModelResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(18).string(message.modelId);
    }
    if (message.model !== undefined) {
      ModelInfo.encode(message.model, writer.uint32(26).fork()).join();
    }
    if (message.loadedAtUnixMs !== 0) {
      writer.uint32(32).int64(message.loadedAtUnixMs);
    }
    if (message.found !== false) {
      writer.uint32(40).bool(message.found);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    if (message.category !== 0) {
      writer.uint32(56).int32(message.category);
    }
    if (message.framework !== 0) {
      writer.uint32(64).int32(message.framework);
    }
    if (message.resolvedPath !== "") {
      writer.uint32(74).string(message.resolvedPath);
    }
    for (const v of message.resolvedArtifacts) {
      ModelFileDescriptor.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CurrentModelResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCurrentModelResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.model = ModelInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.loadedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.resolvedPath = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.resolvedArtifacts.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CurrentModelResult {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      model: isSet(object.model) ? ModelInfo.fromJSON(object.model) : undefined,
      loadedAtUnixMs: isSet(object.loadedAtUnixMs)
        ? globalThis.Number(object.loadedAtUnixMs)
        : isSet(object.loaded_at_unix_ms)
        ? globalThis.Number(object.loaded_at_unix_ms)
        : 0,
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : 0,
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : 0,
      resolvedPath: isSet(object.resolvedPath)
        ? globalThis.String(object.resolvedPath)
        : isSet(object.resolved_path)
        ? globalThis.String(object.resolved_path)
        : "",
      resolvedArtifacts: globalThis.Array.isArray(object?.resolvedArtifacts)
        ? object.resolvedArtifacts.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : globalThis.Array.isArray(object?.resolved_artifacts)
        ? object.resolved_artifacts.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CurrentModelResult): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.model !== undefined) {
      obj.model = ModelInfo.toJSON(message.model);
    }
    if (message.loadedAtUnixMs !== 0) {
      obj.loadedAtUnixMs = Math.round(message.loadedAtUnixMs);
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.category !== 0) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.framework !== 0) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.resolvedPath !== "") {
      obj.resolvedPath = message.resolvedPath;
    }
    if (message.resolvedArtifacts?.length) {
      obj.resolvedArtifacts = message.resolvedArtifacts.map((e) => ModelFileDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CurrentModelResult>, I>>(base?: I): CurrentModelResult {
    return CurrentModelResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CurrentModelResult>, I>>(object: I): CurrentModelResult {
    const message = createBaseCurrentModelResult();
    message.modelId = object.modelId ?? "";
    message.model = (object.model !== undefined && object.model !== null)
      ? ModelInfo.fromPartial(object.model)
      : undefined;
    message.loadedAtUnixMs = object.loadedAtUnixMs ?? 0;
    message.found = object.found ?? false;
    message.errorMessage = object.errorMessage ?? "";
    message.category = object.category ?? 0;
    message.framework = object.framework ?? 0;
    message.resolvedPath = object.resolvedPath ?? "";
    message.resolvedArtifacts = object.resolvedArtifacts?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModelDeleteRequest(): ModelDeleteRequest {
  return { modelId: "", deleteFiles: false, unregister: false, unloadIfLoaded: false };
}

export const ModelDeleteRequest: MessageFns<ModelDeleteRequest> = {
  encode(message: ModelDeleteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.deleteFiles !== false) {
      writer.uint32(16).bool(message.deleteFiles);
    }
    if (message.unregister !== false) {
      writer.uint32(24).bool(message.unregister);
    }
    if (message.unloadIfLoaded !== false) {
      writer.uint32(32).bool(message.unloadIfLoaded);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelDeleteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelDeleteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deleteFiles = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.unregister = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.unloadIfLoaded = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelDeleteRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      deleteFiles: isSet(object.deleteFiles)
        ? globalThis.Boolean(object.deleteFiles)
        : isSet(object.delete_files)
        ? globalThis.Boolean(object.delete_files)
        : false,
      unregister: isSet(object.unregister) ? globalThis.Boolean(object.unregister) : false,
      unloadIfLoaded: isSet(object.unloadIfLoaded)
        ? globalThis.Boolean(object.unloadIfLoaded)
        : isSet(object.unload_if_loaded)
        ? globalThis.Boolean(object.unload_if_loaded)
        : false,
    };
  },

  toJSON(message: ModelDeleteRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.deleteFiles !== false) {
      obj.deleteFiles = message.deleteFiles;
    }
    if (message.unregister !== false) {
      obj.unregister = message.unregister;
    }
    if (message.unloadIfLoaded !== false) {
      obj.unloadIfLoaded = message.unloadIfLoaded;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelDeleteRequest>, I>>(base?: I): ModelDeleteRequest {
    return ModelDeleteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelDeleteRequest>, I>>(object: I): ModelDeleteRequest {
    const message = createBaseModelDeleteRequest();
    message.modelId = object.modelId ?? "";
    message.deleteFiles = object.deleteFiles ?? false;
    message.unregister = object.unregister ?? false;
    message.unloadIfLoaded = object.unloadIfLoaded ?? false;
    return message;
  },
};

function createBaseModelDeleteResult(): ModelDeleteResult {
  return {
    success: false,
    modelId: "",
    deletedBytes: 0,
    filesDeleted: false,
    registryUpdated: false,
    wasLoaded: false,
    errorMessage: "",
    warnings: [],
  };
}

export const ModelDeleteResult: MessageFns<ModelDeleteResult> = {
  encode(message: ModelDeleteResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.modelId !== "") {
      writer.uint32(18).string(message.modelId);
    }
    if (message.deletedBytes !== 0) {
      writer.uint32(24).int64(message.deletedBytes);
    }
    if (message.filesDeleted !== false) {
      writer.uint32(32).bool(message.filesDeleted);
    }
    if (message.registryUpdated !== false) {
      writer.uint32(40).bool(message.registryUpdated);
    }
    if (message.wasLoaded !== false) {
      writer.uint32(48).bool(message.wasLoaded);
    }
    if (message.errorMessage !== "") {
      writer.uint32(58).string(message.errorMessage);
    }
    for (const v of message.warnings) {
      writer.uint32(66).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelDeleteResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelDeleteResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deletedBytes = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.filesDeleted = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.registryUpdated = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.wasLoaded = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.warnings.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelDeleteResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      deletedBytes: isSet(object.deletedBytes)
        ? globalThis.Number(object.deletedBytes)
        : isSet(object.deleted_bytes)
        ? globalThis.Number(object.deleted_bytes)
        : 0,
      filesDeleted: isSet(object.filesDeleted)
        ? globalThis.Boolean(object.filesDeleted)
        : isSet(object.files_deleted)
        ? globalThis.Boolean(object.files_deleted)
        : false,
      registryUpdated: isSet(object.registryUpdated)
        ? globalThis.Boolean(object.registryUpdated)
        : isSet(object.registry_updated)
        ? globalThis.Boolean(object.registry_updated)
        : false,
      wasLoaded: isSet(object.wasLoaded)
        ? globalThis.Boolean(object.wasLoaded)
        : isSet(object.was_loaded)
        ? globalThis.Boolean(object.was_loaded)
        : false,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      warnings: globalThis.Array.isArray(object?.warnings)
        ? object.warnings.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ModelDeleteResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.deletedBytes !== 0) {
      obj.deletedBytes = Math.round(message.deletedBytes);
    }
    if (message.filesDeleted !== false) {
      obj.filesDeleted = message.filesDeleted;
    }
    if (message.registryUpdated !== false) {
      obj.registryUpdated = message.registryUpdated;
    }
    if (message.wasLoaded !== false) {
      obj.wasLoaded = message.wasLoaded;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.warnings?.length) {
      obj.warnings = message.warnings;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelDeleteResult>, I>>(base?: I): ModelDeleteResult {
    return ModelDeleteResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelDeleteResult>, I>>(object: I): ModelDeleteResult {
    const message = createBaseModelDeleteResult();
    message.success = object.success ?? false;
    message.modelId = object.modelId ?? "";
    message.deletedBytes = object.deletedBytes ?? 0;
    message.filesDeleted = object.filesDeleted ?? false;
    message.registryUpdated = object.registryUpdated ?? false;
    message.wasLoaded = object.wasLoaded ?? false;
    message.errorMessage = object.errorMessage ?? "";
    message.warnings = object.warnings?.map((e) => e) || [];
    return message;
  },
};

function createBaseModelCompatibilityRequest(): ModelCompatibilityRequest {
  return {
    modelId: "",
    hardwareProfile: undefined,
    availableRamBytes: 0,
    availableStorageBytes: 0,
    acceleratorPreference: undefined,
    preferredFramework: undefined,
  };
}

export const ModelCompatibilityRequest: MessageFns<ModelCompatibilityRequest> = {
  encode(message: ModelCompatibilityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.hardwareProfile !== undefined) {
      HardwareProfile.encode(message.hardwareProfile, writer.uint32(18).fork()).join();
    }
    if (message.availableRamBytes !== 0) {
      writer.uint32(24).int64(message.availableRamBytes);
    }
    if (message.availableStorageBytes !== 0) {
      writer.uint32(32).int64(message.availableStorageBytes);
    }
    if (message.acceleratorPreference !== undefined) {
      writer.uint32(40).int32(message.acceleratorPreference);
    }
    if (message.preferredFramework !== undefined) {
      writer.uint32(48).int32(message.preferredFramework);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelCompatibilityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelCompatibilityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hardwareProfile = HardwareProfile.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.availableRamBytes = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.availableStorageBytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.acceleratorPreference = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.preferredFramework = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelCompatibilityRequest {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      hardwareProfile: isSet(object.hardwareProfile)
        ? HardwareProfile.fromJSON(object.hardwareProfile)
        : isSet(object.hardware_profile)
        ? HardwareProfile.fromJSON(object.hardware_profile)
        : undefined,
      availableRamBytes: isSet(object.availableRamBytes)
        ? globalThis.Number(object.availableRamBytes)
        : isSet(object.available_ram_bytes)
        ? globalThis.Number(object.available_ram_bytes)
        : 0,
      availableStorageBytes: isSet(object.availableStorageBytes)
        ? globalThis.Number(object.availableStorageBytes)
        : isSet(object.available_storage_bytes)
        ? globalThis.Number(object.available_storage_bytes)
        : 0,
      acceleratorPreference: isSet(object.acceleratorPreference)
        ? accelerationPreferenceFromJSON(object.acceleratorPreference)
        : isSet(object.accelerator_preference)
        ? accelerationPreferenceFromJSON(object.accelerator_preference)
        : undefined,
      preferredFramework: isSet(object.preferredFramework)
        ? inferenceFrameworkFromJSON(object.preferredFramework)
        : isSet(object.preferred_framework)
        ? inferenceFrameworkFromJSON(object.preferred_framework)
        : undefined,
    };
  },

  toJSON(message: ModelCompatibilityRequest): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.hardwareProfile !== undefined) {
      obj.hardwareProfile = HardwareProfile.toJSON(message.hardwareProfile);
    }
    if (message.availableRamBytes !== 0) {
      obj.availableRamBytes = Math.round(message.availableRamBytes);
    }
    if (message.availableStorageBytes !== 0) {
      obj.availableStorageBytes = Math.round(message.availableStorageBytes);
    }
    if (message.acceleratorPreference !== undefined) {
      obj.acceleratorPreference = accelerationPreferenceToJSON(message.acceleratorPreference);
    }
    if (message.preferredFramework !== undefined) {
      obj.preferredFramework = inferenceFrameworkToJSON(message.preferredFramework);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelCompatibilityRequest>, I>>(base?: I): ModelCompatibilityRequest {
    return ModelCompatibilityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelCompatibilityRequest>, I>>(object: I): ModelCompatibilityRequest {
    const message = createBaseModelCompatibilityRequest();
    message.modelId = object.modelId ?? "";
    message.hardwareProfile = (object.hardwareProfile !== undefined && object.hardwareProfile !== null)
      ? HardwareProfile.fromPartial(object.hardwareProfile)
      : undefined;
    message.availableRamBytes = object.availableRamBytes ?? 0;
    message.availableStorageBytes = object.availableStorageBytes ?? 0;
    message.acceleratorPreference = object.acceleratorPreference ?? undefined;
    message.preferredFramework = object.preferredFramework ?? undefined;
    return message;
  },
};

function createBaseModelCompatibilityResult(): ModelCompatibilityResult {
  return {
    isCompatible: false,
    canRun: false,
    canFit: false,
    requiredMemoryBytes: 0,
    availableMemoryBytes: 0,
    requiredStorageBytes: 0,
    availableStorageBytes: 0,
    reasons: [],
    suggestedAlternatives: [],
    modelId: "",
    errorCode: 0,
    errorMessage: "",
  };
}

export const ModelCompatibilityResult: MessageFns<ModelCompatibilityResult> = {
  encode(message: ModelCompatibilityResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isCompatible !== false) {
      writer.uint32(8).bool(message.isCompatible);
    }
    if (message.canRun !== false) {
      writer.uint32(16).bool(message.canRun);
    }
    if (message.canFit !== false) {
      writer.uint32(24).bool(message.canFit);
    }
    if (message.requiredMemoryBytes !== 0) {
      writer.uint32(32).int64(message.requiredMemoryBytes);
    }
    if (message.availableMemoryBytes !== 0) {
      writer.uint32(40).int64(message.availableMemoryBytes);
    }
    if (message.requiredStorageBytes !== 0) {
      writer.uint32(48).int64(message.requiredStorageBytes);
    }
    if (message.availableStorageBytes !== 0) {
      writer.uint32(56).int64(message.availableStorageBytes);
    }
    for (const v of message.reasons) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.suggestedAlternatives) {
      writer.uint32(74).string(v!);
    }
    if (message.modelId !== "") {
      writer.uint32(82).string(message.modelId);
    }
    if (message.errorCode !== 0) {
      writer.uint32(88).int32(message.errorCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(98).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelCompatibilityResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelCompatibilityResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isCompatible = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.canRun = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.canFit = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requiredMemoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.availableMemoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.requiredStorageBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.availableStorageBytes = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reasons.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.suggestedAlternatives.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelCompatibilityResult {
    return {
      isCompatible: isSet(object.isCompatible)
        ? globalThis.Boolean(object.isCompatible)
        : isSet(object.is_compatible)
        ? globalThis.Boolean(object.is_compatible)
        : false,
      canRun: isSet(object.canRun)
        ? globalThis.Boolean(object.canRun)
        : isSet(object.can_run)
        ? globalThis.Boolean(object.can_run)
        : false,
      canFit: isSet(object.canFit)
        ? globalThis.Boolean(object.canFit)
        : isSet(object.can_fit)
        ? globalThis.Boolean(object.can_fit)
        : false,
      requiredMemoryBytes: isSet(object.requiredMemoryBytes)
        ? globalThis.Number(object.requiredMemoryBytes)
        : isSet(object.required_memory_bytes)
        ? globalThis.Number(object.required_memory_bytes)
        : 0,
      availableMemoryBytes: isSet(object.availableMemoryBytes)
        ? globalThis.Number(object.availableMemoryBytes)
        : isSet(object.available_memory_bytes)
        ? globalThis.Number(object.available_memory_bytes)
        : 0,
      requiredStorageBytes: isSet(object.requiredStorageBytes)
        ? globalThis.Number(object.requiredStorageBytes)
        : isSet(object.required_storage_bytes)
        ? globalThis.Number(object.required_storage_bytes)
        : 0,
      availableStorageBytes: isSet(object.availableStorageBytes)
        ? globalThis.Number(object.availableStorageBytes)
        : isSet(object.available_storage_bytes)
        ? globalThis.Number(object.available_storage_bytes)
        : 0,
      reasons: globalThis.Array.isArray(object?.reasons)
        ? object.reasons.map((e: any) => globalThis.String(e))
        : [],
      suggestedAlternatives: globalThis.Array.isArray(object?.suggestedAlternatives)
        ? object.suggestedAlternatives.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.suggested_alternatives)
        ? object.suggested_alternatives.map((e: any) => globalThis.String(e))
        : [],
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ModelCompatibilityResult): unknown {
    const obj: any = {};
    if (message.isCompatible !== false) {
      obj.isCompatible = message.isCompatible;
    }
    if (message.canRun !== false) {
      obj.canRun = message.canRun;
    }
    if (message.canFit !== false) {
      obj.canFit = message.canFit;
    }
    if (message.requiredMemoryBytes !== 0) {
      obj.requiredMemoryBytes = Math.round(message.requiredMemoryBytes);
    }
    if (message.availableMemoryBytes !== 0) {
      obj.availableMemoryBytes = Math.round(message.availableMemoryBytes);
    }
    if (message.requiredStorageBytes !== 0) {
      obj.requiredStorageBytes = Math.round(message.requiredStorageBytes);
    }
    if (message.availableStorageBytes !== 0) {
      obj.availableStorageBytes = Math.round(message.availableStorageBytes);
    }
    if (message.reasons?.length) {
      obj.reasons = message.reasons;
    }
    if (message.suggestedAlternatives?.length) {
      obj.suggestedAlternatives = message.suggestedAlternatives;
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelCompatibilityResult>, I>>(base?: I): ModelCompatibilityResult {
    return ModelCompatibilityResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelCompatibilityResult>, I>>(object: I): ModelCompatibilityResult {
    const message = createBaseModelCompatibilityResult();
    message.isCompatible = object.isCompatible ?? false;
    message.canRun = object.canRun ?? false;
    message.canFit = object.canFit ?? false;
    message.requiredMemoryBytes = object.requiredMemoryBytes ?? 0;
    message.availableMemoryBytes = object.availableMemoryBytes ?? 0;
    message.requiredStorageBytes = object.requiredStorageBytes ?? 0;
    message.availableStorageBytes = object.availableStorageBytes ?? 0;
    message.reasons = object.reasons?.map((e) => e) || [];
    message.suggestedAlternatives = object.suggestedAlternatives?.map((e) => e) || [];
    message.modelId = object.modelId ?? "";
    message.errorCode = object.errorCode ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseModelFormatFromUrlRequest(): ModelFormatFromUrlRequest {
  return { url: "" };
}

export const ModelFormatFromUrlRequest: MessageFns<ModelFormatFromUrlRequest> = {
  encode(message: ModelFormatFromUrlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelFormatFromUrlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelFormatFromUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelFormatFromUrlRequest {
    return { url: isSet(object.url) ? globalThis.String(object.url) : "" };
  },

  toJSON(message: ModelFormatFromUrlRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelFormatFromUrlRequest>, I>>(base?: I): ModelFormatFromUrlRequest {
    return ModelFormatFromUrlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelFormatFromUrlRequest>, I>>(object: I): ModelFormatFromUrlRequest {
    const message = createBaseModelFormatFromUrlRequest();
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseModelFormatFromUrlResult(): ModelFormatFromUrlResult {
  return { format: 0, innerFormat: 0 };
}

export const ModelFormatFromUrlResult: MessageFns<ModelFormatFromUrlResult> = {
  encode(message: ModelFormatFromUrlResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.format !== 0) {
      writer.uint32(8).int32(message.format);
    }
    if (message.innerFormat !== 0) {
      writer.uint32(16).int32(message.innerFormat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelFormatFromUrlResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelFormatFromUrlResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.innerFormat = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelFormatFromUrlResult {
    return {
      format: isSet(object.format) ? modelFormatFromJSON(object.format) : 0,
      innerFormat: isSet(object.innerFormat)
        ? modelFormatFromJSON(object.innerFormat)
        : isSet(object.inner_format)
        ? modelFormatFromJSON(object.inner_format)
        : 0,
    };
  },

  toJSON(message: ModelFormatFromUrlResult): unknown {
    const obj: any = {};
    if (message.format !== 0) {
      obj.format = modelFormatToJSON(message.format);
    }
    if (message.innerFormat !== 0) {
      obj.innerFormat = modelFormatToJSON(message.innerFormat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelFormatFromUrlResult>, I>>(base?: I): ModelFormatFromUrlResult {
    return ModelFormatFromUrlResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelFormatFromUrlResult>, I>>(object: I): ModelFormatFromUrlResult {
    const message = createBaseModelFormatFromUrlResult();
    message.format = object.format ?? 0;
    message.innerFormat = object.innerFormat ?? 0;
    return message;
  },
};

function createBaseArtifactInferFromUrlRequest(): ArtifactInferFromUrlRequest {
  return { url: "", modelId: "" };
}

export const ArtifactInferFromUrlRequest: MessageFns<ArtifactInferFromUrlRequest> = {
  encode(message: ArtifactInferFromUrlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.modelId !== "") {
      writer.uint32(18).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactInferFromUrlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactInferFromUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArtifactInferFromUrlRequest {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
    };
  },

  toJSON(message: ArtifactInferFromUrlRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArtifactInferFromUrlRequest>, I>>(base?: I): ArtifactInferFromUrlRequest {
    return ArtifactInferFromUrlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactInferFromUrlRequest>, I>>(object: I): ArtifactInferFromUrlRequest {
    const message = createBaseArtifactInferFromUrlRequest();
    message.url = object.url ?? "";
    message.modelId = object.modelId ?? "";
    return message;
  },
};

function createBaseArtifactInferFromUrlResult(): ArtifactInferFromUrlResult {
  return { artifactType: 0, archiveType: 0, archiveStructure: 0, primaryRelpath: "", innerFormat: 0 };
}

export const ArtifactInferFromUrlResult: MessageFns<ArtifactInferFromUrlResult> = {
  encode(message: ArtifactInferFromUrlResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.artifactType !== 0) {
      writer.uint32(8).int32(message.artifactType);
    }
    if (message.archiveType !== 0) {
      writer.uint32(16).int32(message.archiveType);
    }
    if (message.archiveStructure !== 0) {
      writer.uint32(24).int32(message.archiveStructure);
    }
    if (message.primaryRelpath !== "") {
      writer.uint32(34).string(message.primaryRelpath);
    }
    if (message.innerFormat !== 0) {
      writer.uint32(40).int32(message.innerFormat);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ArtifactInferFromUrlResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseArtifactInferFromUrlResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.artifactType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.archiveType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.archiveStructure = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.primaryRelpath = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.innerFormat = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ArtifactInferFromUrlResult {
    return {
      artifactType: isSet(object.artifactType)
        ? modelArtifactTypeFromJSON(object.artifactType)
        : isSet(object.artifact_type)
        ? modelArtifactTypeFromJSON(object.artifact_type)
        : 0,
      archiveType: isSet(object.archiveType)
        ? archiveTypeFromJSON(object.archiveType)
        : isSet(object.archive_type)
        ? archiveTypeFromJSON(object.archive_type)
        : 0,
      archiveStructure: isSet(object.archiveStructure)
        ? archiveStructureFromJSON(object.archiveStructure)
        : isSet(object.archive_structure)
        ? archiveStructureFromJSON(object.archive_structure)
        : 0,
      primaryRelpath: isSet(object.primaryRelpath)
        ? globalThis.String(object.primaryRelpath)
        : isSet(object.primary_relpath)
        ? globalThis.String(object.primary_relpath)
        : "",
      innerFormat: isSet(object.innerFormat)
        ? modelFormatFromJSON(object.innerFormat)
        : isSet(object.inner_format)
        ? modelFormatFromJSON(object.inner_format)
        : 0,
    };
  },

  toJSON(message: ArtifactInferFromUrlResult): unknown {
    const obj: any = {};
    if (message.artifactType !== 0) {
      obj.artifactType = modelArtifactTypeToJSON(message.artifactType);
    }
    if (message.archiveType !== 0) {
      obj.archiveType = archiveTypeToJSON(message.archiveType);
    }
    if (message.archiveStructure !== 0) {
      obj.archiveStructure = archiveStructureToJSON(message.archiveStructure);
    }
    if (message.primaryRelpath !== "") {
      obj.primaryRelpath = message.primaryRelpath;
    }
    if (message.innerFormat !== 0) {
      obj.innerFormat = modelFormatToJSON(message.innerFormat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ArtifactInferFromUrlResult>, I>>(base?: I): ArtifactInferFromUrlResult {
    return ArtifactInferFromUrlResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ArtifactInferFromUrlResult>, I>>(object: I): ArtifactInferFromUrlResult {
    const message = createBaseArtifactInferFromUrlResult();
    message.artifactType = object.artifactType ?? 0;
    message.archiveType = object.archiveType ?? 0;
    message.archiveStructure = object.archiveStructure ?? 0;
    message.primaryRelpath = object.primaryRelpath ?? "";
    message.innerFormat = object.innerFormat ?? 0;
    return message;
  },
};

function createBaseModelRegistryFetchAssignmentsRequest(): ModelRegistryFetchAssignmentsRequest {
  return { deviceId: "", environment: undefined, forceRefresh: false };
}

export const ModelRegistryFetchAssignmentsRequest: MessageFns<ModelRegistryFetchAssignmentsRequest> = {
  encode(message: ModelRegistryFetchAssignmentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    if (message.environment !== undefined) {
      writer.uint32(16).int32(message.environment);
    }
    if (message.forceRefresh !== false) {
      writer.uint32(24).bool(message.forceRefresh);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelRegistryFetchAssignmentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelRegistryFetchAssignmentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.environment = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.forceRefresh = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelRegistryFetchAssignmentsRequest {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
      environment: isSet(object.environment) ? sDKEnvironmentFromJSON(object.environment) : undefined,
      forceRefresh: isSet(object.forceRefresh)
        ? globalThis.Boolean(object.forceRefresh)
        : isSet(object.force_refresh)
        ? globalThis.Boolean(object.force_refresh)
        : false,
    };
  },

  toJSON(message: ModelRegistryFetchAssignmentsRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    if (message.environment !== undefined) {
      obj.environment = sDKEnvironmentToJSON(message.environment);
    }
    if (message.forceRefresh !== false) {
      obj.forceRefresh = message.forceRefresh;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelRegistryFetchAssignmentsRequest>, I>>(
    base?: I,
  ): ModelRegistryFetchAssignmentsRequest {
    return ModelRegistryFetchAssignmentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelRegistryFetchAssignmentsRequest>, I>>(
    object: I,
  ): ModelRegistryFetchAssignmentsRequest {
    const message = createBaseModelRegistryFetchAssignmentsRequest();
    message.deviceId = object.deviceId ?? "";
    message.environment = object.environment ?? undefined;
    message.forceRefresh = object.forceRefresh ?? false;
    return message;
  },
};

function createBaseModelRegistryFetchAssignmentsResult(): ModelRegistryFetchAssignmentsResult {
  return { success: false, models: undefined, modelCount: 0, fetchedAtUnixMs: 0, errorCode: 0, errorMessage: "" };
}

export const ModelRegistryFetchAssignmentsResult: MessageFns<ModelRegistryFetchAssignmentsResult> = {
  encode(message: ModelRegistryFetchAssignmentsResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.models !== undefined) {
      ModelInfoList.encode(message.models, writer.uint32(18).fork()).join();
    }
    if (message.modelCount !== 0) {
      writer.uint32(24).int32(message.modelCount);
    }
    if (message.fetchedAtUnixMs !== 0) {
      writer.uint32(32).int64(message.fetchedAtUnixMs);
    }
    if (message.errorCode !== 0) {
      writer.uint32(40).int32(message.errorCode);
    }
    if (message.errorMessage !== "") {
      writer.uint32(50).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelRegistryFetchAssignmentsResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelRegistryFetchAssignmentsResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.models = ModelInfoList.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.modelCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fetchedAtUnixMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelRegistryFetchAssignmentsResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      models: isSet(object.models) ? ModelInfoList.fromJSON(object.models) : undefined,
      modelCount: isSet(object.modelCount)
        ? globalThis.Number(object.modelCount)
        : isSet(object.model_count)
        ? globalThis.Number(object.model_count)
        : 0,
      fetchedAtUnixMs: isSet(object.fetchedAtUnixMs)
        ? globalThis.Number(object.fetchedAtUnixMs)
        : isSet(object.fetched_at_unix_ms)
        ? globalThis.Number(object.fetched_at_unix_ms)
        : 0,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
    };
  },

  toJSON(message: ModelRegistryFetchAssignmentsResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.models !== undefined) {
      obj.models = ModelInfoList.toJSON(message.models);
    }
    if (message.modelCount !== 0) {
      obj.modelCount = Math.round(message.modelCount);
    }
    if (message.fetchedAtUnixMs !== 0) {
      obj.fetchedAtUnixMs = Math.round(message.fetchedAtUnixMs);
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelRegistryFetchAssignmentsResult>, I>>(
    base?: I,
  ): ModelRegistryFetchAssignmentsResult {
    return ModelRegistryFetchAssignmentsResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelRegistryFetchAssignmentsResult>, I>>(
    object: I,
  ): ModelRegistryFetchAssignmentsResult {
    const message = createBaseModelRegistryFetchAssignmentsResult();
    message.success = object.success ?? false;
    message.models = (object.models !== undefined && object.models !== null)
      ? ModelInfoList.fromPartial(object.models)
      : undefined;
    message.modelCount = object.modelCount ?? 0;
    message.fetchedAtUnixMs = object.fetchedAtUnixMs ?? 0;
    message.errorCode = object.errorCode ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseModelInfoMakeRequest(): ModelInfoMakeRequest {
  return { url: "", name: "", framework: undefined, category: undefined, source: undefined };
}

export const ModelInfoMakeRequest: MessageFns<ModelInfoMakeRequest> = {
  encode(message: ModelInfoMakeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.framework !== undefined) {
      writer.uint32(24).int32(message.framework);
    }
    if (message.category !== undefined) {
      writer.uint32(32).int32(message.category);
    }
    if (message.source !== undefined) {
      writer.uint32(40).int32(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelInfoMakeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelInfoMakeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelInfoMakeRequest {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      source: isSet(object.source) ? modelSourceFromJSON(object.source) : undefined,
    };
  },

  toJSON(message: ModelInfoMakeRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.source !== undefined) {
      obj.source = modelSourceToJSON(message.source);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelInfoMakeRequest>, I>>(base?: I): ModelInfoMakeRequest {
    return ModelInfoMakeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelInfoMakeRequest>, I>>(object: I): ModelInfoMakeRequest {
    const message = createBaseModelInfoMakeRequest();
    message.url = object.url ?? "";
    message.name = object.name ?? "";
    message.framework = object.framework ?? undefined;
    message.category = object.category ?? undefined;
    message.source = object.source ?? undefined;
    return message;
  },
};

function createBaseRegisterModelFromUrlRequest(): RegisterModelFromUrlRequest {
  return {
    url: "",
    name: "",
    framework: undefined,
    category: undefined,
    source: undefined,
    memoryRequiredBytes: undefined,
    supportsThinking: undefined,
    supportsLora: undefined,
    artifactType: undefined,
    contextLength: undefined,
    description: undefined,
    downloadSizeBytes: undefined,
    id: undefined,
  };
}

export const RegisterModelFromUrlRequest: MessageFns<RegisterModelFromUrlRequest> = {
  encode(message: RegisterModelFromUrlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.framework !== undefined) {
      writer.uint32(24).int32(message.framework);
    }
    if (message.category !== undefined) {
      writer.uint32(32).int32(message.category);
    }
    if (message.source !== undefined) {
      writer.uint32(40).int32(message.source);
    }
    if (message.memoryRequiredBytes !== undefined) {
      writer.uint32(48).int64(message.memoryRequiredBytes);
    }
    if (message.supportsThinking !== undefined) {
      writer.uint32(56).bool(message.supportsThinking);
    }
    if (message.supportsLora !== undefined) {
      writer.uint32(64).bool(message.supportsLora);
    }
    if (message.artifactType !== undefined) {
      writer.uint32(72).int32(message.artifactType);
    }
    if (message.contextLength !== undefined) {
      writer.uint32(80).int32(message.contextLength);
    }
    if (message.description !== undefined) {
      writer.uint32(90).string(message.description);
    }
    if (message.downloadSizeBytes !== undefined) {
      writer.uint32(96).int64(message.downloadSizeBytes);
    }
    if (message.id !== undefined) {
      writer.uint32(106).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterModelFromUrlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterModelFromUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.memoryRequiredBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.supportsThinking = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.supportsLora = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.artifactType = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.contextLength = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.downloadSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterModelFromUrlRequest {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : undefined,
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      source: isSet(object.source) ? modelSourceFromJSON(object.source) : undefined,
      memoryRequiredBytes: isSet(object.memoryRequiredBytes)
        ? globalThis.Number(object.memoryRequiredBytes)
        : isSet(object.memory_required_bytes)
        ? globalThis.Number(object.memory_required_bytes)
        : undefined,
      supportsThinking: isSet(object.supportsThinking)
        ? globalThis.Boolean(object.supportsThinking)
        : isSet(object.supports_thinking)
        ? globalThis.Boolean(object.supports_thinking)
        : undefined,
      supportsLora: isSet(object.supportsLora)
        ? globalThis.Boolean(object.supportsLora)
        : isSet(object.supports_lora)
        ? globalThis.Boolean(object.supports_lora)
        : undefined,
      artifactType: isSet(object.artifactType)
        ? modelArtifactTypeFromJSON(object.artifactType)
        : isSet(object.artifact_type)
        ? modelArtifactTypeFromJSON(object.artifact_type)
        : undefined,
      contextLength: isSet(object.contextLength)
        ? globalThis.Number(object.contextLength)
        : isSet(object.context_length)
        ? globalThis.Number(object.context_length)
        : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      downloadSizeBytes: isSet(object.downloadSizeBytes)
        ? globalThis.Number(object.downloadSizeBytes)
        : isSet(object.download_size_bytes)
        ? globalThis.Number(object.download_size_bytes)
        : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
    };
  },

  toJSON(message: RegisterModelFromUrlRequest): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.framework !== undefined) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.source !== undefined) {
      obj.source = modelSourceToJSON(message.source);
    }
    if (message.memoryRequiredBytes !== undefined) {
      obj.memoryRequiredBytes = Math.round(message.memoryRequiredBytes);
    }
    if (message.supportsThinking !== undefined) {
      obj.supportsThinking = message.supportsThinking;
    }
    if (message.supportsLora !== undefined) {
      obj.supportsLora = message.supportsLora;
    }
    if (message.artifactType !== undefined) {
      obj.artifactType = modelArtifactTypeToJSON(message.artifactType);
    }
    if (message.contextLength !== undefined) {
      obj.contextLength = Math.round(message.contextLength);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.downloadSizeBytes !== undefined) {
      obj.downloadSizeBytes = Math.round(message.downloadSizeBytes);
    }
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterModelFromUrlRequest>, I>>(base?: I): RegisterModelFromUrlRequest {
    return RegisterModelFromUrlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterModelFromUrlRequest>, I>>(object: I): RegisterModelFromUrlRequest {
    const message = createBaseRegisterModelFromUrlRequest();
    message.url = object.url ?? "";
    message.name = object.name ?? "";
    message.framework = object.framework ?? undefined;
    message.category = object.category ?? undefined;
    message.source = object.source ?? undefined;
    message.memoryRequiredBytes = object.memoryRequiredBytes ?? undefined;
    message.supportsThinking = object.supportsThinking ?? undefined;
    message.supportsLora = object.supportsLora ?? undefined;
    message.artifactType = object.artifactType ?? undefined;
    message.contextLength = object.contextLength ?? undefined;
    message.description = object.description ?? undefined;
    message.downloadSizeBytes = object.downloadSizeBytes ?? undefined;
    message.id = object.id ?? undefined;
    return message;
  },
};

function createBaseRegisterMultiFileModelRequest(): RegisterMultiFileModelRequest {
  return {
    id: "",
    name: "",
    framework: 0,
    files: [],
    category: undefined,
    format: undefined,
    memoryRequiredBytes: undefined,
    downloadSizeBytes: undefined,
    contextLength: undefined,
    supportsThinking: undefined,
    supportsLora: undefined,
    description: undefined,
    source: undefined,
  };
}

export const RegisterMultiFileModelRequest: MessageFns<RegisterMultiFileModelRequest> = {
  encode(message: RegisterMultiFileModelRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.framework !== 0) {
      writer.uint32(24).int32(message.framework);
    }
    for (const v of message.files) {
      ModelFileDescriptor.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.category !== undefined) {
      writer.uint32(40).int32(message.category);
    }
    if (message.format !== undefined) {
      writer.uint32(48).int32(message.format);
    }
    if (message.memoryRequiredBytes !== undefined) {
      writer.uint32(56).int64(message.memoryRequiredBytes);
    }
    if (message.downloadSizeBytes !== undefined) {
      writer.uint32(64).int64(message.downloadSizeBytes);
    }
    if (message.contextLength !== undefined) {
      writer.uint32(72).int32(message.contextLength);
    }
    if (message.supportsThinking !== undefined) {
      writer.uint32(80).bool(message.supportsThinking);
    }
    if (message.supportsLora !== undefined) {
      writer.uint32(88).bool(message.supportsLora);
    }
    if (message.description !== undefined) {
      writer.uint32(98).string(message.description);
    }
    if (message.source !== undefined) {
      writer.uint32(104).int32(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterMultiFileModelRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterMultiFileModelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.framework = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.files.push(ModelFileDescriptor.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.memoryRequiredBytes = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.downloadSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.contextLength = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.supportsThinking = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.supportsLora = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterMultiFileModelRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      framework: isSet(object.framework) ? inferenceFrameworkFromJSON(object.framework) : 0,
      files: globalThis.Array.isArray(object?.files)
        ? object.files.map((e: any) => ModelFileDescriptor.fromJSON(e))
        : [],
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
      format: isSet(object.format) ? modelFormatFromJSON(object.format) : undefined,
      memoryRequiredBytes: isSet(object.memoryRequiredBytes)
        ? globalThis.Number(object.memoryRequiredBytes)
        : isSet(object.memory_required_bytes)
        ? globalThis.Number(object.memory_required_bytes)
        : undefined,
      downloadSizeBytes: isSet(object.downloadSizeBytes)
        ? globalThis.Number(object.downloadSizeBytes)
        : isSet(object.download_size_bytes)
        ? globalThis.Number(object.download_size_bytes)
        : undefined,
      contextLength: isSet(object.contextLength)
        ? globalThis.Number(object.contextLength)
        : isSet(object.context_length)
        ? globalThis.Number(object.context_length)
        : undefined,
      supportsThinking: isSet(object.supportsThinking)
        ? globalThis.Boolean(object.supportsThinking)
        : isSet(object.supports_thinking)
        ? globalThis.Boolean(object.supports_thinking)
        : undefined,
      supportsLora: isSet(object.supportsLora)
        ? globalThis.Boolean(object.supportsLora)
        : isSet(object.supports_lora)
        ? globalThis.Boolean(object.supports_lora)
        : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      source: isSet(object.source) ? modelSourceFromJSON(object.source) : undefined,
    };
  },

  toJSON(message: RegisterMultiFileModelRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.framework !== 0) {
      obj.framework = inferenceFrameworkToJSON(message.framework);
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => ModelFileDescriptor.toJSON(e));
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    if (message.format !== undefined) {
      obj.format = modelFormatToJSON(message.format);
    }
    if (message.memoryRequiredBytes !== undefined) {
      obj.memoryRequiredBytes = Math.round(message.memoryRequiredBytes);
    }
    if (message.downloadSizeBytes !== undefined) {
      obj.downloadSizeBytes = Math.round(message.downloadSizeBytes);
    }
    if (message.contextLength !== undefined) {
      obj.contextLength = Math.round(message.contextLength);
    }
    if (message.supportsThinking !== undefined) {
      obj.supportsThinking = message.supportsThinking;
    }
    if (message.supportsLora !== undefined) {
      obj.supportsLora = message.supportsLora;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.source !== undefined) {
      obj.source = modelSourceToJSON(message.source);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterMultiFileModelRequest>, I>>(base?: I): RegisterMultiFileModelRequest {
    return RegisterMultiFileModelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterMultiFileModelRequest>, I>>(
    object: I,
  ): RegisterMultiFileModelRequest {
    const message = createBaseRegisterMultiFileModelRequest();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.framework = object.framework ?? 0;
    message.files = object.files?.map((e) => ModelFileDescriptor.fromPartial(e)) || [];
    message.category = object.category ?? undefined;
    message.format = object.format ?? undefined;
    message.memoryRequiredBytes = object.memoryRequiredBytes ?? undefined;
    message.downloadSizeBytes = object.downloadSizeBytes ?? undefined;
    message.contextLength = object.contextLength ?? undefined;
    message.supportsThinking = object.supportsThinking ?? undefined;
    message.supportsLora = object.supportsLora ?? undefined;
    message.description = object.description ?? undefined;
    message.source = object.source ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
