// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: solutions.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

/**
 * ---------------------------------------------------------------------------
 * SolutionType — discriminator for the kind of solution backing a
 * `SolutionConfig` / `SolutionHandle`. Mirrors the `SolutionConfig.config`
 * oneof arms so frontends can switch on a single enum value rather than
 * inspecting the oneof shape.
 * ---------------------------------------------------------------------------
 */
export enum SolutionType {
  SOLUTION_TYPE_UNSPECIFIED = 0,
  SOLUTION_TYPE_VOICE_AGENT = 1,
  SOLUTION_TYPE_RAG = 2,
  SOLUTION_TYPE_TIME_SERIES = 4,
  SOLUTION_TYPE_AGENT_LOOP = 5,
  UNRECOGNIZED = -1,
}

export function solutionTypeFromJSON(object: any): SolutionType {
  switch (object) {
    case 0:
    case "SOLUTION_TYPE_UNSPECIFIED":
      return SolutionType.SOLUTION_TYPE_UNSPECIFIED;
    case 1:
    case "SOLUTION_TYPE_VOICE_AGENT":
      return SolutionType.SOLUTION_TYPE_VOICE_AGENT;
    case 2:
    case "SOLUTION_TYPE_RAG":
      return SolutionType.SOLUTION_TYPE_RAG;
    case 4:
    case "SOLUTION_TYPE_TIME_SERIES":
      return SolutionType.SOLUTION_TYPE_TIME_SERIES;
    case 5:
    case "SOLUTION_TYPE_AGENT_LOOP":
      return SolutionType.SOLUTION_TYPE_AGENT_LOOP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SolutionType.UNRECOGNIZED;
  }
}

export function solutionTypeToJSON(object: SolutionType): string {
  switch (object) {
    case SolutionType.SOLUTION_TYPE_UNSPECIFIED:
      return "SOLUTION_TYPE_UNSPECIFIED";
    case SolutionType.SOLUTION_TYPE_VOICE_AGENT:
      return "SOLUTION_TYPE_VOICE_AGENT";
    case SolutionType.SOLUTION_TYPE_RAG:
      return "SOLUTION_TYPE_RAG";
    case SolutionType.SOLUTION_TYPE_TIME_SERIES:
      return "SOLUTION_TYPE_TIME_SERIES";
    case SolutionType.SOLUTION_TYPE_AGENT_LOOP:
      return "SOLUTION_TYPE_AGENT_LOOP";
    case SolutionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum AudioSource {
  AUDIO_SOURCE_UNSPECIFIED = 0,
  /** AUDIO_SOURCE_MICROPHONE - Platform mic (default) */
  AUDIO_SOURCE_MICROPHONE = 1,
  /** AUDIO_SOURCE_FILE - Path supplied in audio_file_path */
  AUDIO_SOURCE_FILE = 2,
  /** AUDIO_SOURCE_CALLBACK - Frontend feeds frames via C ABI */
  AUDIO_SOURCE_CALLBACK = 3,
  UNRECOGNIZED = -1,
}

export function audioSourceFromJSON(object: any): AudioSource {
  switch (object) {
    case 0:
    case "AUDIO_SOURCE_UNSPECIFIED":
      return AudioSource.AUDIO_SOURCE_UNSPECIFIED;
    case 1:
    case "AUDIO_SOURCE_MICROPHONE":
      return AudioSource.AUDIO_SOURCE_MICROPHONE;
    case 2:
    case "AUDIO_SOURCE_FILE":
      return AudioSource.AUDIO_SOURCE_FILE;
    case 3:
    case "AUDIO_SOURCE_CALLBACK":
      return AudioSource.AUDIO_SOURCE_CALLBACK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AudioSource.UNRECOGNIZED;
  }
}

export function audioSourceToJSON(object: AudioSource): string {
  switch (object) {
    case AudioSource.AUDIO_SOURCE_UNSPECIFIED:
      return "AUDIO_SOURCE_UNSPECIFIED";
    case AudioSource.AUDIO_SOURCE_MICROPHONE:
      return "AUDIO_SOURCE_MICROPHONE";
    case AudioSource.AUDIO_SOURCE_FILE:
      return "AUDIO_SOURCE_FILE";
    case AudioSource.AUDIO_SOURCE_CALLBACK:
      return "AUDIO_SOURCE_CALLBACK";
    case AudioSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum VectorStore {
  VECTOR_STORE_UNSPECIFIED = 0,
  /** VECTOR_STORE_USEARCH - default, in-process HNSW */
  VECTOR_STORE_USEARCH = 1,
  /** VECTOR_STORE_PGVECTOR - remote, server deployments only */
  VECTOR_STORE_PGVECTOR = 2,
  UNRECOGNIZED = -1,
}

export function vectorStoreFromJSON(object: any): VectorStore {
  switch (object) {
    case 0:
    case "VECTOR_STORE_UNSPECIFIED":
      return VectorStore.VECTOR_STORE_UNSPECIFIED;
    case 1:
    case "VECTOR_STORE_USEARCH":
      return VectorStore.VECTOR_STORE_USEARCH;
    case 2:
    case "VECTOR_STORE_PGVECTOR":
      return VectorStore.VECTOR_STORE_PGVECTOR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VectorStore.UNRECOGNIZED;
  }
}

export function vectorStoreToJSON(object: VectorStore): string {
  switch (object) {
    case VectorStore.VECTOR_STORE_UNSPECIFIED:
      return "VECTOR_STORE_UNSPECIFIED";
    case VectorStore.VECTOR_STORE_USEARCH:
      return "VECTOR_STORE_USEARCH";
    case VectorStore.VECTOR_STORE_PGVECTOR:
      return "VECTOR_STORE_PGVECTOR";
    case VectorStore.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Top-level union dispatched to the matching solution loader. */
export interface SolutionConfig {
  voiceAgent?: VoiceAgentConfig | undefined;
  rag?: RAGConfig | undefined;
  agentLoop?: AgentLoopConfig | undefined;
  timeSeries?: TimeSeriesConfig | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * SolutionHandle — opaque, serialisable descriptor for a started solution.
 *
 * The native side owns a `rac_solution_handle_t`; this message is the
 * language-agnostic shape that frontends (Swift `SolutionHandle` class,
 * Kotlin/Flutter/RN/Web equivalents) carry across the C ABI to identify
 * the underlying instance. Lifecycle verbs (start/stop/cancel/feed/destroy)
 * are issued against the C handle keyed by `handle_id`.
 * ---------------------------------------------------------------------------
 */
export interface SolutionHandle {
  /**
   * Stable, opaque identifier minted by the core for this solution
   * instance. Used as the lookup key for lifecycle calls.
   */
  handleId: string;
  /**
   * String discriminator for the solution kind, e.g. "voice_agent",
   * "rag", "time_series", "agent_loop". Free-form for
   * forward-compat with future solutions; canonical values match the
   * `SolutionType` enum names lower-cased.
   */
  solutionType: string;
  /** Wall-clock creation timestamp (ms since Unix epoch). */
  createdAtMs: number;
  /**
   * Optional engine-specific state string (e.g. "created", "running",
   * "stopped"). Empty when the host hasn't surfaced state.
   */
  state?: string | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * VoiceAgent — the canonical streaming voice AI loop.
 * ---------------------------------------------------------------------------
 */
export interface VoiceAgentConfig {
  /** Model identifiers — resolved against the model registry. */
  llmModelId: string;
  /** e.g. "whisper-base" */
  sttModelId: string;
  /** e.g. "kokoro" */
  ttsModelId: string;
  /** e.g. "silero-v5" */
  vadModelId: string;
  /**
   * pass3-syn-025/030: explicit TTS voice id for multi-voice TTS engines
   * (Piper, eSpeak-NG, Sherpa-ONNX-TTS multi-voice). When unset, callers
   * fall back to using tts_model_id as the voice id — correct for
   * single-voice engines, wrong for multi-voice. Aligns the caller-facing
   * VoiceAgentConfig with the commons-facing RAVoiceAgentComposeConfig
   * (voice_agent_service.proto:214) which already exposes tts_voice_id.
   */
  ttsVoiceId: string;
  /** Audio configuration. */
  sampleRateHz: number;
  /** default 20 */
  chunkMs: number;
  audioSource: AudioSource;
  /**
   * Absolute path to an audio file. Required when `audio_source` is
   * `AUDIO_SOURCE_FILE`; ignored for MICROPHONE / CALLBACK sources.
   */
  audioFilePath: string;
  /** Barge-in behavior. */
  enableBargeIn?:
    | boolean
    | undefined;
  /** default 200 */
  bargeInThresholdMs: number;
  /** LLM behavior. */
  systemPrompt: string;
  maxContextTokens: number;
  temperature: number;
  /** Emit partial transcripts as UserSaidEvent{is_final=false}. */
  emitPartials: boolean;
  /** Emit thought tokens (qwen3, deepseek-r1) separately from answer tokens. */
  emitThoughts: boolean;
  /**
   * Optional explicit solution-kind tag. Redundant with the `SolutionConfig`
   * oneof arm; provided so callers that pass this message standalone (or
   * log it) can read a single discriminator. Defaults to UNSPECIFIED.
   */
  typeKind?: SolutionType | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * RAG — retrieve → rerank → prompt → LLM.
 * ---------------------------------------------------------------------------
 */
export interface RAGConfig {
  /** e.g. "bge-small-en-v1.5" */
  embedModelId: string;
  /** e.g. "bge-reranker-v2-m3" */
  rerankModelId: string;
  llmModelId: string;
  /** Vector store — USearch (in-process HNSW, default) or remote pgvector. */
  vectorStore: VectorStore;
  /** Local path for USearch index */
  vectorStorePath: string;
  /** default 24 */
  retrieveK: number;
  /** default 6 */
  rerankTop: number;
  /** BM25 parameters. */
  bm25K1: number;
  /** default 0.75 */
  bm25B: number;
  /** RRF fusion parameter. */
  rrfK: number;
  /** Prompt template. Supports {{context}} and {{query}} placeholders. */
  promptTemplate: string;
  /** Optional explicit solution-kind tag. See `SolutionType`. */
  typeKind?: SolutionType | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * Agent loop — multi-turn LLM with tool calling.
 * ---------------------------------------------------------------------------
 */
export interface AgentLoopConfig {
  llmModelId: string;
  systemPrompt: string;
  tools: ToolSpec[];
  /** default 10 */
  maxIterations: number;
  maxContextTokens: number;
  /** Optional explicit solution-kind tag. See `SolutionType`. */
  typeKind?: SolutionType | undefined;
}

export interface ToolSpec {
  name: string;
  description: string;
  /** Parameters schema, OpenAI-compatible */
  jsonSchema: string;
}

/**
 * ---------------------------------------------------------------------------
 * Time series — window + anomaly_detect + generate_text.
 * ---------------------------------------------------------------------------
 */
export interface TimeSeriesConfig {
  anomalyModelId: string;
  llmModelId: string;
  /** Samples per window */
  windowSize: number;
  stride: number;
  anomalyThreshold: number;
  /** Optional explicit solution-kind tag. See `SolutionType`. */
  typeKind?: SolutionType | undefined;
}

function createBaseSolutionConfig(): SolutionConfig {
  return { voiceAgent: undefined, rag: undefined, agentLoop: undefined, timeSeries: undefined };
}

export const SolutionConfig: MessageFns<SolutionConfig> = {
  encode(message: SolutionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.voiceAgent !== undefined) {
      VoiceAgentConfig.encode(message.voiceAgent, writer.uint32(10).fork()).join();
    }
    if (message.rag !== undefined) {
      RAGConfig.encode(message.rag, writer.uint32(18).fork()).join();
    }
    if (message.agentLoop !== undefined) {
      AgentLoopConfig.encode(message.agentLoop, writer.uint32(34).fork()).join();
    }
    if (message.timeSeries !== undefined) {
      TimeSeriesConfig.encode(message.timeSeries, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SolutionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolutionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.voiceAgent = VoiceAgentConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rag = RAGConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.agentLoop = AgentLoopConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timeSeries = TimeSeriesConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolutionConfig {
    return {
      voiceAgent: isSet(object.voiceAgent)
        ? VoiceAgentConfig.fromJSON(object.voiceAgent)
        : isSet(object.voice_agent)
        ? VoiceAgentConfig.fromJSON(object.voice_agent)
        : undefined,
      rag: isSet(object.rag) ? RAGConfig.fromJSON(object.rag) : undefined,
      agentLoop: isSet(object.agentLoop)
        ? AgentLoopConfig.fromJSON(object.agentLoop)
        : isSet(object.agent_loop)
        ? AgentLoopConfig.fromJSON(object.agent_loop)
        : undefined,
      timeSeries: isSet(object.timeSeries)
        ? TimeSeriesConfig.fromJSON(object.timeSeries)
        : isSet(object.time_series)
        ? TimeSeriesConfig.fromJSON(object.time_series)
        : undefined,
    };
  },

  toJSON(message: SolutionConfig): unknown {
    const obj: any = {};
    if (message.voiceAgent !== undefined) {
      obj.voiceAgent = VoiceAgentConfig.toJSON(message.voiceAgent);
    }
    if (message.rag !== undefined) {
      obj.rag = RAGConfig.toJSON(message.rag);
    }
    if (message.agentLoop !== undefined) {
      obj.agentLoop = AgentLoopConfig.toJSON(message.agentLoop);
    }
    if (message.timeSeries !== undefined) {
      obj.timeSeries = TimeSeriesConfig.toJSON(message.timeSeries);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SolutionConfig>, I>>(base?: I): SolutionConfig {
    return SolutionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SolutionConfig>, I>>(object: I): SolutionConfig {
    const message = createBaseSolutionConfig();
    message.voiceAgent = (object.voiceAgent !== undefined && object.voiceAgent !== null)
      ? VoiceAgentConfig.fromPartial(object.voiceAgent)
      : undefined;
    message.rag = (object.rag !== undefined && object.rag !== null) ? RAGConfig.fromPartial(object.rag) : undefined;
    message.agentLoop = (object.agentLoop !== undefined && object.agentLoop !== null)
      ? AgentLoopConfig.fromPartial(object.agentLoop)
      : undefined;
    message.timeSeries = (object.timeSeries !== undefined && object.timeSeries !== null)
      ? TimeSeriesConfig.fromPartial(object.timeSeries)
      : undefined;
    return message;
  },
};

function createBaseSolutionHandle(): SolutionHandle {
  return { handleId: "", solutionType: "", createdAtMs: 0, state: undefined };
}

export const SolutionHandle: MessageFns<SolutionHandle> = {
  encode(message: SolutionHandle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handleId !== "") {
      writer.uint32(10).string(message.handleId);
    }
    if (message.solutionType !== "") {
      writer.uint32(18).string(message.solutionType);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(24).int64(message.createdAtMs);
    }
    if (message.state !== undefined) {
      writer.uint32(34).string(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SolutionHandle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolutionHandle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.handleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.solutionType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.state = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolutionHandle {
    return {
      handleId: isSet(object.handleId)
        ? globalThis.String(object.handleId)
        : isSet(object.handle_id)
        ? globalThis.String(object.handle_id)
        : "",
      solutionType: isSet(object.solutionType)
        ? globalThis.String(object.solutionType)
        : isSet(object.solution_type)
        ? globalThis.String(object.solution_type)
        : "",
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      state: isSet(object.state) ? globalThis.String(object.state) : undefined,
    };
  },

  toJSON(message: SolutionHandle): unknown {
    const obj: any = {};
    if (message.handleId !== "") {
      obj.handleId = message.handleId;
    }
    if (message.solutionType !== "") {
      obj.solutionType = message.solutionType;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.state !== undefined) {
      obj.state = message.state;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SolutionHandle>, I>>(base?: I): SolutionHandle {
    return SolutionHandle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SolutionHandle>, I>>(object: I): SolutionHandle {
    const message = createBaseSolutionHandle();
    message.handleId = object.handleId ?? "";
    message.solutionType = object.solutionType ?? "";
    message.createdAtMs = object.createdAtMs ?? 0;
    message.state = object.state ?? undefined;
    return message;
  },
};

function createBaseVoiceAgentConfig(): VoiceAgentConfig {
  return {
    llmModelId: "",
    sttModelId: "",
    ttsModelId: "",
    vadModelId: "",
    ttsVoiceId: "",
    sampleRateHz: 0,
    chunkMs: 0,
    audioSource: 0,
    audioFilePath: "",
    enableBargeIn: undefined,
    bargeInThresholdMs: 0,
    systemPrompt: "",
    maxContextTokens: 0,
    temperature: 0,
    emitPartials: false,
    emitThoughts: false,
    typeKind: undefined,
  };
}

export const VoiceAgentConfig: MessageFns<VoiceAgentConfig> = {
  encode(message: VoiceAgentConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.llmModelId !== "") {
      writer.uint32(10).string(message.llmModelId);
    }
    if (message.sttModelId !== "") {
      writer.uint32(18).string(message.sttModelId);
    }
    if (message.ttsModelId !== "") {
      writer.uint32(26).string(message.ttsModelId);
    }
    if (message.vadModelId !== "") {
      writer.uint32(34).string(message.vadModelId);
    }
    if (message.ttsVoiceId !== "") {
      writer.uint32(138).string(message.ttsVoiceId);
    }
    if (message.sampleRateHz !== 0) {
      writer.uint32(40).int32(message.sampleRateHz);
    }
    if (message.chunkMs !== 0) {
      writer.uint32(48).int32(message.chunkMs);
    }
    if (message.audioSource !== 0) {
      writer.uint32(56).int32(message.audioSource);
    }
    if (message.audioFilePath !== "") {
      writer.uint32(122).string(message.audioFilePath);
    }
    if (message.enableBargeIn !== undefined) {
      writer.uint32(64).bool(message.enableBargeIn);
    }
    if (message.bargeInThresholdMs !== 0) {
      writer.uint32(72).int32(message.bargeInThresholdMs);
    }
    if (message.systemPrompt !== "") {
      writer.uint32(82).string(message.systemPrompt);
    }
    if (message.maxContextTokens !== 0) {
      writer.uint32(88).int32(message.maxContextTokens);
    }
    if (message.temperature !== 0) {
      writer.uint32(101).float(message.temperature);
    }
    if (message.emitPartials !== false) {
      writer.uint32(104).bool(message.emitPartials);
    }
    if (message.emitThoughts !== false) {
      writer.uint32(112).bool(message.emitThoughts);
    }
    if (message.typeKind !== undefined) {
      writer.uint32(128).int32(message.typeKind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceAgentConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceAgentConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sttModelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ttsModelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vadModelId = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.ttsVoiceId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sampleRateHz = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.chunkMs = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.audioSource = reader.int32() as any;
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.audioFilePath = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.enableBargeIn = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.bargeInThresholdMs = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.maxContextTokens = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 101) {
            break;
          }

          message.temperature = reader.float();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.emitPartials = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.emitThoughts = reader.bool();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.typeKind = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceAgentConfig {
    return {
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      sttModelId: isSet(object.sttModelId)
        ? globalThis.String(object.sttModelId)
        : isSet(object.stt_model_id)
        ? globalThis.String(object.stt_model_id)
        : "",
      ttsModelId: isSet(object.ttsModelId)
        ? globalThis.String(object.ttsModelId)
        : isSet(object.tts_model_id)
        ? globalThis.String(object.tts_model_id)
        : "",
      vadModelId: isSet(object.vadModelId)
        ? globalThis.String(object.vadModelId)
        : isSet(object.vad_model_id)
        ? globalThis.String(object.vad_model_id)
        : "",
      ttsVoiceId: isSet(object.ttsVoiceId)
        ? globalThis.String(object.ttsVoiceId)
        : isSet(object.tts_voice_id)
        ? globalThis.String(object.tts_voice_id)
        : "",
      sampleRateHz: isSet(object.sampleRateHz)
        ? globalThis.Number(object.sampleRateHz)
        : isSet(object.sample_rate_hz)
        ? globalThis.Number(object.sample_rate_hz)
        : 0,
      chunkMs: isSet(object.chunkMs)
        ? globalThis.Number(object.chunkMs)
        : isSet(object.chunk_ms)
        ? globalThis.Number(object.chunk_ms)
        : 0,
      audioSource: isSet(object.audioSource)
        ? audioSourceFromJSON(object.audioSource)
        : isSet(object.audio_source)
        ? audioSourceFromJSON(object.audio_source)
        : 0,
      audioFilePath: isSet(object.audioFilePath)
        ? globalThis.String(object.audioFilePath)
        : isSet(object.audio_file_path)
        ? globalThis.String(object.audio_file_path)
        : "",
      enableBargeIn: isSet(object.enableBargeIn)
        ? globalThis.Boolean(object.enableBargeIn)
        : isSet(object.enable_barge_in)
        ? globalThis.Boolean(object.enable_barge_in)
        : undefined,
      bargeInThresholdMs: isSet(object.bargeInThresholdMs)
        ? globalThis.Number(object.bargeInThresholdMs)
        : isSet(object.barge_in_threshold_ms)
        ? globalThis.Number(object.barge_in_threshold_ms)
        : 0,
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : "",
      maxContextTokens: isSet(object.maxContextTokens)
        ? globalThis.Number(object.maxContextTokens)
        : isSet(object.max_context_tokens)
        ? globalThis.Number(object.max_context_tokens)
        : 0,
      temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
      emitPartials: isSet(object.emitPartials)
        ? globalThis.Boolean(object.emitPartials)
        : isSet(object.emit_partials)
        ? globalThis.Boolean(object.emit_partials)
        : false,
      emitThoughts: isSet(object.emitThoughts)
        ? globalThis.Boolean(object.emitThoughts)
        : isSet(object.emit_thoughts)
        ? globalThis.Boolean(object.emit_thoughts)
        : false,
      typeKind: isSet(object.typeKind)
        ? solutionTypeFromJSON(object.typeKind)
        : isSet(object.type_kind)
        ? solutionTypeFromJSON(object.type_kind)
        : undefined,
    };
  },

  toJSON(message: VoiceAgentConfig): unknown {
    const obj: any = {};
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.sttModelId !== "") {
      obj.sttModelId = message.sttModelId;
    }
    if (message.ttsModelId !== "") {
      obj.ttsModelId = message.ttsModelId;
    }
    if (message.vadModelId !== "") {
      obj.vadModelId = message.vadModelId;
    }
    if (message.ttsVoiceId !== "") {
      obj.ttsVoiceId = message.ttsVoiceId;
    }
    if (message.sampleRateHz !== 0) {
      obj.sampleRateHz = Math.round(message.sampleRateHz);
    }
    if (message.chunkMs !== 0) {
      obj.chunkMs = Math.round(message.chunkMs);
    }
    if (message.audioSource !== 0) {
      obj.audioSource = audioSourceToJSON(message.audioSource);
    }
    if (message.audioFilePath !== "") {
      obj.audioFilePath = message.audioFilePath;
    }
    if (message.enableBargeIn !== undefined) {
      obj.enableBargeIn = message.enableBargeIn;
    }
    if (message.bargeInThresholdMs !== 0) {
      obj.bargeInThresholdMs = Math.round(message.bargeInThresholdMs);
    }
    if (message.systemPrompt !== "") {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.maxContextTokens !== 0) {
      obj.maxContextTokens = Math.round(message.maxContextTokens);
    }
    if (message.temperature !== 0) {
      obj.temperature = message.temperature;
    }
    if (message.emitPartials !== false) {
      obj.emitPartials = message.emitPartials;
    }
    if (message.emitThoughts !== false) {
      obj.emitThoughts = message.emitThoughts;
    }
    if (message.typeKind !== undefined) {
      obj.typeKind = solutionTypeToJSON(message.typeKind);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceAgentConfig>, I>>(base?: I): VoiceAgentConfig {
    return VoiceAgentConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceAgentConfig>, I>>(object: I): VoiceAgentConfig {
    const message = createBaseVoiceAgentConfig();
    message.llmModelId = object.llmModelId ?? "";
    message.sttModelId = object.sttModelId ?? "";
    message.ttsModelId = object.ttsModelId ?? "";
    message.vadModelId = object.vadModelId ?? "";
    message.ttsVoiceId = object.ttsVoiceId ?? "";
    message.sampleRateHz = object.sampleRateHz ?? 0;
    message.chunkMs = object.chunkMs ?? 0;
    message.audioSource = object.audioSource ?? 0;
    message.audioFilePath = object.audioFilePath ?? "";
    message.enableBargeIn = object.enableBargeIn ?? undefined;
    message.bargeInThresholdMs = object.bargeInThresholdMs ?? 0;
    message.systemPrompt = object.systemPrompt ?? "";
    message.maxContextTokens = object.maxContextTokens ?? 0;
    message.temperature = object.temperature ?? 0;
    message.emitPartials = object.emitPartials ?? false;
    message.emitThoughts = object.emitThoughts ?? false;
    message.typeKind = object.typeKind ?? undefined;
    return message;
  },
};

function createBaseRAGConfig(): RAGConfig {
  return {
    embedModelId: "",
    rerankModelId: "",
    llmModelId: "",
    vectorStore: 0,
    vectorStorePath: "",
    retrieveK: 0,
    rerankTop: 0,
    bm25K1: 0,
    bm25B: 0,
    rrfK: 0,
    promptTemplate: "",
    typeKind: undefined,
  };
}

export const RAGConfig: MessageFns<RAGConfig> = {
  encode(message: RAGConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.embedModelId !== "") {
      writer.uint32(10).string(message.embedModelId);
    }
    if (message.rerankModelId !== "") {
      writer.uint32(18).string(message.rerankModelId);
    }
    if (message.llmModelId !== "") {
      writer.uint32(26).string(message.llmModelId);
    }
    if (message.vectorStore !== 0) {
      writer.uint32(32).int32(message.vectorStore);
    }
    if (message.vectorStorePath !== "") {
      writer.uint32(42).string(message.vectorStorePath);
    }
    if (message.retrieveK !== 0) {
      writer.uint32(48).int32(message.retrieveK);
    }
    if (message.rerankTop !== 0) {
      writer.uint32(56).int32(message.rerankTop);
    }
    if (message.bm25K1 !== 0) {
      writer.uint32(69).float(message.bm25K1);
    }
    if (message.bm25B !== 0) {
      writer.uint32(77).float(message.bm25B);
    }
    if (message.rrfK !== 0) {
      writer.uint32(80).int32(message.rrfK);
    }
    if (message.promptTemplate !== "") {
      writer.uint32(90).string(message.promptTemplate);
    }
    if (message.typeKind !== undefined) {
      writer.uint32(96).int32(message.typeKind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RAGConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRAGConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.embedModelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rerankModelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.vectorStore = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.vectorStorePath = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.retrieveK = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.rerankTop = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 69) {
            break;
          }

          message.bm25K1 = reader.float();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.bm25B = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.rrfK = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.promptTemplate = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.typeKind = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RAGConfig {
    return {
      embedModelId: isSet(object.embedModelId)
        ? globalThis.String(object.embedModelId)
        : isSet(object.embed_model_id)
        ? globalThis.String(object.embed_model_id)
        : "",
      rerankModelId: isSet(object.rerankModelId)
        ? globalThis.String(object.rerankModelId)
        : isSet(object.rerank_model_id)
        ? globalThis.String(object.rerank_model_id)
        : "",
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      vectorStore: isSet(object.vectorStore)
        ? vectorStoreFromJSON(object.vectorStore)
        : isSet(object.vector_store)
        ? vectorStoreFromJSON(object.vector_store)
        : 0,
      vectorStorePath: isSet(object.vectorStorePath)
        ? globalThis.String(object.vectorStorePath)
        : isSet(object.vector_store_path)
        ? globalThis.String(object.vector_store_path)
        : "",
      retrieveK: isSet(object.retrieveK)
        ? globalThis.Number(object.retrieveK)
        : isSet(object.retrieve_k)
        ? globalThis.Number(object.retrieve_k)
        : 0,
      rerankTop: isSet(object.rerankTop)
        ? globalThis.Number(object.rerankTop)
        : isSet(object.rerank_top)
        ? globalThis.Number(object.rerank_top)
        : 0,
      bm25K1: isSet(object.bm25K1)
        ? globalThis.Number(object.bm25K1)
        : isSet(object.bm25_k1)
        ? globalThis.Number(object.bm25_k1)
        : 0,
      bm25B: isSet(object.bm25B)
        ? globalThis.Number(object.bm25B)
        : isSet(object.bm25_b)
        ? globalThis.Number(object.bm25_b)
        : 0,
      rrfK: isSet(object.rrfK)
        ? globalThis.Number(object.rrfK)
        : isSet(object.rrf_k)
        ? globalThis.Number(object.rrf_k)
        : 0,
      promptTemplate: isSet(object.promptTemplate)
        ? globalThis.String(object.promptTemplate)
        : isSet(object.prompt_template)
        ? globalThis.String(object.prompt_template)
        : "",
      typeKind: isSet(object.typeKind)
        ? solutionTypeFromJSON(object.typeKind)
        : isSet(object.type_kind)
        ? solutionTypeFromJSON(object.type_kind)
        : undefined,
    };
  },

  toJSON(message: RAGConfig): unknown {
    const obj: any = {};
    if (message.embedModelId !== "") {
      obj.embedModelId = message.embedModelId;
    }
    if (message.rerankModelId !== "") {
      obj.rerankModelId = message.rerankModelId;
    }
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.vectorStore !== 0) {
      obj.vectorStore = vectorStoreToJSON(message.vectorStore);
    }
    if (message.vectorStorePath !== "") {
      obj.vectorStorePath = message.vectorStorePath;
    }
    if (message.retrieveK !== 0) {
      obj.retrieveK = Math.round(message.retrieveK);
    }
    if (message.rerankTop !== 0) {
      obj.rerankTop = Math.round(message.rerankTop);
    }
    if (message.bm25K1 !== 0) {
      obj.bm25K1 = message.bm25K1;
    }
    if (message.bm25B !== 0) {
      obj.bm25B = message.bm25B;
    }
    if (message.rrfK !== 0) {
      obj.rrfK = Math.round(message.rrfK);
    }
    if (message.promptTemplate !== "") {
      obj.promptTemplate = message.promptTemplate;
    }
    if (message.typeKind !== undefined) {
      obj.typeKind = solutionTypeToJSON(message.typeKind);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RAGConfig>, I>>(base?: I): RAGConfig {
    return RAGConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RAGConfig>, I>>(object: I): RAGConfig {
    const message = createBaseRAGConfig();
    message.embedModelId = object.embedModelId ?? "";
    message.rerankModelId = object.rerankModelId ?? "";
    message.llmModelId = object.llmModelId ?? "";
    message.vectorStore = object.vectorStore ?? 0;
    message.vectorStorePath = object.vectorStorePath ?? "";
    message.retrieveK = object.retrieveK ?? 0;
    message.rerankTop = object.rerankTop ?? 0;
    message.bm25K1 = object.bm25K1 ?? 0;
    message.bm25B = object.bm25B ?? 0;
    message.rrfK = object.rrfK ?? 0;
    message.promptTemplate = object.promptTemplate ?? "";
    message.typeKind = object.typeKind ?? undefined;
    return message;
  },
};

function createBaseAgentLoopConfig(): AgentLoopConfig {
  return { llmModelId: "", systemPrompt: "", tools: [], maxIterations: 0, maxContextTokens: 0, typeKind: undefined };
}

export const AgentLoopConfig: MessageFns<AgentLoopConfig> = {
  encode(message: AgentLoopConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.llmModelId !== "") {
      writer.uint32(10).string(message.llmModelId);
    }
    if (message.systemPrompt !== "") {
      writer.uint32(18).string(message.systemPrompt);
    }
    for (const v of message.tools) {
      ToolSpec.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.maxIterations !== 0) {
      writer.uint32(32).int32(message.maxIterations);
    }
    if (message.maxContextTokens !== 0) {
      writer.uint32(40).int32(message.maxContextTokens);
    }
    if (message.typeKind !== undefined) {
      writer.uint32(48).int32(message.typeKind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentLoopConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentLoopConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tools.push(ToolSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxIterations = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxContextTokens = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.typeKind = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentLoopConfig {
    return {
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : "",
      tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => ToolSpec.fromJSON(e)) : [],
      maxIterations: isSet(object.maxIterations)
        ? globalThis.Number(object.maxIterations)
        : isSet(object.max_iterations)
        ? globalThis.Number(object.max_iterations)
        : 0,
      maxContextTokens: isSet(object.maxContextTokens)
        ? globalThis.Number(object.maxContextTokens)
        : isSet(object.max_context_tokens)
        ? globalThis.Number(object.max_context_tokens)
        : 0,
      typeKind: isSet(object.typeKind)
        ? solutionTypeFromJSON(object.typeKind)
        : isSet(object.type_kind)
        ? solutionTypeFromJSON(object.type_kind)
        : undefined,
    };
  },

  toJSON(message: AgentLoopConfig): unknown {
    const obj: any = {};
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.systemPrompt !== "") {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.tools?.length) {
      obj.tools = message.tools.map((e) => ToolSpec.toJSON(e));
    }
    if (message.maxIterations !== 0) {
      obj.maxIterations = Math.round(message.maxIterations);
    }
    if (message.maxContextTokens !== 0) {
      obj.maxContextTokens = Math.round(message.maxContextTokens);
    }
    if (message.typeKind !== undefined) {
      obj.typeKind = solutionTypeToJSON(message.typeKind);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentLoopConfig>, I>>(base?: I): AgentLoopConfig {
    return AgentLoopConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentLoopConfig>, I>>(object: I): AgentLoopConfig {
    const message = createBaseAgentLoopConfig();
    message.llmModelId = object.llmModelId ?? "";
    message.systemPrompt = object.systemPrompt ?? "";
    message.tools = object.tools?.map((e) => ToolSpec.fromPartial(e)) || [];
    message.maxIterations = object.maxIterations ?? 0;
    message.maxContextTokens = object.maxContextTokens ?? 0;
    message.typeKind = object.typeKind ?? undefined;
    return message;
  },
};

function createBaseToolSpec(): ToolSpec {
  return { name: "", description: "", jsonSchema: "" };
}

export const ToolSpec: MessageFns<ToolSpec> = {
  encode(message: ToolSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.jsonSchema !== "") {
      writer.uint32(26).string(message.jsonSchema);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jsonSchema = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolSpec {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      jsonSchema: isSet(object.jsonSchema)
        ? globalThis.String(object.jsonSchema)
        : isSet(object.json_schema)
        ? globalThis.String(object.json_schema)
        : "",
    };
  },

  toJSON(message: ToolSpec): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.jsonSchema !== "") {
      obj.jsonSchema = message.jsonSchema;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolSpec>, I>>(base?: I): ToolSpec {
    return ToolSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolSpec>, I>>(object: I): ToolSpec {
    const message = createBaseToolSpec();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.jsonSchema = object.jsonSchema ?? "";
    return message;
  },
};

function createBaseTimeSeriesConfig(): TimeSeriesConfig {
  return { anomalyModelId: "", llmModelId: "", windowSize: 0, stride: 0, anomalyThreshold: 0, typeKind: undefined };
}

export const TimeSeriesConfig: MessageFns<TimeSeriesConfig> = {
  encode(message: TimeSeriesConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.anomalyModelId !== "") {
      writer.uint32(10).string(message.anomalyModelId);
    }
    if (message.llmModelId !== "") {
      writer.uint32(18).string(message.llmModelId);
    }
    if (message.windowSize !== 0) {
      writer.uint32(24).int32(message.windowSize);
    }
    if (message.stride !== 0) {
      writer.uint32(32).int32(message.stride);
    }
    if (message.anomalyThreshold !== 0) {
      writer.uint32(45).float(message.anomalyThreshold);
    }
    if (message.typeKind !== undefined) {
      writer.uint32(48).int32(message.typeKind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeSeriesConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeSeriesConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.anomalyModelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.windowSize = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.stride = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.anomalyThreshold = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.typeKind = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeSeriesConfig {
    return {
      anomalyModelId: isSet(object.anomalyModelId)
        ? globalThis.String(object.anomalyModelId)
        : isSet(object.anomaly_model_id)
        ? globalThis.String(object.anomaly_model_id)
        : "",
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      windowSize: isSet(object.windowSize)
        ? globalThis.Number(object.windowSize)
        : isSet(object.window_size)
        ? globalThis.Number(object.window_size)
        : 0,
      stride: isSet(object.stride) ? globalThis.Number(object.stride) : 0,
      anomalyThreshold: isSet(object.anomalyThreshold)
        ? globalThis.Number(object.anomalyThreshold)
        : isSet(object.anomaly_threshold)
        ? globalThis.Number(object.anomaly_threshold)
        : 0,
      typeKind: isSet(object.typeKind)
        ? solutionTypeFromJSON(object.typeKind)
        : isSet(object.type_kind)
        ? solutionTypeFromJSON(object.type_kind)
        : undefined,
    };
  },

  toJSON(message: TimeSeriesConfig): unknown {
    const obj: any = {};
    if (message.anomalyModelId !== "") {
      obj.anomalyModelId = message.anomalyModelId;
    }
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.windowSize !== 0) {
      obj.windowSize = Math.round(message.windowSize);
    }
    if (message.stride !== 0) {
      obj.stride = Math.round(message.stride);
    }
    if (message.anomalyThreshold !== 0) {
      obj.anomalyThreshold = message.anomalyThreshold;
    }
    if (message.typeKind !== undefined) {
      obj.typeKind = solutionTypeToJSON(message.typeKind);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeSeriesConfig>, I>>(base?: I): TimeSeriesConfig {
    return TimeSeriesConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeSeriesConfig>, I>>(object: I): TimeSeriesConfig {
    const message = createBaseTimeSeriesConfig();
    message.anomalyModelId = object.anomalyModelId ?? "";
    message.llmModelId = object.llmModelId ?? "";
    message.windowSize = object.windowSize ?? 0;
    message.stride = object.stride ?? 0;
    message.anomalyThreshold = object.anomalyThreshold ?? 0;
    message.typeKind = object.typeKind ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
