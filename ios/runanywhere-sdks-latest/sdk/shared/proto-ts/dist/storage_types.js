"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: storage_types.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.StorageDeleteResult = exports.StorageDeleteRequest = exports.StorageDeletePlan = exports.StorageDeleteCandidate = exports.StorageDeletePlanRequest = exports.StorageAvailabilityResult = exports.StorageAvailabilityRequest = exports.StorageInfoResult = exports.StorageInfoRequest = exports.StorageAvailability = exports.StorageInfo = exports.ModelStorageMetrics = exports.AppStorageInfo = exports.DeviceStorageInfo = exports.NPUChip = exports.protobufPackage = void 0;
exports.nPUChipFromJSON = nPUChipFromJSON;
exports.nPUChipToJSON = nPUChipToJSON;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "runanywhere.v1";
/**
 * ---------------------------------------------------------------------------
 * NPU chipset detected on the host device. Used to drive vendor-NPU
 * model-download URL selection and runtime backend wiring.
 * ---------------------------------------------------------------------------
 */
var NPUChip;
(function (NPUChip) {
    NPUChip[NPUChip["NPU_CHIP_UNSPECIFIED"] = 0] = "NPU_CHIP_UNSPECIFIED";
    /** NPU_CHIP_NONE - No NPU detected on this device */
    NPUChip[NPUChip["NPU_CHIP_NONE"] = 1] = "NPU_CHIP_NONE";
    /** NPU_CHIP_APPLE_NEURAL_ENGINE - Apple Neural Engine (A-series / M-series) */
    NPUChip[NPUChip["NPU_CHIP_APPLE_NEURAL_ENGINE"] = 2] = "NPU_CHIP_APPLE_NEURAL_ENGINE";
    /** NPU_CHIP_QUALCOMM_HEXAGON - Snapdragon 8 Elite, 8 Elite Gen 5, etc. */
    NPUChip[NPUChip["NPU_CHIP_QUALCOMM_HEXAGON"] = 3] = "NPU_CHIP_QUALCOMM_HEXAGON";
    /** NPU_CHIP_MEDIATEK_APU - MediaTek Dimensity APU */
    NPUChip[NPUChip["NPU_CHIP_MEDIATEK_APU"] = 4] = "NPU_CHIP_MEDIATEK_APU";
    /** NPU_CHIP_GOOGLE_TPU - Pixel Tensor / TPU */
    NPUChip[NPUChip["NPU_CHIP_GOOGLE_TPU"] = 5] = "NPU_CHIP_GOOGLE_TPU";
    /** NPU_CHIP_INTEL_NPU - Intel Core Ultra NPU */
    NPUChip[NPUChip["NPU_CHIP_INTEL_NPU"] = 6] = "NPU_CHIP_INTEL_NPU";
    /** NPU_CHIP_OTHER - Detected NPU but vendor unmapped */
    NPUChip[NPUChip["NPU_CHIP_OTHER"] = 99] = "NPU_CHIP_OTHER";
    NPUChip[NPUChip["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(NPUChip || (exports.NPUChip = NPUChip = {}));
function nPUChipFromJSON(object) {
    switch (object) {
        case 0:
        case "NPU_CHIP_UNSPECIFIED":
            return NPUChip.NPU_CHIP_UNSPECIFIED;
        case 1:
        case "NPU_CHIP_NONE":
            return NPUChip.NPU_CHIP_NONE;
        case 2:
        case "NPU_CHIP_APPLE_NEURAL_ENGINE":
            return NPUChip.NPU_CHIP_APPLE_NEURAL_ENGINE;
        case 3:
        case "NPU_CHIP_QUALCOMM_HEXAGON":
            return NPUChip.NPU_CHIP_QUALCOMM_HEXAGON;
        case 4:
        case "NPU_CHIP_MEDIATEK_APU":
            return NPUChip.NPU_CHIP_MEDIATEK_APU;
        case 5:
        case "NPU_CHIP_GOOGLE_TPU":
            return NPUChip.NPU_CHIP_GOOGLE_TPU;
        case 6:
        case "NPU_CHIP_INTEL_NPU":
            return NPUChip.NPU_CHIP_INTEL_NPU;
        case 99:
        case "NPU_CHIP_OTHER":
            return NPUChip.NPU_CHIP_OTHER;
        case -1:
        case "UNRECOGNIZED":
        default:
            return NPUChip.UNRECOGNIZED;
    }
}
function nPUChipToJSON(object) {
    switch (object) {
        case NPUChip.NPU_CHIP_UNSPECIFIED:
            return "NPU_CHIP_UNSPECIFIED";
        case NPUChip.NPU_CHIP_NONE:
            return "NPU_CHIP_NONE";
        case NPUChip.NPU_CHIP_APPLE_NEURAL_ENGINE:
            return "NPU_CHIP_APPLE_NEURAL_ENGINE";
        case NPUChip.NPU_CHIP_QUALCOMM_HEXAGON:
            return "NPU_CHIP_QUALCOMM_HEXAGON";
        case NPUChip.NPU_CHIP_MEDIATEK_APU:
            return "NPU_CHIP_MEDIATEK_APU";
        case NPUChip.NPU_CHIP_GOOGLE_TPU:
            return "NPU_CHIP_GOOGLE_TPU";
        case NPUChip.NPU_CHIP_INTEL_NPU:
            return "NPU_CHIP_INTEL_NPU";
        case NPUChip.NPU_CHIP_OTHER:
            return "NPU_CHIP_OTHER";
        case NPUChip.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseDeviceStorageInfo() {
    return { totalBytes: 0, freeBytes: 0, usedBytes: 0, usedPercent: 0 };
}
exports.DeviceStorageInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.totalBytes !== 0) {
            writer.uint32(8).int64(message.totalBytes);
        }
        if (message.freeBytes !== 0) {
            writer.uint32(16).int64(message.freeBytes);
        }
        if (message.usedBytes !== 0) {
            writer.uint32(24).int64(message.usedBytes);
        }
        if (message.usedPercent !== 0) {
            writer.uint32(37).float(message.usedPercent);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeviceStorageInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.totalBytes = longToNumber(reader.int64());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.freeBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.usedBytes = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 37) {
                        break;
                    }
                    message.usedPercent = reader.float();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            totalBytes: isSet(object.totalBytes)
                ? globalThis.Number(object.totalBytes)
                : isSet(object.total_bytes)
                    ? globalThis.Number(object.total_bytes)
                    : 0,
            freeBytes: isSet(object.freeBytes)
                ? globalThis.Number(object.freeBytes)
                : isSet(object.free_bytes)
                    ? globalThis.Number(object.free_bytes)
                    : 0,
            usedBytes: isSet(object.usedBytes)
                ? globalThis.Number(object.usedBytes)
                : isSet(object.used_bytes)
                    ? globalThis.Number(object.used_bytes)
                    : 0,
            usedPercent: isSet(object.usedPercent)
                ? globalThis.Number(object.usedPercent)
                : isSet(object.used_percent)
                    ? globalThis.Number(object.used_percent)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.totalBytes !== 0) {
            obj.totalBytes = Math.round(message.totalBytes);
        }
        if (message.freeBytes !== 0) {
            obj.freeBytes = Math.round(message.freeBytes);
        }
        if (message.usedBytes !== 0) {
            obj.usedBytes = Math.round(message.usedBytes);
        }
        if (message.usedPercent !== 0) {
            obj.usedPercent = message.usedPercent;
        }
        return obj;
    },
    create(base) {
        return exports.DeviceStorageInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDeviceStorageInfo();
        message.totalBytes = object.totalBytes ?? 0;
        message.freeBytes = object.freeBytes ?? 0;
        message.usedBytes = object.usedBytes ?? 0;
        message.usedPercent = object.usedPercent ?? 0;
        return message;
    },
};
function createBaseAppStorageInfo() {
    return { documentsBytes: 0, cacheBytes: 0, appSupportBytes: 0, totalBytes: 0 };
}
exports.AppStorageInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.documentsBytes !== 0) {
            writer.uint32(8).int64(message.documentsBytes);
        }
        if (message.cacheBytes !== 0) {
            writer.uint32(16).int64(message.cacheBytes);
        }
        if (message.appSupportBytes !== 0) {
            writer.uint32(24).int64(message.appSupportBytes);
        }
        if (message.totalBytes !== 0) {
            writer.uint32(32).int64(message.totalBytes);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAppStorageInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.documentsBytes = longToNumber(reader.int64());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.cacheBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.appSupportBytes = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.totalBytes = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            documentsBytes: isSet(object.documentsBytes)
                ? globalThis.Number(object.documentsBytes)
                : isSet(object.documents_bytes)
                    ? globalThis.Number(object.documents_bytes)
                    : 0,
            cacheBytes: isSet(object.cacheBytes)
                ? globalThis.Number(object.cacheBytes)
                : isSet(object.cache_bytes)
                    ? globalThis.Number(object.cache_bytes)
                    : 0,
            appSupportBytes: isSet(object.appSupportBytes)
                ? globalThis.Number(object.appSupportBytes)
                : isSet(object.app_support_bytes)
                    ? globalThis.Number(object.app_support_bytes)
                    : 0,
            totalBytes: isSet(object.totalBytes)
                ? globalThis.Number(object.totalBytes)
                : isSet(object.total_bytes)
                    ? globalThis.Number(object.total_bytes)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.documentsBytes !== 0) {
            obj.documentsBytes = Math.round(message.documentsBytes);
        }
        if (message.cacheBytes !== 0) {
            obj.cacheBytes = Math.round(message.cacheBytes);
        }
        if (message.appSupportBytes !== 0) {
            obj.appSupportBytes = Math.round(message.appSupportBytes);
        }
        if (message.totalBytes !== 0) {
            obj.totalBytes = Math.round(message.totalBytes);
        }
        return obj;
    },
    create(base) {
        return exports.AppStorageInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAppStorageInfo();
        message.documentsBytes = object.documentsBytes ?? 0;
        message.cacheBytes = object.cacheBytes ?? 0;
        message.appSupportBytes = object.appSupportBytes ?? 0;
        message.totalBytes = object.totalBytes ?? 0;
        return message;
    },
};
function createBaseModelStorageMetrics() {
    return { modelId: "", sizeOnDiskBytes: 0, lastUsedMs: undefined };
}
exports.ModelStorageMetrics = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.modelId !== "") {
            writer.uint32(10).string(message.modelId);
        }
        if (message.sizeOnDiskBytes !== 0) {
            writer.uint32(16).int64(message.sizeOnDiskBytes);
        }
        if (message.lastUsedMs !== undefined) {
            writer.uint32(24).int64(message.lastUsedMs);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseModelStorageMetrics();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.sizeOnDiskBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.lastUsedMs = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
            sizeOnDiskBytes: isSet(object.sizeOnDiskBytes)
                ? globalThis.Number(object.sizeOnDiskBytes)
                : isSet(object.size_on_disk_bytes)
                    ? globalThis.Number(object.size_on_disk_bytes)
                    : 0,
            lastUsedMs: isSet(object.lastUsedMs)
                ? globalThis.Number(object.lastUsedMs)
                : isSet(object.last_used_ms)
                    ? globalThis.Number(object.last_used_ms)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        if (message.sizeOnDiskBytes !== 0) {
            obj.sizeOnDiskBytes = Math.round(message.sizeOnDiskBytes);
        }
        if (message.lastUsedMs !== undefined) {
            obj.lastUsedMs = Math.round(message.lastUsedMs);
        }
        return obj;
    },
    create(base) {
        return exports.ModelStorageMetrics.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseModelStorageMetrics();
        message.modelId = object.modelId ?? "";
        message.sizeOnDiskBytes = object.sizeOnDiskBytes ?? 0;
        message.lastUsedMs = object.lastUsedMs ?? undefined;
        return message;
    },
};
function createBaseStorageInfo() {
    return { app: undefined, device: undefined, models: [], totalModels: 0, totalModelsBytes: 0 };
}
exports.StorageInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.app !== undefined) {
            exports.AppStorageInfo.encode(message.app, writer.uint32(10).fork()).join();
        }
        if (message.device !== undefined) {
            exports.DeviceStorageInfo.encode(message.device, writer.uint32(18).fork()).join();
        }
        for (const v of message.models) {
            exports.ModelStorageMetrics.encode(v, writer.uint32(26).fork()).join();
        }
        if (message.totalModels !== 0) {
            writer.uint32(32).int32(message.totalModels);
        }
        if (message.totalModelsBytes !== 0) {
            writer.uint32(40).int64(message.totalModelsBytes);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.app = exports.AppStorageInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.device = exports.DeviceStorageInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.models.push(exports.ModelStorageMetrics.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.totalModels = reader.int32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.totalModelsBytes = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            app: isSet(object.app) ? exports.AppStorageInfo.fromJSON(object.app) : undefined,
            device: isSet(object.device) ? exports.DeviceStorageInfo.fromJSON(object.device) : undefined,
            models: globalThis.Array.isArray(object?.models)
                ? object.models.map((e) => exports.ModelStorageMetrics.fromJSON(e))
                : [],
            totalModels: isSet(object.totalModels)
                ? globalThis.Number(object.totalModels)
                : isSet(object.total_models)
                    ? globalThis.Number(object.total_models)
                    : 0,
            totalModelsBytes: isSet(object.totalModelsBytes)
                ? globalThis.Number(object.totalModelsBytes)
                : isSet(object.total_models_bytes)
                    ? globalThis.Number(object.total_models_bytes)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.app !== undefined) {
            obj.app = exports.AppStorageInfo.toJSON(message.app);
        }
        if (message.device !== undefined) {
            obj.device = exports.DeviceStorageInfo.toJSON(message.device);
        }
        if (message.models?.length) {
            obj.models = message.models.map((e) => exports.ModelStorageMetrics.toJSON(e));
        }
        if (message.totalModels !== 0) {
            obj.totalModels = Math.round(message.totalModels);
        }
        if (message.totalModelsBytes !== 0) {
            obj.totalModelsBytes = Math.round(message.totalModelsBytes);
        }
        return obj;
    },
    create(base) {
        return exports.StorageInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageInfo();
        message.app = (object.app !== undefined && object.app !== null)
            ? exports.AppStorageInfo.fromPartial(object.app)
            : undefined;
        message.device = (object.device !== undefined && object.device !== null)
            ? exports.DeviceStorageInfo.fromPartial(object.device)
            : undefined;
        message.models = object.models?.map((e) => exports.ModelStorageMetrics.fromPartial(e)) || [];
        message.totalModels = object.totalModels ?? 0;
        message.totalModelsBytes = object.totalModelsBytes ?? 0;
        return message;
    },
};
function createBaseStorageAvailability() {
    return {
        isAvailable: false,
        requiredBytes: 0,
        availableBytes: 0,
        warningMessage: undefined,
        recommendation: undefined,
        shortfallBytes: 0,
        requiredToAvailableRatio: 0,
    };
}
exports.StorageAvailability = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.isAvailable !== false) {
            writer.uint32(8).bool(message.isAvailable);
        }
        if (message.requiredBytes !== 0) {
            writer.uint32(16).int64(message.requiredBytes);
        }
        if (message.availableBytes !== 0) {
            writer.uint32(24).int64(message.availableBytes);
        }
        if (message.warningMessage !== undefined) {
            writer.uint32(34).string(message.warningMessage);
        }
        if (message.recommendation !== undefined) {
            writer.uint32(42).string(message.recommendation);
        }
        if (message.shortfallBytes !== 0) {
            writer.uint32(48).int64(message.shortfallBytes);
        }
        if (message.requiredToAvailableRatio !== 0) {
            writer.uint32(61).float(message.requiredToAvailableRatio);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageAvailability();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.isAvailable = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.requiredBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.availableBytes = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.warningMessage = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.recommendation = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.shortfallBytes = longToNumber(reader.int64());
                    continue;
                }
                case 7: {
                    if (tag !== 61) {
                        break;
                    }
                    message.requiredToAvailableRatio = reader.float();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            isAvailable: isSet(object.isAvailable)
                ? globalThis.Boolean(object.isAvailable)
                : isSet(object.is_available)
                    ? globalThis.Boolean(object.is_available)
                    : false,
            requiredBytes: isSet(object.requiredBytes)
                ? globalThis.Number(object.requiredBytes)
                : isSet(object.required_bytes)
                    ? globalThis.Number(object.required_bytes)
                    : 0,
            availableBytes: isSet(object.availableBytes)
                ? globalThis.Number(object.availableBytes)
                : isSet(object.available_bytes)
                    ? globalThis.Number(object.available_bytes)
                    : 0,
            warningMessage: isSet(object.warningMessage)
                ? globalThis.String(object.warningMessage)
                : isSet(object.warning_message)
                    ? globalThis.String(object.warning_message)
                    : undefined,
            recommendation: isSet(object.recommendation) ? globalThis.String(object.recommendation) : undefined,
            shortfallBytes: isSet(object.shortfallBytes)
                ? globalThis.Number(object.shortfallBytes)
                : isSet(object.shortfall_bytes)
                    ? globalThis.Number(object.shortfall_bytes)
                    : 0,
            requiredToAvailableRatio: isSet(object.requiredToAvailableRatio)
                ? globalThis.Number(object.requiredToAvailableRatio)
                : isSet(object.required_to_available_ratio)
                    ? globalThis.Number(object.required_to_available_ratio)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.isAvailable !== false) {
            obj.isAvailable = message.isAvailable;
        }
        if (message.requiredBytes !== 0) {
            obj.requiredBytes = Math.round(message.requiredBytes);
        }
        if (message.availableBytes !== 0) {
            obj.availableBytes = Math.round(message.availableBytes);
        }
        if (message.warningMessage !== undefined) {
            obj.warningMessage = message.warningMessage;
        }
        if (message.recommendation !== undefined) {
            obj.recommendation = message.recommendation;
        }
        if (message.shortfallBytes !== 0) {
            obj.shortfallBytes = Math.round(message.shortfallBytes);
        }
        if (message.requiredToAvailableRatio !== 0) {
            obj.requiredToAvailableRatio = message.requiredToAvailableRatio;
        }
        return obj;
    },
    create(base) {
        return exports.StorageAvailability.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageAvailability();
        message.isAvailable = object.isAvailable ?? false;
        message.requiredBytes = object.requiredBytes ?? 0;
        message.availableBytes = object.availableBytes ?? 0;
        message.warningMessage = object.warningMessage ?? undefined;
        message.recommendation = object.recommendation ?? undefined;
        message.shortfallBytes = object.shortfallBytes ?? 0;
        message.requiredToAvailableRatio = object.requiredToAvailableRatio ?? 0;
        return message;
    },
};
function createBaseStorageInfoRequest() {
    return { includeDevice: false, includeApp: false, includeModels: false, includeCache: false };
}
exports.StorageInfoRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.includeDevice !== false) {
            writer.uint32(8).bool(message.includeDevice);
        }
        if (message.includeApp !== false) {
            writer.uint32(16).bool(message.includeApp);
        }
        if (message.includeModels !== false) {
            writer.uint32(24).bool(message.includeModels);
        }
        if (message.includeCache !== false) {
            writer.uint32(32).bool(message.includeCache);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageInfoRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.includeDevice = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.includeApp = reader.bool();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.includeModels = reader.bool();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.includeCache = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            includeDevice: isSet(object.includeDevice)
                ? globalThis.Boolean(object.includeDevice)
                : isSet(object.include_device)
                    ? globalThis.Boolean(object.include_device)
                    : false,
            includeApp: isSet(object.includeApp)
                ? globalThis.Boolean(object.includeApp)
                : isSet(object.include_app)
                    ? globalThis.Boolean(object.include_app)
                    : false,
            includeModels: isSet(object.includeModels)
                ? globalThis.Boolean(object.includeModels)
                : isSet(object.include_models)
                    ? globalThis.Boolean(object.include_models)
                    : false,
            includeCache: isSet(object.includeCache)
                ? globalThis.Boolean(object.includeCache)
                : isSet(object.include_cache)
                    ? globalThis.Boolean(object.include_cache)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.includeDevice !== false) {
            obj.includeDevice = message.includeDevice;
        }
        if (message.includeApp !== false) {
            obj.includeApp = message.includeApp;
        }
        if (message.includeModels !== false) {
            obj.includeModels = message.includeModels;
        }
        if (message.includeCache !== false) {
            obj.includeCache = message.includeCache;
        }
        return obj;
    },
    create(base) {
        return exports.StorageInfoRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageInfoRequest();
        message.includeDevice = object.includeDevice ?? false;
        message.includeApp = object.includeApp ?? false;
        message.includeModels = object.includeModels ?? false;
        message.includeCache = object.includeCache ?? false;
        return message;
    },
};
function createBaseStorageInfoResult() {
    return { success: false, info: undefined, errorMessage: "", warnings: [] };
}
exports.StorageInfoResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.info !== undefined) {
            exports.StorageInfo.encode(message.info, writer.uint32(18).fork()).join();
        }
        if (message.errorMessage !== "") {
            writer.uint32(26).string(message.errorMessage);
        }
        for (const v of message.warnings) {
            writer.uint32(34).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageInfoResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.info = exports.StorageInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.errorMessage = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.warnings.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            info: isSet(object.info) ? exports.StorageInfo.fromJSON(object.info) : undefined,
            errorMessage: isSet(object.errorMessage)
                ? globalThis.String(object.errorMessage)
                : isSet(object.error_message)
                    ? globalThis.String(object.error_message)
                    : "",
            warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e) => globalThis.String(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.info !== undefined) {
            obj.info = exports.StorageInfo.toJSON(message.info);
        }
        if (message.errorMessage !== "") {
            obj.errorMessage = message.errorMessage;
        }
        if (message.warnings?.length) {
            obj.warnings = message.warnings;
        }
        return obj;
    },
    create(base) {
        return exports.StorageInfoResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageInfoResult();
        message.success = object.success ?? false;
        message.info = (object.info !== undefined && object.info !== null)
            ? exports.StorageInfo.fromPartial(object.info)
            : undefined;
        message.errorMessage = object.errorMessage ?? "";
        message.warnings = object.warnings?.map((e) => e) || [];
        return message;
    },
};
function createBaseStorageAvailabilityRequest() {
    return {
        modelId: "",
        requiredBytes: 0,
        safetyMargin: 0,
        includeExistingModelBytes: false,
        includeDeletePlan: false,
        allowCacheReclamation: false,
    };
}
exports.StorageAvailabilityRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.modelId !== "") {
            writer.uint32(10).string(message.modelId);
        }
        if (message.requiredBytes !== 0) {
            writer.uint32(16).int64(message.requiredBytes);
        }
        if (message.safetyMargin !== 0) {
            writer.uint32(25).double(message.safetyMargin);
        }
        if (message.includeExistingModelBytes !== false) {
            writer.uint32(32).bool(message.includeExistingModelBytes);
        }
        if (message.includeDeletePlan !== false) {
            writer.uint32(40).bool(message.includeDeletePlan);
        }
        if (message.allowCacheReclamation !== false) {
            writer.uint32(48).bool(message.allowCacheReclamation);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageAvailabilityRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.requiredBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 25) {
                        break;
                    }
                    message.safetyMargin = reader.double();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.includeExistingModelBytes = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.includeDeletePlan = reader.bool();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.allowCacheReclamation = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
            requiredBytes: isSet(object.requiredBytes)
                ? globalThis.Number(object.requiredBytes)
                : isSet(object.required_bytes)
                    ? globalThis.Number(object.required_bytes)
                    : 0,
            safetyMargin: isSet(object.safetyMargin)
                ? globalThis.Number(object.safetyMargin)
                : isSet(object.safety_margin)
                    ? globalThis.Number(object.safety_margin)
                    : 0,
            includeExistingModelBytes: isSet(object.includeExistingModelBytes)
                ? globalThis.Boolean(object.includeExistingModelBytes)
                : isSet(object.include_existing_model_bytes)
                    ? globalThis.Boolean(object.include_existing_model_bytes)
                    : false,
            includeDeletePlan: isSet(object.includeDeletePlan)
                ? globalThis.Boolean(object.includeDeletePlan)
                : isSet(object.include_delete_plan)
                    ? globalThis.Boolean(object.include_delete_plan)
                    : false,
            allowCacheReclamation: isSet(object.allowCacheReclamation)
                ? globalThis.Boolean(object.allowCacheReclamation)
                : isSet(object.allow_cache_reclamation)
                    ? globalThis.Boolean(object.allow_cache_reclamation)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        if (message.requiredBytes !== 0) {
            obj.requiredBytes = Math.round(message.requiredBytes);
        }
        if (message.safetyMargin !== 0) {
            obj.safetyMargin = message.safetyMargin;
        }
        if (message.includeExistingModelBytes !== false) {
            obj.includeExistingModelBytes = message.includeExistingModelBytes;
        }
        if (message.includeDeletePlan !== false) {
            obj.includeDeletePlan = message.includeDeletePlan;
        }
        if (message.allowCacheReclamation !== false) {
            obj.allowCacheReclamation = message.allowCacheReclamation;
        }
        return obj;
    },
    create(base) {
        return exports.StorageAvailabilityRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageAvailabilityRequest();
        message.modelId = object.modelId ?? "";
        message.requiredBytes = object.requiredBytes ?? 0;
        message.safetyMargin = object.safetyMargin ?? 0;
        message.includeExistingModelBytes = object.includeExistingModelBytes ?? false;
        message.includeDeletePlan = object.includeDeletePlan ?? false;
        message.allowCacheReclamation = object.allowCacheReclamation ?? false;
        return message;
    },
};
function createBaseStorageAvailabilityResult() {
    return { success: false, availability: undefined, warnings: [], errorMessage: "", deletePlan: undefined };
}
exports.StorageAvailabilityResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.availability !== undefined) {
            exports.StorageAvailability.encode(message.availability, writer.uint32(18).fork()).join();
        }
        for (const v of message.warnings) {
            writer.uint32(26).string(v);
        }
        if (message.errorMessage !== "") {
            writer.uint32(34).string(message.errorMessage);
        }
        if (message.deletePlan !== undefined) {
            exports.StorageDeletePlan.encode(message.deletePlan, writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageAvailabilityResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.availability = exports.StorageAvailability.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.warnings.push(reader.string());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.errorMessage = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.deletePlan = exports.StorageDeletePlan.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            availability: isSet(object.availability) ? exports.StorageAvailability.fromJSON(object.availability) : undefined,
            warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e) => globalThis.String(e)) : [],
            errorMessage: isSet(object.errorMessage)
                ? globalThis.String(object.errorMessage)
                : isSet(object.error_message)
                    ? globalThis.String(object.error_message)
                    : "",
            deletePlan: isSet(object.deletePlan)
                ? exports.StorageDeletePlan.fromJSON(object.deletePlan)
                : isSet(object.delete_plan)
                    ? exports.StorageDeletePlan.fromJSON(object.delete_plan)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.availability !== undefined) {
            obj.availability = exports.StorageAvailability.toJSON(message.availability);
        }
        if (message.warnings?.length) {
            obj.warnings = message.warnings;
        }
        if (message.errorMessage !== "") {
            obj.errorMessage = message.errorMessage;
        }
        if (message.deletePlan !== undefined) {
            obj.deletePlan = exports.StorageDeletePlan.toJSON(message.deletePlan);
        }
        return obj;
    },
    create(base) {
        return exports.StorageAvailabilityResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageAvailabilityResult();
        message.success = object.success ?? false;
        message.availability = (object.availability !== undefined && object.availability !== null)
            ? exports.StorageAvailability.fromPartial(object.availability)
            : undefined;
        message.warnings = object.warnings?.map((e) => e) || [];
        message.errorMessage = object.errorMessage ?? "";
        message.deletePlan = (object.deletePlan !== undefined && object.deletePlan !== null)
            ? exports.StorageDeletePlan.fromPartial(object.deletePlan)
            : undefined;
        return message;
    },
};
function createBaseStorageDeletePlanRequest() {
    return {
        modelIds: [],
        requiredBytes: 0,
        includeCache: false,
        oldestFirst: false,
        allowLoadedModels: false,
        includeDownloadPartials: false,
    };
}
exports.StorageDeletePlanRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.modelIds) {
            writer.uint32(10).string(v);
        }
        if (message.requiredBytes !== 0) {
            writer.uint32(16).int64(message.requiredBytes);
        }
        if (message.includeCache !== false) {
            writer.uint32(24).bool(message.includeCache);
        }
        if (message.oldestFirst !== false) {
            writer.uint32(32).bool(message.oldestFirst);
        }
        if (message.allowLoadedModels !== false) {
            writer.uint32(40).bool(message.allowLoadedModels);
        }
        if (message.includeDownloadPartials !== false) {
            writer.uint32(48).bool(message.includeDownloadPartials);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageDeletePlanRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.modelIds.push(reader.string());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.requiredBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.includeCache = reader.bool();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.oldestFirst = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.allowLoadedModels = reader.bool();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.includeDownloadPartials = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelIds: globalThis.Array.isArray(object?.modelIds)
                ? object.modelIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.model_ids)
                    ? object.model_ids.map((e) => globalThis.String(e))
                    : [],
            requiredBytes: isSet(object.requiredBytes)
                ? globalThis.Number(object.requiredBytes)
                : isSet(object.required_bytes)
                    ? globalThis.Number(object.required_bytes)
                    : 0,
            includeCache: isSet(object.includeCache)
                ? globalThis.Boolean(object.includeCache)
                : isSet(object.include_cache)
                    ? globalThis.Boolean(object.include_cache)
                    : false,
            oldestFirst: isSet(object.oldestFirst)
                ? globalThis.Boolean(object.oldestFirst)
                : isSet(object.oldest_first)
                    ? globalThis.Boolean(object.oldest_first)
                    : false,
            allowLoadedModels: isSet(object.allowLoadedModels)
                ? globalThis.Boolean(object.allowLoadedModels)
                : isSet(object.allow_loaded_models)
                    ? globalThis.Boolean(object.allow_loaded_models)
                    : false,
            includeDownloadPartials: isSet(object.includeDownloadPartials)
                ? globalThis.Boolean(object.includeDownloadPartials)
                : isSet(object.include_download_partials)
                    ? globalThis.Boolean(object.include_download_partials)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelIds?.length) {
            obj.modelIds = message.modelIds;
        }
        if (message.requiredBytes !== 0) {
            obj.requiredBytes = Math.round(message.requiredBytes);
        }
        if (message.includeCache !== false) {
            obj.includeCache = message.includeCache;
        }
        if (message.oldestFirst !== false) {
            obj.oldestFirst = message.oldestFirst;
        }
        if (message.allowLoadedModels !== false) {
            obj.allowLoadedModels = message.allowLoadedModels;
        }
        if (message.includeDownloadPartials !== false) {
            obj.includeDownloadPartials = message.includeDownloadPartials;
        }
        return obj;
    },
    create(base) {
        return exports.StorageDeletePlanRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageDeletePlanRequest();
        message.modelIds = object.modelIds?.map((e) => e) || [];
        message.requiredBytes = object.requiredBytes ?? 0;
        message.includeCache = object.includeCache ?? false;
        message.oldestFirst = object.oldestFirst ?? false;
        message.allowLoadedModels = object.allowLoadedModels ?? false;
        message.includeDownloadPartials = object.includeDownloadPartials ?? false;
        return message;
    },
};
function createBaseStorageDeleteCandidate() {
    return {
        modelId: "",
        reclaimableBytes: 0,
        lastUsedMs: undefined,
        isLoaded: false,
        localPath: "",
        requiresUnload: false,
        requiresPlatformDelete: false,
        storageKey: "",
    };
}
exports.StorageDeleteCandidate = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.modelId !== "") {
            writer.uint32(10).string(message.modelId);
        }
        if (message.reclaimableBytes !== 0) {
            writer.uint32(16).int64(message.reclaimableBytes);
        }
        if (message.lastUsedMs !== undefined) {
            writer.uint32(24).int64(message.lastUsedMs);
        }
        if (message.isLoaded !== false) {
            writer.uint32(32).bool(message.isLoaded);
        }
        if (message.localPath !== "") {
            writer.uint32(42).string(message.localPath);
        }
        if (message.requiresUnload !== false) {
            writer.uint32(48).bool(message.requiresUnload);
        }
        if (message.requiresPlatformDelete !== false) {
            writer.uint32(56).bool(message.requiresPlatformDelete);
        }
        if (message.storageKey !== "") {
            writer.uint32(66).string(message.storageKey);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageDeleteCandidate();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.reclaimableBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.lastUsedMs = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.isLoaded = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.localPath = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.requiresUnload = reader.bool();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.requiresPlatformDelete = reader.bool();
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.storageKey = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
            reclaimableBytes: isSet(object.reclaimableBytes)
                ? globalThis.Number(object.reclaimableBytes)
                : isSet(object.reclaimable_bytes)
                    ? globalThis.Number(object.reclaimable_bytes)
                    : 0,
            lastUsedMs: isSet(object.lastUsedMs)
                ? globalThis.Number(object.lastUsedMs)
                : isSet(object.last_used_ms)
                    ? globalThis.Number(object.last_used_ms)
                    : undefined,
            isLoaded: isSet(object.isLoaded)
                ? globalThis.Boolean(object.isLoaded)
                : isSet(object.is_loaded)
                    ? globalThis.Boolean(object.is_loaded)
                    : false,
            localPath: isSet(object.localPath)
                ? globalThis.String(object.localPath)
                : isSet(object.local_path)
                    ? globalThis.String(object.local_path)
                    : "",
            requiresUnload: isSet(object.requiresUnload)
                ? globalThis.Boolean(object.requiresUnload)
                : isSet(object.requires_unload)
                    ? globalThis.Boolean(object.requires_unload)
                    : false,
            requiresPlatformDelete: isSet(object.requiresPlatformDelete)
                ? globalThis.Boolean(object.requiresPlatformDelete)
                : isSet(object.requires_platform_delete)
                    ? globalThis.Boolean(object.requires_platform_delete)
                    : false,
            storageKey: isSet(object.storageKey)
                ? globalThis.String(object.storageKey)
                : isSet(object.storage_key)
                    ? globalThis.String(object.storage_key)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        if (message.reclaimableBytes !== 0) {
            obj.reclaimableBytes = Math.round(message.reclaimableBytes);
        }
        if (message.lastUsedMs !== undefined) {
            obj.lastUsedMs = Math.round(message.lastUsedMs);
        }
        if (message.isLoaded !== false) {
            obj.isLoaded = message.isLoaded;
        }
        if (message.localPath !== "") {
            obj.localPath = message.localPath;
        }
        if (message.requiresUnload !== false) {
            obj.requiresUnload = message.requiresUnload;
        }
        if (message.requiresPlatformDelete !== false) {
            obj.requiresPlatformDelete = message.requiresPlatformDelete;
        }
        if (message.storageKey !== "") {
            obj.storageKey = message.storageKey;
        }
        return obj;
    },
    create(base) {
        return exports.StorageDeleteCandidate.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageDeleteCandidate();
        message.modelId = object.modelId ?? "";
        message.reclaimableBytes = object.reclaimableBytes ?? 0;
        message.lastUsedMs = object.lastUsedMs ?? undefined;
        message.isLoaded = object.isLoaded ?? false;
        message.localPath = object.localPath ?? "";
        message.requiresUnload = object.requiresUnload ?? false;
        message.requiresPlatformDelete = object.requiresPlatformDelete ?? false;
        message.storageKey = object.storageKey ?? "";
        return message;
    },
};
function createBaseStorageDeletePlan() {
    return {
        canReclaimRequiredBytes: false,
        requiredBytes: 0,
        reclaimableBytes: 0,
        candidates: [],
        warnings: [],
        errorMessage: "",
        requiresUnload: false,
        requiresPlatformDelete: false,
        candidateCount: 0,
    };
}
exports.StorageDeletePlan = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.canReclaimRequiredBytes !== false) {
            writer.uint32(8).bool(message.canReclaimRequiredBytes);
        }
        if (message.requiredBytes !== 0) {
            writer.uint32(16).int64(message.requiredBytes);
        }
        if (message.reclaimableBytes !== 0) {
            writer.uint32(24).int64(message.reclaimableBytes);
        }
        for (const v of message.candidates) {
            exports.StorageDeleteCandidate.encode(v, writer.uint32(34).fork()).join();
        }
        for (const v of message.warnings) {
            writer.uint32(42).string(v);
        }
        if (message.errorMessage !== "") {
            writer.uint32(50).string(message.errorMessage);
        }
        if (message.requiresUnload !== false) {
            writer.uint32(56).bool(message.requiresUnload);
        }
        if (message.requiresPlatformDelete !== false) {
            writer.uint32(64).bool(message.requiresPlatformDelete);
        }
        if (message.candidateCount !== 0) {
            writer.uint32(72).int32(message.candidateCount);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageDeletePlan();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.canReclaimRequiredBytes = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.requiredBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.reclaimableBytes = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.candidates.push(exports.StorageDeleteCandidate.decode(reader, reader.uint32()));
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.warnings.push(reader.string());
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.errorMessage = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.requiresUnload = reader.bool();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.requiresPlatformDelete = reader.bool();
                    continue;
                }
                case 9: {
                    if (tag !== 72) {
                        break;
                    }
                    message.candidateCount = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            canReclaimRequiredBytes: isSet(object.canReclaimRequiredBytes)
                ? globalThis.Boolean(object.canReclaimRequiredBytes)
                : isSet(object.can_reclaim_required_bytes)
                    ? globalThis.Boolean(object.can_reclaim_required_bytes)
                    : false,
            requiredBytes: isSet(object.requiredBytes)
                ? globalThis.Number(object.requiredBytes)
                : isSet(object.required_bytes)
                    ? globalThis.Number(object.required_bytes)
                    : 0,
            reclaimableBytes: isSet(object.reclaimableBytes)
                ? globalThis.Number(object.reclaimableBytes)
                : isSet(object.reclaimable_bytes)
                    ? globalThis.Number(object.reclaimable_bytes)
                    : 0,
            candidates: globalThis.Array.isArray(object?.candidates)
                ? object.candidates.map((e) => exports.StorageDeleteCandidate.fromJSON(e))
                : [],
            warnings: globalThis.Array.isArray(object?.warnings)
                ? object.warnings.map((e) => globalThis.String(e))
                : [],
            errorMessage: isSet(object.errorMessage)
                ? globalThis.String(object.errorMessage)
                : isSet(object.error_message)
                    ? globalThis.String(object.error_message)
                    : "",
            requiresUnload: isSet(object.requiresUnload)
                ? globalThis.Boolean(object.requiresUnload)
                : isSet(object.requires_unload)
                    ? globalThis.Boolean(object.requires_unload)
                    : false,
            requiresPlatformDelete: isSet(object.requiresPlatformDelete)
                ? globalThis.Boolean(object.requiresPlatformDelete)
                : isSet(object.requires_platform_delete)
                    ? globalThis.Boolean(object.requires_platform_delete)
                    : false,
            candidateCount: isSet(object.candidateCount)
                ? globalThis.Number(object.candidateCount)
                : isSet(object.candidate_count)
                    ? globalThis.Number(object.candidate_count)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.canReclaimRequiredBytes !== false) {
            obj.canReclaimRequiredBytes = message.canReclaimRequiredBytes;
        }
        if (message.requiredBytes !== 0) {
            obj.requiredBytes = Math.round(message.requiredBytes);
        }
        if (message.reclaimableBytes !== 0) {
            obj.reclaimableBytes = Math.round(message.reclaimableBytes);
        }
        if (message.candidates?.length) {
            obj.candidates = message.candidates.map((e) => exports.StorageDeleteCandidate.toJSON(e));
        }
        if (message.warnings?.length) {
            obj.warnings = message.warnings;
        }
        if (message.errorMessage !== "") {
            obj.errorMessage = message.errorMessage;
        }
        if (message.requiresUnload !== false) {
            obj.requiresUnload = message.requiresUnload;
        }
        if (message.requiresPlatformDelete !== false) {
            obj.requiresPlatformDelete = message.requiresPlatformDelete;
        }
        if (message.candidateCount !== 0) {
            obj.candidateCount = Math.round(message.candidateCount);
        }
        return obj;
    },
    create(base) {
        return exports.StorageDeletePlan.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageDeletePlan();
        message.canReclaimRequiredBytes = object.canReclaimRequiredBytes ?? false;
        message.requiredBytes = object.requiredBytes ?? 0;
        message.reclaimableBytes = object.reclaimableBytes ?? 0;
        message.candidates = object.candidates?.map((e) => exports.StorageDeleteCandidate.fromPartial(e)) || [];
        message.warnings = object.warnings?.map((e) => e) || [];
        message.errorMessage = object.errorMessage ?? "";
        message.requiresUnload = object.requiresUnload ?? false;
        message.requiresPlatformDelete = object.requiresPlatformDelete ?? false;
        message.candidateCount = object.candidateCount ?? 0;
        return message;
    },
};
function createBaseStorageDeleteRequest() {
    return {
        modelIds: [],
        deleteFiles: false,
        clearRegistryPaths: false,
        unloadIfLoaded: false,
        dryRun: false,
        plan: undefined,
        requirePlanMatch: false,
        allowPlatformDelete: false,
    };
}
exports.StorageDeleteRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.modelIds) {
            writer.uint32(10).string(v);
        }
        if (message.deleteFiles !== false) {
            writer.uint32(16).bool(message.deleteFiles);
        }
        if (message.clearRegistryPaths !== false) {
            writer.uint32(24).bool(message.clearRegistryPaths);
        }
        if (message.unloadIfLoaded !== false) {
            writer.uint32(32).bool(message.unloadIfLoaded);
        }
        if (message.dryRun !== false) {
            writer.uint32(40).bool(message.dryRun);
        }
        if (message.plan !== undefined) {
            exports.StorageDeletePlan.encode(message.plan, writer.uint32(50).fork()).join();
        }
        if (message.requirePlanMatch !== false) {
            writer.uint32(56).bool(message.requirePlanMatch);
        }
        if (message.allowPlatformDelete !== false) {
            writer.uint32(64).bool(message.allowPlatformDelete);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageDeleteRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.modelIds.push(reader.string());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.deleteFiles = reader.bool();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.clearRegistryPaths = reader.bool();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.unloadIfLoaded = reader.bool();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.dryRun = reader.bool();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.plan = exports.StorageDeletePlan.decode(reader, reader.uint32());
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.requirePlanMatch = reader.bool();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.allowPlatformDelete = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelIds: globalThis.Array.isArray(object?.modelIds)
                ? object.modelIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.model_ids)
                    ? object.model_ids.map((e) => globalThis.String(e))
                    : [],
            deleteFiles: isSet(object.deleteFiles)
                ? globalThis.Boolean(object.deleteFiles)
                : isSet(object.delete_files)
                    ? globalThis.Boolean(object.delete_files)
                    : false,
            clearRegistryPaths: isSet(object.clearRegistryPaths)
                ? globalThis.Boolean(object.clearRegistryPaths)
                : isSet(object.clear_registry_paths)
                    ? globalThis.Boolean(object.clear_registry_paths)
                    : false,
            unloadIfLoaded: isSet(object.unloadIfLoaded)
                ? globalThis.Boolean(object.unloadIfLoaded)
                : isSet(object.unload_if_loaded)
                    ? globalThis.Boolean(object.unload_if_loaded)
                    : false,
            dryRun: isSet(object.dryRun)
                ? globalThis.Boolean(object.dryRun)
                : isSet(object.dry_run)
                    ? globalThis.Boolean(object.dry_run)
                    : false,
            plan: isSet(object.plan) ? exports.StorageDeletePlan.fromJSON(object.plan) : undefined,
            requirePlanMatch: isSet(object.requirePlanMatch)
                ? globalThis.Boolean(object.requirePlanMatch)
                : isSet(object.require_plan_match)
                    ? globalThis.Boolean(object.require_plan_match)
                    : false,
            allowPlatformDelete: isSet(object.allowPlatformDelete)
                ? globalThis.Boolean(object.allowPlatformDelete)
                : isSet(object.allow_platform_delete)
                    ? globalThis.Boolean(object.allow_platform_delete)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelIds?.length) {
            obj.modelIds = message.modelIds;
        }
        if (message.deleteFiles !== false) {
            obj.deleteFiles = message.deleteFiles;
        }
        if (message.clearRegistryPaths !== false) {
            obj.clearRegistryPaths = message.clearRegistryPaths;
        }
        if (message.unloadIfLoaded !== false) {
            obj.unloadIfLoaded = message.unloadIfLoaded;
        }
        if (message.dryRun !== false) {
            obj.dryRun = message.dryRun;
        }
        if (message.plan !== undefined) {
            obj.plan = exports.StorageDeletePlan.toJSON(message.plan);
        }
        if (message.requirePlanMatch !== false) {
            obj.requirePlanMatch = message.requirePlanMatch;
        }
        if (message.allowPlatformDelete !== false) {
            obj.allowPlatformDelete = message.allowPlatformDelete;
        }
        return obj;
    },
    create(base) {
        return exports.StorageDeleteRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageDeleteRequest();
        message.modelIds = object.modelIds?.map((e) => e) || [];
        message.deleteFiles = object.deleteFiles ?? false;
        message.clearRegistryPaths = object.clearRegistryPaths ?? false;
        message.unloadIfLoaded = object.unloadIfLoaded ?? false;
        message.dryRun = object.dryRun ?? false;
        message.plan = (object.plan !== undefined && object.plan !== null)
            ? exports.StorageDeletePlan.fromPartial(object.plan)
            : undefined;
        message.requirePlanMatch = object.requirePlanMatch ?? false;
        message.allowPlatformDelete = object.allowPlatformDelete ?? false;
        return message;
    },
};
function createBaseStorageDeleteResult() {
    return {
        success: false,
        deletedBytes: 0,
        deletedModelIds: [],
        failedModelIds: [],
        warnings: [],
        errorMessage: "",
        skippedModelIds: [],
        dryRun: false,
        registryUpdated: false,
        filesDeleted: false,
    };
}
exports.StorageDeleteResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.success !== false) {
            writer.uint32(8).bool(message.success);
        }
        if (message.deletedBytes !== 0) {
            writer.uint32(16).int64(message.deletedBytes);
        }
        for (const v of message.deletedModelIds) {
            writer.uint32(26).string(v);
        }
        for (const v of message.failedModelIds) {
            writer.uint32(34).string(v);
        }
        for (const v of message.warnings) {
            writer.uint32(42).string(v);
        }
        if (message.errorMessage !== "") {
            writer.uint32(50).string(message.errorMessage);
        }
        for (const v of message.skippedModelIds) {
            writer.uint32(58).string(v);
        }
        if (message.dryRun !== false) {
            writer.uint32(64).bool(message.dryRun);
        }
        if (message.registryUpdated !== false) {
            writer.uint32(72).bool(message.registryUpdated);
        }
        if (message.filesDeleted !== false) {
            writer.uint32(80).bool(message.filesDeleted);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStorageDeleteResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.success = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.deletedBytes = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.deletedModelIds.push(reader.string());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.failedModelIds.push(reader.string());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.warnings.push(reader.string());
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.errorMessage = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.skippedModelIds.push(reader.string());
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.dryRun = reader.bool();
                    continue;
                }
                case 9: {
                    if (tag !== 72) {
                        break;
                    }
                    message.registryUpdated = reader.bool();
                    continue;
                }
                case 10: {
                    if (tag !== 80) {
                        break;
                    }
                    message.filesDeleted = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
            deletedBytes: isSet(object.deletedBytes)
                ? globalThis.Number(object.deletedBytes)
                : isSet(object.deleted_bytes)
                    ? globalThis.Number(object.deleted_bytes)
                    : 0,
            deletedModelIds: globalThis.Array.isArray(object?.deletedModelIds)
                ? object.deletedModelIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.deleted_model_ids)
                    ? object.deleted_model_ids.map((e) => globalThis.String(e))
                    : [],
            failedModelIds: globalThis.Array.isArray(object?.failedModelIds)
                ? object.failedModelIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.failed_model_ids)
                    ? object.failed_model_ids.map((e) => globalThis.String(e))
                    : [],
            warnings: globalThis.Array.isArray(object?.warnings) ? object.warnings.map((e) => globalThis.String(e)) : [],
            errorMessage: isSet(object.errorMessage)
                ? globalThis.String(object.errorMessage)
                : isSet(object.error_message)
                    ? globalThis.String(object.error_message)
                    : "",
            skippedModelIds: globalThis.Array.isArray(object?.skippedModelIds)
                ? object.skippedModelIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.skipped_model_ids)
                    ? object.skipped_model_ids.map((e) => globalThis.String(e))
                    : [],
            dryRun: isSet(object.dryRun)
                ? globalThis.Boolean(object.dryRun)
                : isSet(object.dry_run)
                    ? globalThis.Boolean(object.dry_run)
                    : false,
            registryUpdated: isSet(object.registryUpdated)
                ? globalThis.Boolean(object.registryUpdated)
                : isSet(object.registry_updated)
                    ? globalThis.Boolean(object.registry_updated)
                    : false,
            filesDeleted: isSet(object.filesDeleted)
                ? globalThis.Boolean(object.filesDeleted)
                : isSet(object.files_deleted)
                    ? globalThis.Boolean(object.files_deleted)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.success !== false) {
            obj.success = message.success;
        }
        if (message.deletedBytes !== 0) {
            obj.deletedBytes = Math.round(message.deletedBytes);
        }
        if (message.deletedModelIds?.length) {
            obj.deletedModelIds = message.deletedModelIds;
        }
        if (message.failedModelIds?.length) {
            obj.failedModelIds = message.failedModelIds;
        }
        if (message.warnings?.length) {
            obj.warnings = message.warnings;
        }
        if (message.errorMessage !== "") {
            obj.errorMessage = message.errorMessage;
        }
        if (message.skippedModelIds?.length) {
            obj.skippedModelIds = message.skippedModelIds;
        }
        if (message.dryRun !== false) {
            obj.dryRun = message.dryRun;
        }
        if (message.registryUpdated !== false) {
            obj.registryUpdated = message.registryUpdated;
        }
        if (message.filesDeleted !== false) {
            obj.filesDeleted = message.filesDeleted;
        }
        return obj;
    },
    create(base) {
        return exports.StorageDeleteResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStorageDeleteResult();
        message.success = object.success ?? false;
        message.deletedBytes = object.deletedBytes ?? 0;
        message.deletedModelIds = object.deletedModelIds?.map((e) => e) || [];
        message.failedModelIds = object.failedModelIds?.map((e) => e) || [];
        message.warnings = object.warnings?.map((e) => e) || [];
        message.errorMessage = object.errorMessage ?? "";
        message.skippedModelIds = object.skippedModelIds?.map((e) => e) || [];
        message.dryRun = object.dryRun ?? false;
        message.registryUpdated = object.registryUpdated ?? false;
        message.filesDeleted = object.filesDeleted ?? false;
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
