# TypeScript build output (regenerate via `yarn build` from the repo root)
lib/

# TypeScript incremental build state
*.tsbuildinfo

# Vendored iOS transport — regenerated by the `prepack` script from the shared
# canonical (sdk/shared/ios/URLSessionHttpTransport/). The dev build falls back
# to the shared copy via __has_include, so this vendored copy must not be tracked.
ios/URLSessionHttpTransport/URLSessionHttpTransportImpl.inc.mm
