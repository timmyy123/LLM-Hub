// GENERATED by idl/codegen/generate_dart_convenience.py — DO NOT EDIT.
//
// Convenience accessors derived from RunAnywhere's custom proto
// annotations (see idl/rac_options.proto):
//   - wireString getter / <enumName>FromWireString reverse factory
//                              (rac_wire_string)
//   - displayName getter       (rac_display_name)
//   - analyticsKey getter      (rac_analytics_key)
//   - defaults() factory       (rac_default)
//   - validate()               (rac_required / rac_min / rac_max /
//                              rac_min_float / rac_max_float)

// ignore_for_file: prefer_const_constructors, unnecessary_this
// ignore_for_file: constant_identifier_names, non_constant_identifier_names

import 'package:fixnum/fixnum.dart';
import 'package:runanywhere/foundation/errors/sdk_exception.dart';
import 'package:runanywhere/generated/test_options.pb.dart';

extension RacTestModeWireString on RacTestMode {
  String get wireString {
    switch (this) {
      case RacTestMode.RAC_TEST_MODE_UNSPECIFIED:
        return 'unspecified';
      case RacTestMode.RAC_TEST_MODE_FAST:
        return 'fast';
      case RacTestMode.RAC_TEST_MODE_BALANCED:
        return 'balanced';
      case RacTestMode.RAC_TEST_MODE_ACCURATE:
        return 'accurate';
    }
    return '';
  }
}

extension RacTestModeDisplayName on RacTestMode {
  String get displayName {
    switch (this) {
      case RacTestMode.RAC_TEST_MODE_UNSPECIFIED:
        return 'Unspecified';
      case RacTestMode.RAC_TEST_MODE_FAST:
        return 'Fast';
      case RacTestMode.RAC_TEST_MODE_BALANCED:
        return 'Balanced';
      case RacTestMode.RAC_TEST_MODE_ACCURATE:
        return 'Accurate';
    }
    return '';
  }
}

extension RacTestModeAnalyticsKey on RacTestMode {
  String get analyticsKey {
    switch (this) {
      case RacTestMode.RAC_TEST_MODE_UNSPECIFIED:
        return 'unspecified';
      case RacTestMode.RAC_TEST_MODE_FAST:
        return 'fast';
      case RacTestMode.RAC_TEST_MODE_BALANCED:
        return 'balanced';
      case RacTestMode.RAC_TEST_MODE_ACCURATE:
        return 'accurate';
    }
    return '';
  }
}

RacTestMode? racTestModeFromWireString(String value) {
  switch (value.toLowerCase()) {
    case 'unspecified':
      return RacTestMode.RAC_TEST_MODE_UNSPECIFIED;
    case 'fast':
      return RacTestMode.RAC_TEST_MODE_FAST;
    case 'balanced':
      return RacTestMode.RAC_TEST_MODE_BALANCED;
    case 'accurate':
      return RacTestMode.RAC_TEST_MODE_ACCURATE;
  }
  return null;
}

extension RacTestConfigConvenience on RacTestConfig {
  static RacTestConfig defaults() {
    final r = RacTestConfig();
    r.nameString = 'default-name';
    r.retriesInt32 = 3;
    r.limitInt64 = Int64(1024);
    r.thresholdFloat = 0.5;
    r.precisionDouble = 0.7;
    r.enableFeatureBool = true;
    r.modeEnum = RacTestMode.RAC_TEST_MODE_BALANCED;
    return r;
  }
}

extension RacTestConfigValidate on RacTestConfig {
  void validate() {
    if (nameString.isEmpty) {
      throw SDKException.validationFailed(
        'name_string is required',
        fieldPath: 'RacTestConfig.name_string',
      );
    }
    if (retriesInt32 < 0 || retriesInt32 > 10) {
      throw SDKException.validationFailed(
        'retries_int32 must be in 0...10 (got $retriesInt32)',
        fieldPath: 'RacTestConfig.retries_int32',
      );
    }
    if (limitInt64 < Int64(1) || limitInt64 > Int64(65536)) {
      throw SDKException.validationFailed(
        'limit_int64 must be in 1...65536 (got $limitInt64)',
        fieldPath: 'RacTestConfig.limit_int64',
      );
    }
    if (thresholdFloat < 0.0 || thresholdFloat > 1.0) {
      throw SDKException.validationFailed(
        'threshold_float must be in 0.0...1.0 (got $thresholdFloat)',
        fieldPath: 'RacTestConfig.threshold_float',
      );
    }
    if (precisionDouble < 0.0 || precisionDouble > 1.0) {
      throw SDKException.validationFailed(
        'precision_double must be in 0.0...1.0 (got $precisionDouble)',
        fieldPath: 'RacTestConfig.precision_double',
      );
    }
  }
}
